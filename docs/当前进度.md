# 当前进度记录

## 〇、2026-06-01 补充修复：课程直播外接 API 回复约束与诊断增强

### 1. 本次修改日期

2026-06-01

### 2. 本次修改目标

针对“课程直播产品升级”在外接被测模型 API 时效果不稳定的问题，增强外部模型提示上下文、custom endpoint payload 和返回后修复逻辑，同时避免 API 未配置或调用失败时在禁止 fallback 的情况下静默退回 mock。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/rule_judge.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `openai_compatible` / `custom_endpoint` 在缺少关键配置且 `TARGET_MODEL_ALLOW_FALLBACK=false` 时会抛出可诊断错误，不再静默使用 mock。
- 兼容 `qianwen` / `qwen` / `dashscope` / `tongyi` 作为 `openai_compatible` provider 别名，避免 `.env` 旧写法被误归一化为 mock。
- 对 `TARGET_MODEL_BASE_URL` / `TARGET_MODEL_ENDPOINT` 做行内注释清理，避免 `https://...  # comment` 被当成完整 URL。
- 外接模型连接失败时不再只返回 `Connection error.`，会返回 Base URL、Model、Qwen/DashScope 常见 base_url 提示、网络/代理/证书排查建议和原始错误。
- 课程直播 OpenAI prompt 增加“本轮回复契约”，包含当前阶段、阶段要求、推荐下一句和只答一个信息点的约束。
- `custom_endpoint` payload 增加 `reply_contract` 和 `prompt_messages`，让外部服务可直接使用与 OpenAI compatible 一致的强约束上下文。
- 课程直播外部回复后处理增强：用户问直播区别但回复缺少 5-10 秒 / 1-2 秒关键事实时，会修复为当前阶段应答；用户问费用但回复未说明费用规则时，会修复为“低延迟费用略高，以页面为准。”。
- 课程直播串场 fallback 调整为规范话术：“麻烦您帮忙转达负责人，直播发布页升级了。”
- 将课程直播“后台低延迟知情确认”从长句改为“后台已走低延迟，您知道吗？”，并同步更新阶段识别和规则命中关键词，避免完整流程出现超长话术。

### 5. 当前验证结果

- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest backend\tests\test_model_providers.py backend\tests\test_active_rules.py -q`，结果 `49 passed`。
- 后端全量测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest backend\tests -q`，结果 `108 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 前端构建：`npm run build` 通过，仍保留 Vite 既有 chunk size / PURE 注释 warning。
- 测试过程中仍有 `.pytest_cache` 写入被拒绝的 pytest warning，不影响用例结果。

### 6. 当前仍存在的问题

- 本次没有真实调用外部 API，只通过 prompt/payload 结构和后处理回归测试验证链路；仍建议用实际 Qwen/DeepSeek/GPT compatible 配置跑课程直播完整流程复测。
- `custom_endpoint` 是否采纳 `prompt_messages` 取决于外部服务实现；后端已提供完整约束上下文，但外部服务若忽略该字段，仍可能需要适配。

### 7. 下一步建议

- 用真实外接 API 跑课程直播“负责人正常沟通”“追问直播区别”“费用问题”“商家说在开车”四类用例，重点看 `target_fallback_used`、`reply_contract` 和报告失败规则。
- 如果真实模型仍经常答偏，可继续把 `reply_contract` 中的 `recommended_reply` 做成更严格的 “must_follow_when_stage_matches” 字段。

AGENTS.md 更新说明：
- 本次属于外接 API 约束、诊断和课程直播话术修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-28 补充修复：课程直播本地规则模拟打断与流程记忆

### 1. 本次修改日期

2026-05-28

### 2. 本次修改目标

暂不接外部 API，先把课程直播任务的本地规则版用户模拟器、被测模型 mock 和流程记忆跑顺。重点支持任意 Step 的随机追问/打断/忙碌/开车等事件，并保证正常全流程不会被通用触发条件误判为异常退出。

### 3. 修改文件列表

- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/memory_service.py`
- `backend/app/services/conversation_memory.py`
- `backend/app/services/course_flow.py`
- `frontend/src/components/ConversationTimeline.vue`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_runs.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 用户模拟器新增 `choose_user_event(...)`，支持按 run seed 复现的 `normal / ask_question / interrupt / busy / driving / price_sensitive / confused / technical_issue` 事件。
- 开车与忙碌只从用例名称、用户画像、初始话术等身份字段强制触发，避免完整流程用例的通用触发说明误判为“开车”。
- 开车事件会返回拒绝继续沟通状态，并在记忆中记录 `user_event / current_step / interrupted / pending_return_step / next_best_action` 等字段。
- 课程 mock 被测模型优先处理开车、忙碌、真正打断和第三方配置追问；普通主线追问继续按阶段推进，保留 5-10 秒、1-2 秒等分轮说明。
- 第三方配置路径保持一步一答：进入【我的】、点直播平台管理、选择【直播平台】、勾选低延迟并保存。
- RunConsole 时间线元数据可展示 `user_event`、当前步骤和打断回流信息。
- 课程完整流程完成判定兼容分步配置路径词，如“进【我的】”“直播平台管理”“勾选低延迟”。

### 5. 本次验证结果

- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_active_rules.py -q`，结果 `37 passed`。
- 后端全量测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `101 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 前端构建：`npm run build` 通过，仍保留 Vite 既有 chunk size / PURE 注释警告。

### 6. 当前仍存在的问题

- 本阶段只修本地规则版模拟链路，未接外部被测模型 API。
- 随机事件已做 seed 复现，但演示时仍建议用固定 run/case 复现特定分支。

AGENTS.md 更新说明：
- 本次属于当前评测链路实现与测试补强，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-28 补充修复：课程直播简称任务路由 404

### 1. 本次修改日期

2026-05-28

### 2. 本次修改目标

继续修复 TaskDetail 旧链接兼容问题。实际日志显示前端请求的是 `/api/tasks/课程直播任务id`，属于“任务简称 + 多余 id 后缀”，不是完整任务名，因此前一次完整任务名兼容仍会返回 404。

### 3. 修改文件列表

- `backend/app/api/tasks.py`
- `backend/tests/test_instruction_parser.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `_resolve_task(...)` 在精确任务名、去尾部 `id`、包含匹配都失败后，根据 key 中的业务关键词推断任务类型。
- 包含“课程 / 直播 / 低延迟 / 机构 / 负责人”时兜底返回 `course_platform_outbound` 任务。
- 包含“飞毛腿 / 骑手 / 配送 / 派单”时兜底返回 `rider_outbound` 任务。
- 新增回归测试覆盖 `/api/tasks/课程直播任务id`。

### 5. 本次验证结果

- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_instruction_parser.py -q`，结果 `7 passed`。
- TestClient 复现：`GET /api/tasks/课程直播任务id` 返回 `200`，任务 id=5，任务类型 `course_platform_outbound`，`steps` 非空。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 根目录完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `98 passed`。

### 6. 当前仍存在的问题

- 如果浏览器仍然请求旧路径，可以继续保留该兼容；更理想的状态仍是前端通过数字 id 进入 TaskDetail。
- 本次只修复路由简称兜底，不修改解析、评测执行或页面布局。

AGENTS.md 更新说明：
- 本次属于旧链接路由兼容 bug 修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-28 补充修复：任务详情中文名尾部 id 后缀 404

### 1. 本次修改日期

2026-05-28

### 2. 本次修改目标

继续修复 TaskDetail 旧链接兼容问题。实际请求中出现 `/api/tasks/课程直播产品升级外呼评测id` 这类“中文任务名 + 多余 id 后缀”的参数，后端虽然已不再 422，但仍因名称不完全匹配返回 404。

### 3. 修改文件列表

- `backend/app/api/tasks.py`
- `backend/tests/test_instruction_parser.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `_resolve_task(...)` 增加 URL 解码，避免编码字符串和中文任务名匹配不一致。
- 详情查询在精确匹配失败后，会兼容尾部误拼的 `id` 后缀。
- 仍找不到时，会按已存在任务名做包含/前缀兜底匹配，专门兼容旧前端链接或浏览器历史地址。
- 新增回归测试覆盖 `/api/tasks/课程直播产品升级外呼评测id`。

### 5. 本次验证结果

- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_instruction_parser.py -q`，结果 `6 passed`。
- TestClient 复现：`GET /api/tasks/课程直播产品升级外呼评测id` 返回 `200`，任务 id=5，`role_text` 非空。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 根目录完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `97 passed`。

### 6. 当前仍存在的问题

- 如果终端中的 uvicorn 还没有 reload 到最新代码，需要等它自动重载完成，或手动停止后重启后端。
- 本次只做旧链接兼容，不修改 TaskDetail 展示结构和评测链路。

AGENTS.md 更新说明：
- 本次属于路由兼容 bug 修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-28 补充修复：任务详情中文任务名路由 422

### 1. 本次修改日期

2026-05-28

### 2. 本次修改目标

修复 TaskDetail 页面从旧链接进入时，前端把任务名称作为 `/api/tasks/{task_id}` 请求参数，导致后端返回 `422 Unprocessable Entity`，页面继续显示本地演示空详情的问题。

### 3. 修改文件列表

- `backend/app/api/tasks.py`
- `frontend/src/pages/TaskDetail.vue`
- `backend/tests/test_instruction_parser.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 后端 `GET /api/tasks/{...}` 改为兼容数字 id 和任务名称：数字优先按 id 查询，非数字按 `EvaluationTask.name` 查询。
- 前端 TaskDetail 加载详情后使用后端返回的真实数字 `task.id` 查询 `/api/cases?task_id=...`，避免继续用中文任务名查询 cases。
- 前端如果发现当前 URL 参数不是数字 id，会自动 `router.replace('/tasks/{id}')`，把旧中文任务名链接收敛回数字 id 链接。
- 新增回归测试，覆盖 `/api/tasks/飞毛腿骑手合同生效外呼评测` 能返回任务详情且解析字段非空。

### 5. 本次验证结果

- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_instruction_parser.py -q`，结果 `5 passed`。
- TestClient 复现中文任务名请求：`GET /api/tasks/飞毛腿骑手合同生效外呼评测` 返回 id=4，`role_text` 和 `executable_policy` 非空；再用 id 查询 cases 返回 3 条。
- 前端构建：`npm run build` 通过；仍只有既有 Vite chunk 体积和第三方注释告警。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 根目录完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `96 passed`。

### 6. 当前仍存在的问题

- 已运行的前端开发服务可能需要刷新页面；如果 Vite 未热更新成功，可重启 `npm run dev`。
- 本次只修复 TaskDetail 路由兼容和用例查询 id，没有修改解析逻辑、评测执行逻辑或 UI 布局。

AGENTS.md 更新说明：
- 本次属于前后端路由兼容 bug 修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-28 补充修复：任务详情解析字段为空

### 1. 本次修改日期

2026-05-28

### 2. 本次修改目标

停止进入下一阶段，专门修复 TaskDetail 中 `instruction_text` 能显示但 Role、Task、Opening Line、Conversation Flow、Knowledge Points、Constraints、Steps 和 `executable_policy` 全部为空的问题。

### 3. 修改文件列表

- `backend/app/services/instruction_parser.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/seed/sample_data.py`
- `backend/tests/test_instruction_parser.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `instruction_parser.py` 增加中文标题兼容：角色、任务、约束、开场白、对话流程、通话流程、流程、知识点、参考知识、常见问题。
- 修复 Step 解析策略：优先解析显式 `Step N / 第N步`；只有全文没有显式 Step 时，才把 `1. / 2. / 3.` 这类编号列表解析为步骤，避免课程直播 Step 4 内部编号被误当成顶层 Step。
- 飞毛腿 Excel 指令的编号式 Conversation Flow 现在可解析为 4 个 steps；课程直播仍稳定解析为 Step 1 到 Step 7。
- Excel 导入增加诊断日志，打印有效行数、任务名称、指令前 100 字、Role/Task/Opening 是否解析成功、constraints 数量、steps 数量和 policy 是否生成。
- Excel 重复导入继续更新已有任务，不因任务已存在而跳过解析字段和 `executable_policy`。
- 本地演示 fallback 任务增加结构化字段和执行策略回填，避免 Excel 不可用或旧 mock_sample 详情页出现全空。
- 已重新执行本地 seed/import，当前数据库任务详情中飞毛腿和课程直播任务均已回填解析字段与执行策略。

### 5. 本次验证结果

- Excel 记录检查：飞毛腿任务解析出 4 个 steps、6 条 constraints；课程直播任务解析出 7 个 steps、11 条 constraints。
- TestClient 抽查：`GET /api/tasks` 返回两个 Excel 默认任务；`GET /api/tasks/{id}` 中两个任务的 `instruction_text`、`role_text`、`task_text`、`opening_line`、`constraints`、`steps`、`executable_policy` 均非空。
- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_instruction_parser.py tests\test_executable_policy.py -q`，结果 `7 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端目录完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `95 passed`。
- 根目录完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `95 passed`。

### 6. 当前仍存在的问题

- 本次没有修改 TaskDetail 前端样式或进入新的功能阶段；如果浏览器仍显示旧空数据，需要刷新页面或重启后端触发 seed。
- 本次没有删除用户环境中可能存在的临时 Excel 锁文件或其他未跟踪文件。

AGENTS.md 更新说明：
- 本次属于解析、导入和本地数据回填 bug 修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-28 补充修复：兼容对象型 LLM 指标分

### 1. 本次修改日期

2026-05-28

### 2. 本次修改目标

修复评测报告生成时报错 `float() argument must be a string or a real number, not 'dict'`。该问题来自真实 Judge 或外部评估可能把六项指标返回为 `{score: 80, reason: ...}` 这类对象，而报告合成逻辑原先只接受纯数字。

### 3. 修改文件列表

- `backend/app/services/agents/evaluator_agent.py`
- `backend/app/services/report_service.py`
- `backend/tests/test_reports.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `EvaluatorAgent` 增加指标分归一化能力，兼容数字、数字字符串、列表首项，以及 `score` / `value` / `points` / `分数` 字段对象。
- `ReportService._combine_metrics(...)` 同样增加对象型指标分兼容，避免 LLM 结果进入加权公式时报错。
- 新增回归测试，覆盖 evaluator 归一化和 report 合成两个入口。

### 5. 本次验证结果

- 报告回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_reports.py -q`，结果 `3 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 根目录完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `93 passed`。

### 6. 当前仍存在的问题

- 本次修复只增强指标格式兼容，没有改变评分公式、规则评分、用户模拟器或被测模型回复生成逻辑。

AGENTS.md 更新说明：
- 本次属于 bug 修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-28 第三阶段：executable_policy 生成与展示

### 1. 本次修改日期

2026-05-28

### 2. 本次修改目标

在不修改 `instruction_text` 原文、不接入实际评测执行链路的前提下，根据结构化字段生成 `executable_policy`，用于展示任务如何分轮、短句、处理打断、处理优先级分支和推进流程。

### 3. 修改文件列表

- `backend/app/services/policy_generator.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/models/task.py`
- `backend/app/schemas/task.py`
- `backend/app/core/database.py`
- `backend/app/api/tasks.py`
- `backend/tests/test_executable_policy.py`
- `frontend/src/pages/TaskDetail.vue`
- `frontend/src/pages/TaskList.vue`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增 `generate_executable_policy(task)`，兼容 dict 和 ORM task，兼容 `constraints` / `steps` 为 JSON 字符串、list 或空值。
- Task 模型新增 `executable_policy` Text 字段，SQLite 自动补列；Excel 导入和重复导入时会为已有 `excel_desensitized` 任务补齐并更新 policy。
- 课程直播任务生成专属执行策略：20 字软上限、25 字硬上限、单轮单句、说完等待用户、开车立即稍后再打、忙碌先短句挽留、打断先回应、用户提问先回答。
- 课程直播 `step_policies` 固定覆盖 Step 1 到 Step 7；Step 2 已拆成“您之前选标准直播，对吗？”和“后台已走低延迟，您知道吗？”，不直接使用原始长句。
- 飞毛腿任务生成简化执行策略：30 字软上限、40 字硬上限，并包含天气抱怨、拒绝配送、排名质疑、退出流程、超范围兜底分支。
- API schema 拆分为列表和详情：`GET /api/tasks` 不返回 `instruction_text` / `executable_policy` 等长字段；`GET /api/tasks/{task_id}` 返回完整详情。
- TaskDetail 增加“执行策略 executable_policy”折叠区域，深色展示 `reply_rules`、优先级规则、步骤策略、分支策略、禁止规则、记忆字段和示例。
- TaskList 因列表不再返回长字段，编辑任务时改为先请求详情，避免编辑弹窗缺少 `system_instruction`。

### 5. 本次验证结果

- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_executable_policy.py tests\test_instruction_parser.py -q`，结果 `5 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 根目录完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `91 passed`。
- 前端构建：`npm run build` 通过；仍有 Vite chunk 体积和第三方注释的常规告警。
- TestClient 抽查：任务列表不含 `executable_policy`；课程详情包含 20/25 字回复规则、Step 1-7、开车/忙/打断/用户提问优先级；飞毛腿详情包含 30/40 字回复规则和核心分支。

### 6. 当前仍存在的问题

- 本阶段 `executable_policy` 只用于保存与 TaskDetail 展示，尚未接入 `user_simulator`、`target_model_client`、`rule_judge`、`evaluation_service` 或实际 `memory_state` 运行逻辑。
- 本次未启动浏览器做 TaskDetail 实机截图验证；构建已通过，样式仍建议演示前目测确认。

AGENTS.md 更新说明：
- 本次属于第三阶段数据字段和展示能力增强，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-27 补充修复：第二阶段解析结果复查与根目录 pytest

### 1. 本次修改日期

2026-05-27

### 2. 本次修改目标

继续停留在第二阶段，检查结构化解析结果和 TaskDetail 展示，不进入用户模拟、评分或执行策略阶段；同时恢复根目录 `pytest -q` 的本地回归能力。

### 3. 修改文件列表

- `pytest.ini`
- `frontend/src/pages/TaskDetail.vue`
- `backend/tests/test_instruction_parser.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增根目录 `pytest.ini`，设置 `pythonpath = backend`，使从项目根目录运行 `pytest -q` 时可以正常导入后端 `app` 包。
- TaskDetail 的原始指令、Constraints 和解析结果区域增加深色容器样式，避免 Element Plus 折叠面板出现大面积白底。
- 原始指令继续完整展示在暗色滚动容器中；Constraints 继续按列表展示；Steps 继续按卡片展示。
- `test_instruction_parser.py` 增加课程直播任务 Step 1 到 Step 7 的断言，并确认 constraints 可解析、instruction_text 存在。
- 使用本地 TestClient 检查：课程直播解析出 11 条 constraints，Step 1 到 Step 7 均存在；飞毛腿任务未出现课程直播用例，课程直播任务未出现飞毛腿用例。

### 5. 本次验证结果

- 根目录测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `88 passed`。
- 前端构建：`npm run build` 通过；仍有 Vite chunk 体积和第三方注释的常规告警。

### 6. 当前仍存在的问题

- 本次未启动浏览器进行 TaskDetail 实机截图确认；构建已通过，样式已显式覆盖为深色。

AGENTS.md 更新说明：
- 本次属于第二阶段解析展示复查和测试配置修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-27 补充修复：任务指令结构化解析与 TaskDetail 展示

### 1. 本次修改日期

2026-05-27

### 2. 本次修改目标

在不修改 `instruction_text` 原文、不影响评测执行链路的前提下，从 Excel 导入后的完整任务指令中解析 Role、Task、Opening Line、Conversation Flow、Knowledge Points、Constraints 和 Step 结构，用于任务详情页展示和后续执行策略生成。

### 3. 修改文件列表

- `backend/app/services/instruction_parser.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/models/task.py`
- `backend/app/schemas/task.py`
- `backend/app/core/database.py`
- `backend/app/api/tasks.py`
- `backend/tests/test_instruction_parser.py`
- `frontend/src/pages/TaskDetail.vue`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增 `parse_instruction(instruction_text)`，兼容 Markdown 标题、无 `#` 标题、`Conversation Flow / Call Flow`、`Knowledge Points / FAQ / 参考知识` 等格式。
- `constraints` 解析为列表；数据库继续用 Text 保存 JSON 字符串，避免破坏现有知识召回、prompt 和评分代码的字符串兼容。
- 从 `Conversation Flow` 中解析 `Step 1 / Step 2 / Step 3`，并兼容 `### 3.1` 形式的子步骤；`steps` 作为 Task 级原始指令结构保存，不影响 `case.expected_steps`。
- Excel 导入流程改为先完整保存 `instruction_text`，再安全调用 parser；解析失败时仅记录 warning，并以空字段继续导入。
- Task 模型新增 `steps` Text 字段，SQLite 自动补列；`conversation_flow` 在 API 中作为 `call_flow` 的只读别名返回。
- TaskDetail 在原有页面结构内增强展示：原始指令可滚动，Constraints 列表化展示，Steps 用小卡片展示，空字段显示“暂无解析结果”。

### 5. 本次验证结果

- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_instruction_parser.py tests\test_case_deduplication.py -q`，结果 `7 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端离线测试目录：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests -q`，结果 `88 passed`。
- 前端构建：`npm run build` 通过；仍有 Vite chunk 体积和第三方注释的常规告警。
- 根目录 `pytest -q` 未作为通过结果：收集到 `scripts/test_qwen_api.py` 时触发真实 Qwen API 调用，外部接口返回 403 免费额度不足，与本次改动无关。

### 6. 当前仍存在的问题

- 本次未启动前后端进行浏览器实机截图验证；TaskDetail 构建通过，但仍建议重启前端后在页面上确认长文本滚动和 steps 展示效果。
- 现阶段 `steps` 只用于任务详情展示和后续策略生成准备，不参与评测结束逻辑和规则评分。

AGENTS.md 更新说明：
- 本次属于任务指令解析和详情展示增强，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-27 补充修复：课程直播先问知情与独立低延迟选项话术

### 1. 本次修改日期

2026-05-27

### 2. 本次修改目标

只修复课程直播 `course_platform_outbound` 本地 `mock_fallback` 的当前问题：负责人确认后应先询问是否了解低延迟直播；用户问“升级了什么”或表示“不知道”时，应说明发布页新增了独立的低延迟直播选项；仍保持每轮 assistant 只回复一次、短句说完等待商家回应。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_task_reply_isolation.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 课程直播负责人确认后的本地 mock 回复从直接说明升级，调整为先问“您了解低延迟直播吗？”。
- 用户问“升级了什么/新增了什么”或回答“我之前不知道”时，回复统一为“发布页新增了独立的低延迟直播选项。”，避免只说“现在可选低延迟了”导致语义不完整。
- `dialogue_state.py` 增加课程直播“已询问知情”的话题识别，并让 `owner` / `awareness_check` 阶段能识别新的知情询问与独立选项说明。
- 更新课程直播相关测试，覆盖先问知情、独立低延迟选项说明和任务隔离断言。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py tests\test_dialogue_deduplication.py tests\test_task_reply_isolation.py -q`，结果 `36 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `86 passed`。

### 6. 当前仍存在的问题

- 本次没有启动前端做浏览器截图复测；页面若仍显示旧对话，需要重启后端并重新跑评测。
- 本次只修课程直播本地 mock 的当前话术与阶段推进，未修改用户模拟器、规则评分、前端 UI、Excel 导入或真实 API provider。

AGENTS.md 更新说明：
- 本次属于课程直播本地话术与阶段识别的小范围修复，未改变长期规则、核心架构、启动方式或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-27 补充修复：课程直播短句流程、FAQ 触发与安全上限

### 1. 本次修改日期

2026-05-27

### 2. 本次修改目标

修复课程直播任务中被测模型回复过长、标准直播/低延迟直播区别回答不完整、用户模拟器追问不自然、以及 full_flow 被固定轮数截断的问题。保持原则：Call Flow 按流程逐步覆盖；FAQ 只在用户追问时答对；Constraints 作为说话方式约束。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/course_flow.py`
- `backend/app/services/conversation_memory.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/schemas/case.py`
- `frontend/src/pages/RunConsole.vue`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_task_reply_isolation.py`
- `backend/tests/test_knowledge_base.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 课程直播 mock/兜底回复改为电话口语短句，常规每句约 15-20 字，例如“我们直播产品升级了。”、“新增低延迟直播选项。”、“发课时选低延迟直播。”。
- 标准直播与低延迟直播区别改为分轮短答：标准直播费用较低、延迟 5-10 秒、适合大班课；低延迟直播延迟 1-2 秒、互动更流畅、适合小班课/实操课。
- 用户模拟器 full_flow 改为自然递进追问：确认负责人后问升级内容、使用方式、流程是否变化，再进入直播区别、费用、发布方式、配置路径、学员端费用和企业微信。
- 课程直播评分新增 `live_difference` 阶段，允许标准直播和低延迟直播区别分轮回答；费用、配置路径、企业微信等 FAQ/分支问题只在用户问到时激活。
- full_flow 不再以低轮数强截断，后端使用隐藏安全上限规划到 30 轮；前端展示文案由“最大轮数”改为“安全上限”，避免误解为业务限制。
- 飞毛腿结束确认识别补充“按规则、尽量完成、会按要求”等用户接受表达，避免同义说法导致 full_flow 覆盖误判。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py tests\test_dialogue_deduplication.py tests\test_case_deduplication.py tests\test_knowledge_base.py tests\test_task_reply_isolation.py -q`，结果 `48 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `86 passed`。
- 前端构建：`npm run build`，结果通过；仅保留 Vite 关于 chunk 体积和第三方注释的常规告警。

### 6. 当前仍存在的问题

- 本轮未重新启动前后端做浏览器截图验证；若页面仍显示旧“最大轮数”或旧对话，需要重启后端/前端开发服务刷新。
- 真实 API provider 仍可能输出更长内容，但返回前会继续经过任务类型校验与短句后处理；后续可再增加更细的真实 API 回放用例。

AGENTS.md 更新说明：
- 本次属于课程直播话术、用户模拟器、评分和展示文案修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-26 补充修复：飞毛腿 30 字约束、重复推进与 FAQ 分支覆盖

### 1. 本次修改日期

2026-05-26

### 2. 本次修改目标

修复飞毛腿被测模型后续回复超过 30 字、合同要求被重复回答、以及真实 API 全流程用例中看不到“激励/退出”等 FAQ 覆盖的问题。保持原则：Call Flow 在 full_flow 中必须覆盖；FAQ 不在全流程里硬塞，改由 branch 用例或用户主动追问触发。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/services/case_generator_service.py`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_case_generation.py`
- `backend/tests/test_case_deduplication.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 飞毛腿非开场回复统一压缩到 30 字内，固定话术改为短句，例如合同要求、奖励、退出、排名与保资格规则。
- 真实 API 或 custom endpoint 返回后增加飞毛腿后处理：超长回复会压缩，重复合同要求会推进到安全提醒、保资格或排名规则。
- 修复用户说“刚才说过了”后仍诱导模型继续重复合同要求的问题，用户模拟器改为追问“资格怎么保”。
- 排名与保资格短句改为“名额按系统排名，非站长定；少拒单取消超时保资格。”，并同步规则识别。
- 飞毛腿官方下拉目录保持单一用例：`飞毛腿骑手合同生效外呼用例`，不再拆出奖励、退出、不想配送三条独立默认用例。
- 奖励/退出 FAQ 改为同一个 full_flow 用例内部的可选追问分支：不同 run variant 会在主流程后追问激励或退出，用来测试 API 是否能答对 FAQ，同时不污染用例下拉。
- 不想配送、奖励、退出等仍按 FAQ/分支触发评分，不再作为 full_flow 必讲 Call Flow。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_model_providers.py tests\test_dialogue_deduplication.py tests\test_active_rules.py tests\test_case_generation.py tests\test_case_deduplication.py -q`，结果 `63 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `86 passed`。
- 额外用 TestClient 跑飞毛腿 full_flow，确认合同要求不再连续原样重复，奖励/退出需通过分支用例触发。
- 已执行 `seed_sample_data()` 同步本地数据库，并确认飞毛腿任务下只剩 `飞毛腿骑手合同生效外呼用例` 一个官方用例。

### 6. 当前仍存在的问题

- 如果前端仍看到“询问飞毛腿激励奖励 / 询问如何退出飞毛腿 / 骑手不想配送挽留”等独立用例，需要重启后端或触发 seed/import，让 catalog 同步清理旧记录。
- 本轮未重新启动前端做浏览器截图验证。

AGENTS.md 更新说明：
- 本次属于飞毛腿话术、评分和默认用例目录修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-26 补充修复：区分 Call Flow、FAQ 与 Constraints 的评分边界

### 1. 本次修改日期

2026-05-26

### 2. 本次修改目标

按脱敏指令结构重新校准飞毛腿评测边界：Call Flow 是 full_flow 必须覆盖的主流程；FAQ 只在骑手提问、反驳或犹豫时触发评分和标准回答；Constraints 作为说话方式和边界约束，不再当作必须主动讲出的内容点。

### 3. 修改文件列表

- `backend/app/services/rider_flow.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_case_deduplication.py`
- `backend/tests/test_case_generation.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 飞毛腿 full_flow 必达步骤恢复为 Call Flow：身份确认、合同生效、午晚高峰和单量要求、询问是否开跑、态度鼓励/安抚、安全提醒、排名与保资格规则、结束确认。
- 从 full_flow 必达步骤中移除“回答奖励规则”和“回答退出规则”；奖励/退出继续作为 FAQ 分支问题触发。
- 用户模拟器 full_flow 不再主动问“配送时有什么安全要注意？”、“连续跑完有奖励吗？”、“怎么退出飞毛腿？”。
- 安全提醒改为模型主动完成的 Call Flow 话术，例如“路上注意安全 / 注意安全”；不再把“少拒单、少取消、别超时”当作安全注意事项。
- “少拒单、少取消、别超时”归入保住飞毛腿资格/排名规则，不再与安全提醒互相替代。
- 飞毛腿安全类回复修正：用户即使问安全，也不会用“少拒单少取消别超时”来冒充安全回答。
- 默认用例生成和 Excel 导入的飞毛腿 full_flow 最大轮数恢复为 10，符合只覆盖 Call Flow 的长度预期。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_model_providers.py tests\test_dialogue_deduplication.py tests\test_case_deduplication.py tests\test_case_generation.py tests\test_active_rules.py -q`，结果 `62 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `84 passed`。

### 6. 当前仍存在的问题

- 本轮未重新启动前端做浏览器截图验证；如果页面仍显示旧问题或旧回答，需要确认后端进程已 reload 或手动重启。

AGENTS.md 更新说明：
- 本次属于评分边界、用户模拟器和飞毛腿话术实现修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-26 补充修复：飞毛腿奖励/退出问题首轮直答

### 1. 本次修改日期

2026-05-26

### 2. 本次修改目标

修复飞毛腿 full_flow 中用户问“多日都完成会有激励吗？”时第一轮先答合同 X/Y、第二轮才答奖励的问题；同时修复“如果不参加了在哪里取消？”被错误回答成合同完成要求的问题。要求被测模型对奖励和退出类问题首轮直接给出脱敏指令中的标准答案。

### 3. 修改文件列表

- `backend/app/services/dialogue_state.py`
- `backend/app/services/knowledge_base.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/rule_judge.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 飞毛腿用户意图识别优先级收紧为：退出问题、奖励/激励问题优先于合同 X/Y 完成要求。
- “激励”加入奖励触发词；“不参加 / 不想参加 / 哪里取消 / 在哪取消 / 在哪里取消”加入退出触发词。
- 真实 provider 回复修复顺序调整：奖励问题不再被“多日”关键词误判为合同要求，退出问题不再被“取消”或“合同”上下文带偏。
- 奖励类问题固定回答：
  `连续完成 W 天多日合同，且每天完成 Y 单，将获得额外奖励，例如与单日合同相比每单多 +$ 元。`
- 退出类问题固定回答：
  `如需退出飞毛腿，必须在前一天 Z 点之前在 App 的“飞毛腿报名”中取消；次日生效。`
- 评分和知识召回同步补充新触发词，保证前端规则命中和引用知识点与模型答案一致。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_model_providers.py tests\test_dialogue_deduplication.py tests\test_active_rules.py tests\test_knowledge_base.py -q`，结果 `61 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `83 passed`。

### 6. 当前仍存在的问题

- 本轮未重新启动前端页面做浏览器截图验证；如果页面仍显示旧回答，需要确认后端进程已 reload 或手动重启。

AGENTS.md 更新说明：
- 本次属于飞毛腿任务意图识别和标准话术修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-26 补充修复：飞毛腿合同 X/Y 固定话术与全流程 FAQ 覆盖

### 1. 本次修改日期

2026-05-26

### 2. 本次修改目标

修复飞毛腿任务中被测模型把合同要求说成“每天至少完成 X 单或 Y 单”的错误话术，并让用户模拟器在 full_flow 中主动追问连续完成激励和退出飞毛腿流程；同时收紧被测模型同一类问题的回复，使其稳定按脱敏指令标准句回答，不再根据 run variant 改写关键业务答案。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/rider_flow.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/services/llm_client.py`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_case_deduplication.py`
- `backend/tests/test_case_generation.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 飞毛腿合同要求统一为标准句：“单日合同生效当天必须完成 X 单；多日合同每天必须完成 Y 单；未完成合同及派单可能受影响。”
- `TargetModelClient` 增加错误合同话术识别，遇到“每天至少完成 X 单或 Y 单 / 单日合同每天 / X 或 Y”等表达会强制修正为标准句。
- 飞毛腿 mock fallback 的关键业务回复改为固定话术；`_rider_variant()` 不再影响被测模型答案，避免同一问题每次回复不同。
- 真实 provider 的飞毛腿奖励、退出、排名、拒配挽留、合同要求等高频问题改为按触发意图稳定修复为标准脱敏话术。
- 飞毛腿 full_flow 期望步骤新增“回答奖励规则”和“回答退出规则”，并把默认最大轮数提升到 14，保证奖励、退出、排名与安全提醒都有机会覆盖。
- 修正“回答奖励规则”的完成判断：不能因为出现“多日合同”就误判已说明奖励，必须包含 W 天 / +$ / 额外奖励等激励信息。
- 用户模拟器 full_flow 在安全提醒后会继续追问“连续跑完有额外奖励吗？”，奖励回答后继续追问“怎么退出飞毛腿？”。
- 默认用例生成和 Excel 导入的飞毛腿 full_flow 同步使用新的步骤和轮数。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_model_providers.py tests\test_dialogue_deduplication.py tests\test_active_rules.py tests\test_knowledge_base.py -q`，结果 `58 passed`。
- 默认用例/任务隔离测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_case_deduplication.py tests\test_case_generation.py tests\test_task_reply_isolation.py -q`，结果 `9 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `80 passed`。

### 6. 当前仍存在的问题

- 本轮未重新启动前端做浏览器截图验证；修复集中在后端对话生成、真实模型回复修复、用例规划和自动化测试。
- 如果已有后端进程不是 `--reload` 启动，需要重启后端后页面才会使用新的标准话术和 full_flow 步骤。

AGENTS.md 更新说明：
- 本次属于飞毛腿脱敏任务话术、用例步骤和评分完成判断修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-26 补充修复：飞毛腿 FAQ 挽留、奖励和合同影响覆盖

### 1. 本次修改日期

2026-05-26

### 2. 本次修改目标

修复飞毛腿任务中真实模型或 mock 回复只给出浅层提醒、没有充分体现脱敏指令 FAQ / Call Flow 的问题，重点覆盖骑手不想配送时的挽留、连续完成激励、单日/多日合同未完成对合同及派单的影响、退出飞毛腿流程和报名排名规则。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/knowledge_base.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/conversation_memory.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/user_simulator.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_knowledge_base.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `TargetModelClient` 飞毛腿真实 provider 回复修复逻辑增强：当骑手说“不想干 / 不想跑 / 跑不了”时，如果外部模型遗漏 FAQ，会统一修复为包含“许多骑手正在申请、连续 Y 天跑不了名额可能被占、单日 X 单 / 多日每天 Y 单、未完成影响合同及派单、W 天每天 Y 单有额外奖励”的话术。
- 飞毛腿合同完成要求话术统一补充“未完成影响合同及派单”，避免只说 X/Y 单、不说明后果。
- 排名/名额回复统一补充“报名按系统排名、不是站长人工干预、连续 Y 天跑不了名额可能被占、少拒单取消超时可保资格”。
- 奖励回复修复逻辑改为必须包含 W 天、每天 Y 单、多日合同、额外奖励和 +$ 占位，避免真实模型只泛泛说“有奖励”。
- 轻量知识库默认 chunk 补充“不想配送挽留”和“连续完成激励”的完整脱敏 FAQ，并将奖励约束调整为“可说脱敏指令内 +$ 激励，但不能编真实金额或知识库外奖励”。
- `RuleJudge` 将“不想干 / 不干”纳入骑手拒配触发，并把“许多骑手正在申请且名额可能被占用”“连续完成多日合同可能有额外奖励”纳入该分支评分规则。
- 对话状态、run 记忆、用户模拟器兼容入口均补充“不想干 / 不干”识别，避免 API 执行时分支状态和评分状态不一致。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_model_providers.py tests\test_knowledge_base.py tests\test_active_rules.py -q`，结果 `43 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `78 passed`。

### 6. 当前仍存在的问题

- 本轮未重新启动前端页面做浏览器截图验证；修复集中在后端知识召回、真实模型回复修复、评分触发和自动化测试。
- 真实 `openai_compatible/custom_endpoint` 仍可能先返回浅层答案，但返回前会经过本次增强的飞毛腿任务修复逻辑；报告也会按新规则识别 FAQ 遗漏。

AGENTS.md 更新说明：
- 本次属于飞毛腿脱敏任务 FAQ 覆盖和评分触发修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-26 补充修复：同一 API 用例重复运行时用户问题可控变体

### 1. 本次修改日期

2026-05-26

### 2. 本次修改目标

修复连续调用 `/api/runs/start` 跑同一个 `task_id + case_id` 时，用户模拟器每轮问题几乎完全一样的问题；同时修复被测模型回复去重漏掉上一轮 assistant 导致相邻回复可能重复的问题。

### 3. 修改文件列表

- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/target_model_client.py`
- `backend/tests/test_dialogue_deduplication.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `UserSimulatorAgent` 读取 run 级 `memory_state.run_context.variant_id`，在保持同一测试目标和同一业务语义的前提下，为骑手、课程直播和 full_flow 追问生成不同表达。
- 飞毛腿 full_flow 重复运行时，“可以配送”“安全注意”“名额/排名”“结束确认”等用户问题会随 run variant 变化，不再每次完全一致。
- 课程直播 full_flow 的“说重点”“区别/价格/发布方式/配置路径/企业微信”等追问也增加同义变体，同时保留关键评分词，避免影响规则识别。
- `TargetModelClient.deduplicate_assistant_reply()` 改为检查全部历史 assistant 回复，避免漏掉上一轮回复导致相邻 assistant 气泡重复。
- 新增用户模拟器 variant 单元测试，确认相同历史在不同 `variant_id` 下会生成不同问题，且仍命中“名额/资格/站长”等关键语义。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_runs.py -q`，结果 `18 passed`。
- 扩展相关测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py tests\test_knowledge_base.py tests\test_dialogue_deduplication.py tests\test_model_providers.py tests\test_runs.py -q`，结果 `58 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `75 passed`。
- 本地 API 复现：连续两次 POST `/api/runs/start`，使用 `task_id=4, case_id=64, model_provider=mock_fallback`，返回的用户问题序列 `same=false`。

### 6. 当前仍存在的问题

- 该变体是“可控同义变体”，不是完全随机用户；相同 run 内仍会围绕同一 case 目标推进，便于评测解释和回归复现。
- 当前服务控制台对中文 JSON 有编码显示问题，但 API 判断结果和前端展示不受影响。

AGENTS.md 更新说明：
- 本次属于用户模拟器和回复去重的实现修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-25 补充修复：飞毛腿逐轮评分生命周期与关键知识覆盖

### 1. 本次修改日期

2026-05-25

### 2. 本次修改目标

修复飞毛腿评测中“首轮未说单日/多日要求即显示失败，但后续已补充”的评分解释问题，并补强退出飞毛腿、连续完成激励、挽留不想配送、鼓励能配送、恶劣天气安全与保资格、减少拒单取消超时等脱敏指令内容在知识召回、mock_fallback 回复和报告中的体现。

### 3. 修改文件列表

- `backend/app/services/rule_judge.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/knowledge_base.py`
- `backend/app/services/memory_service.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/api/reports.py`
- `backend/app/schemas/report.py`
- `frontend/src/components/ConversationTimeline.vue`
- `frontend/src/pages/RunConsole.vue`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_knowledge_base.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `RuleJudge.score_turn()` 增加逐轮规则生命周期：飞毛腿正常流程中可后续补足的规则先进入 `pending_rules` / `rule_lifecycle.pending`，不再在首轮直接显示为最终失败。
- `RuleJudge.evaluate_conversation()` 增加 `late_satisfied_rules`：整段对话最终已补足的规则不再作为 `failed_rules` 计入最终报告，仅按“延迟补足”轻微影响指令遵循和流程覆盖。
- `EvaluationService` 分支用例停止逻辑会识别 `pending_rules`，避免首轮待补充时过早结束；SSE `rule_result` 事件也补充 pending、untriggered 和 lifecycle 字段，前端可实时展示。
- 飞毛腿知识库新增“不想配送挽留”“愿意配送鼓励”“连续完成激励”等 chunk，并强化恶劣天气、排名资格、拒单取消超时、退出流程召回。
- `TargetModelClient.generate_rider_reply()` 和外部模型修复逻辑补强飞毛腿话术：天气场景同时说明安全、订单更多和保资格；不想配送场景先理解再挽留，不强迫；奖励场景避免承诺具体金额；接受配送时提醒少拒单、少取消、别超时和注意安全。
- `UserSimulatorAgent` 飞毛腿首轮发言改为尊重 `case.initial_message`，使“退出飞毛腿”“恶劣天气”等 branch 用例从第一轮就触发对应测试目标，不再统一说“是我，你说。”。
- run 级 memory 增加 `run_context.run_seed / variant_id`，供 mock 话术在保持事实一致的前提下做可控变体，减少连续演示完全相同的观感。
- 前端对话时间线新增“迟补”标签和规则详情里的“延迟补足”，区分本轮待补充、最终失败和后续已补足。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py tests\test_knowledge_base.py tests\test_dialogue_deduplication.py tests\test_model_providers.py -q`，结果 `52 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `74 passed`。
- 前端构建：`npm run build` 通过；保留 Vite/Rollup 的第三方注释和 chunk size 警告。

### 6. 当前仍存在的问题

- 本轮未重新启动浏览器演示页跑人工点击验证；当前验证集中在自动化测试和前端构建。
- 真实 `openai_compatible/custom_endpoint` 仍取决于外部模型本身是否充分利用知识片段；平台会继续通过修复逻辑和规则评分识别遗漏、串场和编造。

### 7. 下一步建议

- 用开始评测页分别跑飞毛腿“询问如何退出飞毛腿”“抱怨恶劣天气”“飞毛腿骑手合同生效外呼用例”，重点确认首轮 pending、后续 late_satisfied、天气保资格和退出流程召回是否符合演示预期。

AGENTS.md 更新说明：
- 本次属于评分解释、知识覆盖和展示字段的实现修复，未改变长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-25 补充修复：每轮对话后更新 run 级 memory_state

### 1. 本次修改日期

2026-05-25

### 2. 本次修改目标

实现每轮用户发言、被测模型回复和规则评分后的 run 级 `memory_state` 更新，让用户模拟器和真实 API provider 能读取当前 run 内的流程阶段、用户分支、模型表现、未完成事项和下一步建议，而不是只依赖固定轮次。

### 3. 修改文件列表

- `backend/app/services/memory_service.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/conversation_memory.py`
- `backend/app/services/target_model_client.py`
- `backend/tests/test_runs.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `memory_service.py` 新增 `update_after_user()`：识别负责人/非负责人、忙、开车、知情状态、发布渠道、选项可见性、费用关注、企业微信可添加性、用户打断和当前主要问题，并更新 `user_branch_memory`、`conversation_memory`、`next_best_action`。
- 新增 `update_after_model()`：根据规则评分结果更新 `flow_memory`、必达/待完成/禁止规则、啰嗦/未回答/重复/串场/约束违规等模型表现计数，以及最后模型动作和下一步建议。
- `EvaluationService` 普通评测和流式评测均改为每轮加载 run memory，用户模拟器生成后先保存用户侧更新，被测模型回复和规则评分后再保存模型侧更新，确保 `memory_state` 不只在 run 结束时更新。
- `TargetModelClient` 调用真实 `openai_compatible` / `custom_endpoint` 时的 `compact_memory_for_prompt()` 已兼容 run 级 memory 和旧逐轮 snapshot 两种结构，避免外部 API 拿到空的 pending/next action。
- 修复课程直播 full_flow 已完成企业微信后，用户说“可以/继续”时应进入结束确认，不再停在泛泛配置提醒。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_runs.py tests\test_dialogue_deduplication.py tests\test_run_stream.py tests\test_model_providers.py -q`，结果 `31 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `69 passed`。

### 6. 当前仍存在的问题

- 本轮未改前端 UI，也未重新跑 `npm run build`；前端如需展示 run 级 memory 的更多字段，可在后续小步补充。
- `mock_fallback` 仍是可复现的规则兜底模型，不等于真实大模型能力；真实 API 的价值在于它会拿到任务指令、知识片段、最近消息和当前 run memory 后自由生成，被平台按同一规则评分。

### 7. 下一步建议

- 在开始评测页分别用 `mock_fallback` 和真实 `openai_compatible/custom_endpoint` 跑课程直播 full_flow，重点查看用户打断、知情分支、配置路径、企业微信收口是否会随 `memory_state` 自然推进。

AGENTS.md 更新说明：
- 本次属于每轮 run 内状态更新和 provider prompt 兼容补充，未改变项目长期规则、核心架构或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-25 补充修复：用户模拟器按 memory_state 动态生成发言

### 1. 本次修改日期

2026-05-25

### 2. 本次修改目标

只增强 `user_simulator.py` / `user_simulator_agent.py`：用户模拟器生成下一轮发言时读取当前 run 的 `memory_state`、任务指令、测试用例和最近 6 条消息，不再只依赖固定轮次模板。

### 3. 修改文件列表

- `backend/app/services/user_simulator.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/evaluation_service.py`
- `backend/tests/test_dialogue_deduplication.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `UserSimulator` 传给 LLM 用户模拟器的上下文收敛为任务指令、任务类型、用例画像、目标规则、当前 `memory_state`、最近 6 条消息和当前轮次。
- `UserSimulatorAgent` 增加 memory 驱动反应：忙碌打断、开车终止、非负责人拒绝强推、冗长/未回答/重复/串场反应，以及按未完成事项自然追问。
- 保留原有输出字段 `content`、`intent`、`user_state`、`should_continue`，同时新增兼容字段 `metadata`，包含 `intent`、`emotion_level`、`patience`、`should_continue`、`branch_update`。
- `EvaluationService` 将 `user_metadata` 保存到 `RunMessage.detail`，不新增消息表字段。
- 修正 memory 反应优先级，避免发布方式、知情确认、排名资格等节点抢跑，保持课程 full_flow 和飞毛腿知识召回稳定。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_runs.py tests\test_run_stream.py -q`，结果 `16 passed`。
- 知识召回与对话回归：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_knowledge_base.py tests\test_dialogue_deduplication.py -q`，结果 `17 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `67 passed`。
- 前端构建：`npm run build` 通过；仅保留 Vite/Rollup 的 chunk size 和第三方注释警告。

### 6. 当前仍存在的问题

- 用户模拟器仍是可复现的规则/状态驱动 Agent，不是跨 run 学习型长期记忆；这是为了保证评测独立性和可解释性。

### 7. 下一步建议

- 在开始评测页重新跑课程直播 full_flow，重点看忙碌、知情、发布方式、配置路径是否按 `memory_state` 自然推进；再用飞毛腿 full_flow 检查排名/资格追问是否仍能召回知识。

AGENTS.md 更新说明：
- 本次属于用户模拟器增强和消息 detail 补充，未改变项目长期规则或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-25 补充修复：Run 级 memory_state 数据结构持久化

### 1. 本次修改日期

2026-05-25

### 2. 本次修改目标

在 `EvaluationRun` 数据模型中新增 run 级 `memory_state` 字段，并提供 `memory_service.py` 统一初始化、读取、保存和新 run 重置逻辑；该字段用于保存当前 run 的稳定上下文记忆结构，不跨 run 复用。

### 3. 修改文件列表

- `backend/app/models/run.py`
- `backend/app/core/database.py`
- `backend/app/schemas/run.py`
- `backend/app/api/runs.py`
- `backend/app/services/memory_service.py`
- `backend/app/services/evaluation_service.py`
- `backend/tests/test_runs.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `EvaluationRun` 新增 `memory_state` JSON 字段；SQLite 旧库启动时会通过 `_ensure_run_columns()` 自动补列。
- 新增 `memory_service.py`，实现 `init_memory(task, case)`、`load_memory(run)`、`save_memory(db, run, memory)`、`reset_memory_for_new_run(task, case)`。
- run 级默认结构按要求拆为 `flow_memory`、`user_branch_memory`、`model_performance_memory`、`unfinished_memory`、`conversation_memory`。
- 每次创建新 run 时写入全新的初始 `memory_state`，避免复用旧 run 记忆。
- 评测过程中会把每轮细粒度 memory 快照映射回 run 级稳定结构，`/api/runs/{id}` 可稳定读取。
- 保留上一轮已做的 `RunMessage.detail.memory_state` 细粒度快照，不影响对话生成和报告解释。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_runs.py tests\test_dialogue_deduplication.py tests\test_run_stream.py -q`，结果 `13 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `64 passed`。
- 前端构建：`npm run build` 通过；仅保留 Vite/Rollup 的 chunk size 和第三方注释警告。

### 6. 当前仍存在的问题

- run 级 `memory_state` 是稳定汇总结构；每轮更细的推理状态仍在 `RunMessage.detail.memory_state` 中查看。

### 7. 下一步建议

- 用 `/api/runs/{run_id}` 检查 run 级 `memory_state`，用 `/api/runs/{run_id}/messages` 检查逐轮 memory 快照，确认两层数据分别满足“汇总可读”和“逐轮可解释”。

AGENTS.md 更新说明：
- 本次属于数据结构和持久化补充，未改变项目长期规则或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-25 补充修复：单次评测 Run 内上下文状态记忆

### 1. 本次修改日期

2026-05-25

### 2. 本次修改目标

新增 run-local 的 `memory_state`，记录当前评测 run 内的流程阶段、用户分支、模型表现和未完成事项；该记忆只服务于单次评测，不跨用例、不跨 run 复用，保证评测可复现、可解释。

### 3. 修改文件列表

- `backend/app/services/conversation_memory.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/user_simulator.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/report_service.py`
- `backend/app/api/reports.py`
- `backend/app/schemas/report.py`
- `frontend/src/components/ConversationTimeline.vue`
- `frontend/src/pages/RunConsole.vue`
- `frontend/src/pages/ReportDetail.vue`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_run_stream.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增 `conversation_memory.py`，统一初始化和更新 `memory_state`，包含 `flow_memory`、`user_branch_memory`、`model_performance_memory`、`unfinished_items_memory` 四类状态。
- `EvaluationService` 在每个 run 开始时重新初始化 memory；第 0 轮 Opening Line 和后续每轮评分后都会把最新 `memory_state` 写入 `RunMessage.detail`。
- `UserSimulatorAgent` 接收 memory，full_flow 用户模拟可参考当前待覆盖步骤继续推进，避免只按固定轮次回复。
- `TargetModelClient` 在调用 `openai_compatible` 或 `custom_endpoint` 时把压缩后的 memory 放进 prompt / payload，帮助真实被测模型识别当前阶段和待覆盖事项；同时明确不得跨 run 使用记忆。
- 报告生成时把最后一轮 memory 写入 `explainability.memory_state`，`/api/reports/{id}` 顶层返回 `memory_state`。
- 前端对话时间线新增“上下文记忆”折叠块，报告页新增“上下文记忆”概览，不大改现有 UI。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_active_rules.py tests\test_run_stream.py -q`，结果 `27 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `62 passed`。
- 前端构建：`npm run build` 通过；仅保留 Vite/Rollup 的 chunk size 和第三方注释警告。

### 6. 当前仍存在的问题

- `memory_state` 当前是规则派生的状态快照，不是长期学习能力；真实 API 是否利用好 memory 仍取决于被测模型自身回复质量，平台会保留其合法但不佳的回复用于评分。

### 7. 下一步建议

- 在开始评测页重新跑课程直播 full_flow，用“展开查看上下文记忆”确认当前阶段、用户分支、待覆盖事项是否与对话一致；再用真实 API provider 对比其是否比 mock 更能利用 memory 自然推进。

AGENTS.md 更新说明：
- 本次属于 run 内评测状态增强和报告展示补充，未改变项目长期规则或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-22 补充修复：打断场景、Opening Line 显示与 API 评测边界

### 1. 本次修改日期

2026-05-22

### 2. 本次修改目标

修复课程直播评测中看不到用户打断、Opening Line 第 0 轮把“是否没有串场”展示成普通命中规则、100 分仍显示“扣分原因”的问题；同时收窄外部 API 回复修复逻辑，避免把合法但啰嗦的 API 回复模板化替换。

### 3. 修改文件列表

- `backend/app/services/evaluation_service.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/target_model_client.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/services/case_generator_service.py`
- `frontend/src/components/ConversationTimeline.vue`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_active_rules.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 课程 full_flow 默认加入一次轻量打断：模型说明升级后，用户模拟器会说“我有点忙，说重点。”，模型需先处理忙碌约束，再继续主流程。
- 用户模拟器课程任务的长回复打断阈值从 70 字收紧到 42 字；外部 API 若返回过长但未串场，不再被模板替换，下一轮由用户模拟器打断并由规则评分扣分。
- 课程忙碌场景修正为优先回复“就1分钟，保证简短。”，避免已经说明升级后跳过忙碌处理。
- Opening Line 的“是否没有串场”改为隐藏防串场检查：通过时进入 `hidden_guardrail_rules.matched`，不再作为普通命中标签展示；违规时仍会影响开场得分。
- 流式 `rule_result` 不再用 `reason` 填充 `deduction_reason`，避免 100 分轮次显示“扣分原因：Opening Line 开场检查完成”。
- 前端 `ConversationTimeline` 仅在存在真实扣分且轮次得分低于 100 时显示“扣分原因”。
- 课程 full_flow 默认最大轮数放宽到 20，容纳“忙碌打断 + 第三方未显示慢速配置 + 费用 + 企业微信 + 结束确认”的完整链路。

### 5. 本次验证结果

- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_active_rules.py tests\test_task_reply_isolation.py tests\test_run_stream.py -q`，结果 `29 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `62 passed`。
- 前端构建：`npm run build` 通过；仅保留 Vite/Rollup 的 chunk size 和第三方注释警告。

### 6. 当前仍存在的问题

- 真实页面需要重新启动前后端并重新跑一次课程直播用例，历史 run 不会自动重算 Opening Line 和打断显示。

### 7. 下一步建议

- 在开始评测页分别用 `mock_fallback` 和真实 `openai_compatible/custom_endpoint` 跑课程直播默认用例：本地应看到忙碌打断；真实 API 若啰嗦，应看到用户模拟器下一轮打断并在规则中扣分。

AGENTS.md 更新说明：
- 本次属于流程模拟、评分展示和 provider 评测边界修复，未改变项目长期规则或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-21 补充修复：课程直播强渐进流程角色边界与分支推进

### 1. 本次修改日期

2026-05-21

### 2. 本次修改目标

根据最新确认，修复课程直播外呼评测中“被测模型”和“用户模拟器”角色混淆、用户不回答知情分支、后续配置/费用/企业微信被提前判完成，以及全流程未来步骤过早进入失败规则的问题。

### 3. 修改文件列表

- `backend/app/services/course_flow.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/models/case.py`
- `backend/app/schemas/case.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/services/case_generator_service.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_task_reply_isolation.py`

### 4. 已完成内容

- 课程 full_flow 改为严格按主流程推进：身份确认后先说明直播产品升级，再询问是否知道后台低延迟线路，用户模拟器只回答“知道/不知道”等分支条件。
- 用户模拟器按 case profile 选择不同分支答案：知情/不知情、Web/校务/SaaS、已显示/未显示、费用是否设置、当前号码是否可添加。
- 修正“确认是否知情”“费用检查”“企业微信添加”等步骤完成判定：不能因为模型只问了一句就算完成，必须等用户分支回答或模型完成对应处理。
- 课程配置路径支持“下一步/继续/收到”等自然回应，并只根据上一句模型回复推进，避免重复跳回旧步骤。
- 规则评分中 full_flow 未完成步骤进入 pending，不再作为当前轮失败；负责人首轮只激活身份/升级说明，不提前扣知情、费用、企业微信等后续步骤。
- 外部 API 回复修复层收窄为硬违规拦截：禁用语气词、非开车“稍后再打”、承诺优惠、超长回复、未说明升级就跳配置；正常合规 API 回复不再被模板化替换。
- 课程 full_flow 默认最大轮数放宽到 18，以覆盖第三方未显示时的慢速配置路径、费用检查、企业微信和结束确认。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_active_rules.py tests\test_task_reply_isolation.py -q`，结果 `26 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `61 passed`。

### 6. 当前仍存在的问题

- 本次仍未启动前端浏览器复测截图页面；后端测试已确认默认课程 full_flow 可覆盖到企业微信和结束确认。

### 7. 下一步建议

- 在开始评测页重新跑课程直播默认用例，重点确认用户气泡只回答分支条件，模型气泡按 15-20 字左右逐步说明，不再把后续规则提前打成失败。

AGENTS.md 更新说明：
- 本次属于课程直播流程、模拟器和评分修正，未改变项目长期规则或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-21 补充修复：课程直播外部模型回复硬拦截

### 1. 本次修改日期

2026-05-21

### 2. 本次修改目标

修复前端实际评测中课程直播被测模型出现的错误话术：负责人正常沟通时误说“稍后再打”、回复中出现禁用语气词“好的”、以及未说明升级内容就跳到 Web/校务A/SaaS B 发布方式。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/tests/test_active_rules.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 在 `TargetModelClient` 中为课程直播增加外部模型回复修复层，不再只依赖 prompt。
- 课程任务下，只有用户明确说开车时才允许回复“那我稍后再打。”并结束；非开车场景出现“稍后再打”会按当前阶段替换为白名单短句。
- 课程任务下，外部回复出现“好的 / 哈哈 / 嘿嘿 / 嘻嘻 / 嗯嗯”等禁用语气词时，会被替换为当前阶段白名单短句。
- 在未完成升级说明前，如果外部模型直接跳到 Web/校务A/SaaS B、第三方、配置路径等发布方式内容，会被拉回“发布页分标准直播和低延迟直播。”。
- 新增回归测试覆盖：非开车误说稍后再打、跳过升级直接问发布方式、真实开车时允许稍后再打并关闭。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py tests\test_dialogue_deduplication.py tests\test_task_reply_isolation.py -q`，结果 `24 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `59 passed`。

### 6. 当前仍存在的问题

- 本次仍未操作前端浏览器复测截图中的页面；后端链路已增加硬拦截，建议刷新后重新跑同一课程直播用例确认页面对话变化。

### 7. 下一步建议

- 在开始评测页重新跑课程直播默认用例，重点看前三轮：负责人确认后应先问“之前后台走低延迟，您知道吗？”，随后说明“发布页分标准直播和低延迟直播。”，不应出现“好的”或非开车“稍后再打”。

AGENTS.md 更新说明：
- 本次属于课程直播回复修复和测试补充，未改变项目长期规则或启动方式，因此未更新 `AGENTS.md`。

## 〇、2026-05-21 本次修改：课程直播用例收敛为强渐进主流程

### 1. 本次修改日期

2026-05-21

### 2. 本次修改目标

根据课程直播脱敏指令和流程图，将课程直播测试从多条独立分支用例调整为一条强渐进式 `full_flow` 用例：主流程按身份确认、知情确认、升级说明、区别/价格、发布方式、配置路径、费用检查、企业微信和结束确认推进；忙/开车/区别/费用/第三方配置等情况作为步骤内条件分支处理，不再作为默认下拉里的分开用例。

### 3. 修改文件列表

- `backend/app/services/course_flow.py`
- `backend/app/services/case_registry.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/seed/sample_data.py`
- `backend/tests/test_case_deduplication.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_knowledge_base.py`
- `backend/tests/test_task_reply_isolation.py`
- `backend/tests/test_case_generation.py`
- `backend/tests/test_active_rules.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增课程直播统一流程定义，默认 expected_steps 收敛为 10 步，并将 `课程直播产品升级外呼用例` 设为唯一默认课程用例。
- Excel 导入、sample seed 和 AI 用例生成目录均改为生成/同步这一条强渐进 `full_flow` 用例。
- 用例同步增加旧课程分支清理：`负责人正常沟通`、`非负责人转达`、`商家说忙`、`商家说在开车`、`追问直播区别`、`第三方系统看不到选项`、`询问费用或优惠` 等旧默认分支会迁移引用到统一用例后删除。
- 用户模拟器按 pending step 逐步追问：先问升级内容，再分步问标准/低延迟区别、费用、发布方式、SaaS 配置路径、企业微信，最后接受结束。
- 课程 mock fallback 按强渐进流程生成短句，开车优先回复“那我稍后再打。”，忙碌优先回复“就1分钟，保证简短。”；配置路径按“进【我的】→点直播平台管理→选择【直播平台】→勾选低延迟并保存”逐步推进。
- 规则评分和 full_flow stop decision 改为按课程关键步骤判断，不再把旧分支专项当成默认用例。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_case_deduplication.py tests\test_dialogue_deduplication.py tests\test_task_reply_isolation.py tests\test_knowledge_base.py tests\test_active_rules.py -q`，结果 `32 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `56 passed`。
- 抽样打印 mock 全流程确认顺序：开场 → 知情确认 → 发布页分标准直播/低延迟直播 → 5-10 秒 → 1-2 秒 → 费用略高 → Web/校务A/SaaS B → SaaS 配置路径 → 企业微信 → 祝课程顺利。

### 6. 当前仍存在的问题

- 本次未启动前端页面做浏览器级验证；后端 API 和测试已确认默认课程用例收敛，但仍建议在开始评测页手动刷新查看下拉项。
- 历史文档中仍保留旧分支用例的开发记录，作为历史进度不清理。

### 7. 下一步建议

- 在前端开始评测页跑一次课程直播默认用例，确认下拉只剩统一课程用例，且报告页展示 `full_flow_expected_steps` 全覆盖。
- 后续若需要专项测试“开车/费用/配置路径”，优先作为强渐进主流程内触发场景验证，不再恢复为默认分支用例。

AGENTS.md 更新说明：
- 本次属于课程直播用例组织和后端流程模拟修正，未改变项目定位、核心架构、启动方式或长期工作规范，因此未更新 `AGENTS.md`。

## 〇、2026-05-21 本次修改：飞毛腿排名与保资格规则改为主流程末尾收口

### 1. 本次修改日期

2026-05-21

### 2. 本次修改目标

根据飞毛腿 Call Flow 第 4 条重新调整外呼模拟逻辑：报名按系统排名、不是站长人工干预、减少拒单/取消/超时和恶劣天气订单有助于保住资格，不再作为开场后立即抛出的普通 FAQ，而是飞毛腿 full_flow 主流程末尾必须覆盖的规则收口；如果骑手中途主动问排名、名额、站长干预、拒单取消超时或保资格，则允许提前触发。

### 3. 修改文件列表

- `backend/app/services/rider_flow.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/knowledge_base.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/seed/sample_data.py`
- `backend/tests/test_case_deduplication.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_knowledge_base.py`
- `backend/tests/test_task_reply_isolation.py`
- `backend/tests/test_case_generation.py`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_active_rules.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增飞毛腿主流程统一定义：确认身份 → 告知今天飞毛腿合同已生效 → 说明午晚高峰和单量要求 → 询问是否可以开始配送 → 根据骑手态度鼓励/挽留/安抚 → 提醒注意安全 → 说明排名与保资格规则 → 结束确认。
- `说明排名与保资格规则` 的完成判定收紧为同时包含：排名/系统排名/按排名、非站长或不是站长人工干预、拒单/取消/超时至少一项、资格/保住资格/名额至少一项。
- 用户模拟器在普通愿意配送场景下，不再在“能跑”或安全提醒后直接 accepted；若排名与保资格规则未完成，会继续追问“名额是不是站长定的”或“怎么保住资格”。
- 被测模型 mock fallback 和外部模型修复逻辑同步补齐完整排名收口话术：`报名按系统排名，不是站长人工干预；少拒单取消超时可保资格。`
- 轻量知识召回将“报名排名规则”调整为流程型知识，并覆盖拒单、取消、超时、资格等触发词。
- 规则评分、full_flow stop decision 和测试用例生成同步使用新的飞毛腿主流程，避免 70% 覆盖时在排名收口前提前结束。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_case_deduplication.py tests\test_dialogue_deduplication.py tests\test_knowledge_base.py tests\test_task_reply_isolation.py tests\test_case_generation.py tests\test_model_providers.py tests\test_active_rules.py -q`，结果 `47 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `56 passed`。

### 6. 当前仍存在的问题

- 本次未启动前端页面做浏览器级验证，因为修改集中在后端对话模拟、被测模型兜底回复、知识召回和规则评分。
- 真实外部模型仍可能返回不完整话术，但 `TargetModelClient` 会在排名/资格触发场景下修复为安全白名单回复。

### 7. 下一步建议

- 用前端开始评测页抽样跑一次飞毛腿 full_flow，确认页面展示的对话顺序和报告证据与后端测试一致。
- 后续如重新拆回分支专项用例，可保留“退出飞毛腿”“奖励”“超范围咨询”为 branch，不再塞回 full_flow 主流程。

AGENTS.md 更新说明：
- 本次属于飞毛腿任务主流程和评分逻辑修正，未改变项目定位、核心架构、启动方式或长期工作规范，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次修改：飞毛腿改为单一递进式完整外呼用例

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

根据飞毛腿脱敏任务的 Call Flow，把默认测试用例从多个拆散的分支专项用例，收敛为一个“飞毛腿骑手合同生效外呼用例”。该用例按递进式外呼流程运行，分支问题作为流程中的可能回应依次覆盖，而不是在下拉框里拆成“愿意配送 / 不想配送 / 退出 / 排名 / 奖励”等多个独立默认用例。

### 3. 修改文件列表

- `backend/app/seed/import_instruction_excel.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/services/case_registry.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/tests/test_case_deduplication.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_knowledge_base.py`
- `backend/tests/test_task_reply_isolation.py`
- `backend/tests/test_case_generation.py`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_runs.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 默认飞毛腿官方用例目录现在只保留 `飞毛腿骑手合同生效外呼用例`，模式为 `full_flow`，最大轮数为 12。
- 该用例 expected steps 调整为：确认身份 → 告知合同签署并询问是否开跑 → 说明午晚高峰和单量要求 → 说明连续配送和未完成影响 → 挽留鼓励和安全提醒 → 回答退出规则 → 回答奖励规则 → 说明排名与保资格规则 → 处理超出职责问题 → 结束确认。
- `sync_task_cases_to_catalog(...)` 会清理旧的同名 legacy 默认手工记录 `喂，哪位？我现在能接电话，你说吧。`，避免开始评测下拉框继续显示旧泛化用例；其他真正手工用例仍保留。
- 被测模型开场调整为 `你好，请问是王师傅吗？我是美团外卖骑手的站长。`
- 身份确认后话术调整为：`我看到你已报名飞毛腿，今天合同已签署并生效，请问是否可以开始配送？`
- 话术不再过度压缩，保持约 30 字以内但更贴近脱敏指令：午晚高峰上线、单日 X 单、多日每天 Y 单、连续 Y 天、退出 App 飞毛腿报名、W 天奖励、排名非站长干预和超出职责回电均按官方口径表达。
- 用户模拟器对飞毛腿 full_flow 改为递进追问：可以开跑 → 为什么连续 Y 天 → 配送注意事项 → 怎么退出 → 是否有额外奖励 → 为什么排不上 → 平台算法/派单少 → 明白并收口。
- 评分和停止逻辑同步支持新的 10 个 full_flow 步骤，避免 70% 覆盖时提前结束。
- AI 生成飞毛腿用例的 mock catalog 也改为生成单一递进式完整流程草稿，不再默认生成分散的分支专项草稿。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_case_deduplication.py tests\test_dialogue_deduplication.py tests\test_knowledge_base.py tests\test_task_reply_isolation.py tests\test_case_generation.py tests\test_model_providers.py -q`，结果 `32 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `53 passed`。
- 实际单用例抽样确认：第 0 轮为美团外卖骑手站长开场；后续依次覆盖合同签署、午晚高峰、X/Y 单、连续 Y 天、退出、奖励、排名、超出职责和最终收口，`full_flow_status.complete=true`。

AGENTS.md 更新说明：
- 本次属于默认飞毛腿用例组织和话术实现调整，未改变项目定位、核心架构、启动方式或长期工作规范，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次修复：飞毛腿退出流程回答与分支触发收紧

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

修复飞毛腿“我想退出飞毛腿，怎么取消？”场景下，真实模型可能回答成“少拒单少取消，别超时，注意安全。”的问题；同时收紧“取消”触发词，避免“减少拒单、取消和超时”被误识别成退出飞毛腿分支。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/rule_judge.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_knowledge_base.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `TargetModelClient` 的飞毛腿 `exit` 阶段固定使用官方 FAQ 口径：`需前一天 Z 点前在 App 的飞毛腿报名中取消，次日生效。`
- 真实 provider 返回后处理新增退出分支强制纠偏：当用户问退出/怎么取消/取消飞毛腿/飞毛腿报名时，如果模型未同时说明提前时间、App 飞毛腿报名入口、取消动作和次日生效，就替换为官方退出流程。
- 排名分支也补充轻量纠偏：用户问报名排名时，若真实模型未说明“按排名、不是站长干预”，会回退到安全短答。
- 用户模拟器和对话状态识别不再把裸的“取消”当成退出飞毛腿，必须出现“退出”“怎么取消”“取消飞毛腿”“取消报名”或“飞毛腿报名”等语义，避免“少取消/减少取消超时”误进退出分支。
- 规则评分侧同步收紧退出触发词，防止未触发退出问题时提前展示或扣除退出流程规则。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_case_deduplication.py tests\test_active_rules.py tests\test_model_providers.py -q`，结果 `39 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `58 passed`。

AGENTS.md 更新说明：
- 本次是飞毛腿退出流程和触发词误判修复，未改变长期规则、核心架构或接口契约，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次修复：飞毛腿正常愿意配送全流程与 rider_name 开场

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

根据官方飞毛腿脱敏任务和“正常愿意配送”完整用例，修正 `${rider_name}` 占位符、用户首句身份确认、被测模型后续流程推进，以及全流程覆盖检查。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/services/case_generator_service.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_case_deduplication.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 飞毛腿 Opening Line 现在会把 `${rider_name}` 稳定替换为骑手称呼，默认使用“王师傅”；若用例画像里包含 `姓名：...`、`rider_name=...` 或“某师傅”，会优先使用该值。
- 官方长 Opening Line 会被拆成开场身份确认句：`你好，请问是王师傅吗？我是站长。`，不会把报名、X/Y 单、高峰上线等后续内容塞进第 0 轮。
- 飞毛腿“正常愿意配送”的 `case.initial_message` 调整为“是我，你说。”，语义仍是“用户听到 Opening Line 后的第一句回应”。
- 用户模拟器在全流程模式下按：确认身份 → 告知合同生效 → 询问能否开始配送 → 午晚高峰上线 → 单日/多日单量 → 未完成影响 → 少拒单少取消别超时和注意安全 → 收口 推进。
- 被测模型兜底和真实模型输出后处理增加飞毛腿正常流程保护：不再反问骑手“合同是否已经生效”，而是由站长告知合同已生效；用户问单量、影响、安全时分别给出对应短答。
- 全流程覆盖检查和报告评分同步新增“提醒午晚高峰上线”“说明未完成影响”“提醒少拒单取消超时和安全”等步骤。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向回归测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_case_deduplication.py tests\test_model_providers.py -q`，结果 `25 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `56 passed`。

AGENTS.md 更新说明：
- 本次是在既有飞毛腿官方任务约束内修正占位符、用例数据和流程推进，未改变接口契约、核心架构或长期验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次修复：飞毛腿任务内问题不再误判超范围

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

根据官方飞毛腿脱敏任务，修复合同完成要求、X/Y 单、合同影响、报名排名、退出流程、天气安全等奖励/资格相关问题被误判为“超范围问题”的评分问题，并清理模型回复中的 Markdown 粗体标记。

### 3. 修改文件列表

- `backend/app/services/rule_judge.py`
- `backend/app/services/target_model_client.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `rule_judge.py` 新增任务内 / 职责外问题判定：飞毛腿 FAQ 内的单日、多日、X 单、Y 单、合同影响、派单、退出、排名、奖励、天气、安全、拒单、取消、超时不再触发“超范围问题是否说明向同事确认后再回电”。
- 真正职责外问题，如工伤、赔偿、保险、社保、工资、劳动仲裁、投诉站长等，仍会触发“向同事确认后再回电”规则。
- 飞毛腿用户问“单日和多日分别要完成多少单”时，会激活“是否说明单日/多日合同完成要求”，不再激活超范围规则。
- `TargetModelClient.validate_reply_by_task_type(...)` 统一去除模型回复中的 `**` Markdown 粗体标记，因此 `**X 单**`、`**Y 单**` 会保存和展示为 `X 单`、`Y 单`。
- 新增回归测试覆盖任务内合同完成要求不触发超范围、真实职责外问题仍触发回电规则，以及飞毛腿回复去除 `**`。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 规则与 provider 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py tests\test_model_providers.py -q`，结果 `22 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `55 passed`。

AGENTS.md 更新说明：
- 本次是评分误判和输出格式清洗的 bug 修复，符合既有飞毛腿任务约束，没有改变长期规则、核心架构或接口契约，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次修复：openai_compatible 单消息限制兼容

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

修复部分 OpenAI-compatible 服务要求 `messages` 数组只能包含 1 条消息时报错 `The length of the input messages field has exceeded the limit_only_one` 的问题。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `TargetModelClient._openai_compatible_messages(...)` 改为把系统指令、任务上下文、历史用户发言和历史模型回复序列化进一条 `user` 消息中。
- 真实模型请求最终始终只发送一条 `{"role": "user", "content": "..."}`，兼容只允许单消息的接口。
- 保留 `stream=True` 优先调用；前端仍按 `/api/runs/stream` 的 `assistant_delta` 做打字机展示。
- 继续过滤空 content，避免 Opening Line 第 0 轮空用户消息进入真实模型请求。
- 对 openai provider 返回的 Opening Line 增加安全收口：如果模型把飞毛腿开场扩写成合同、报名、X/Y 单等后续说明，会回退为标准开场“你好，请问是你本人吗？我是站长。”。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- Provider 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_model_providers.py -q`，结果 `9 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `52 passed`。

AGENTS.md 更新说明：
- 本次是 provider 兼容性 bug 修复，没有改变长期规则、核心架构、接口契约或验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次修复：openai_compatible role 兼容

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

修复部分 OpenAI-compatible 服务只接受 `user` / `assistant` role 时，RunConsole 选择 `openai_compatible` 启动评测报错 `Role must be in [user, assistant]` 的问题。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/tests/test_model_providers.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `TargetModelClient._call_openai_chat(...)` 在发起真实模型调用前统一清洗消息。
- 将 `system` role 的系统约束、任务白名单和知识召回约束合并到第一条 `user` 消息中。
- 过滤空 content 消息，避免 Opening Line 第 0 轮的空用户消息传给兼容接口。
- 真实请求最终只包含 `user` / `assistant` role，同时仍优先使用 `stream=True`。
- 新增单测覆盖 system role 合并、空消息过滤和 `stream=True` 保留。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- Provider 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_model_providers.py -q`，结果 `8 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `51 passed`。

AGENTS.md 更新说明：
- 本次是 provider 兼容性 bug 修复，没有改变长期规则、核心架构、接口契约或验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次补充：RunConsole 保持边收边渲染的流式消息

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

在已有 `/api/runs/stream` 事件协议基础上，进一步修正 RunConsole 的前端消费方式：流式过程中构建出来的 `messages` 不再在 `done` 后被完整历史接口覆盖，确保页面始终保持 AI 对话式的边收边渲染效果。

### 3. 修改文件列表

- `frontend/src/pages/RunConsole.vue`
- `docs/当前进度.md`

### 4. 已完成内容

- `assistant_start` 仍负责创建空模型气泡。
- `assistant_delta` 继续逐字追加到当前模型气泡末尾。
- `user_message` 到达后立即显示用户气泡。
- `report_generating` 继续更新当前阶段文案，显示“报告生成中...”等报告阶段状态。
- `done` 到达后立即设置“评测完成”、启用“查看完整报告”，并后台拉取报告摘要。
- 流式路径不再执行 `messages.value = await request.get('/api/runs/{id}/messages')`，避免完成时一次性替换整段历史对话。
- 非流式回退路径仍保留完整消息拉取，用于浏览器不支持流式读取或流式接口不可用的兜底场景。

### 5. 本次验证结果

- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方注释 warning，不影响本次功能。
- 源码检查确认：`messages.value = await request.get('/api/runs/{id}/messages')` 仅存在于非流式回退路径，流式 `done` 不再覆盖 `messages`。
- 课程直播“负责人正常沟通”流式抽样确认：第 0 轮先收到 `assistant_start`，随后逐字 delta 拼出“您好，请问您是负责人吗？”；第 1 轮立即收到用户“我是负责人，你说吧。”，再逐字输出“课程发布页新增两个直播选项。”；报告阶段最后发送“报告生成中...”“LLM Judge 正在生成评分解释...”“Report Agent 正在生成报告...”和 `done`。

AGENTS.md 更新说明：
- 本次是 RunConsole 前端流式消费细节修正，没有改变长期规则、核心架构或接口契约，因此未更新 `AGENTS.md`。

## 〇、2026-05-20 本次修改：实现开始评测真正流式输出

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

本阶段不重建项目、不大改 UI，在现有 RunConsole 和 `/api/runs/stream` 基础上补齐真正的逐字/逐段流式输出：用户气泡先出现，被测模型气泡先创建，再逐字追加模型回复；对话结束后通过独立报告生成事件提示 Judge / Report Agent 正在处理。

### 3. 修改文件列表

- `backend/app/services/evaluation_service.py`
- `backend/app/services/target_model_client.py`
- `backend/tests/test_run_stream.py`
- `backend/tests/test_model_providers.py`
- `frontend/src/pages/RunConsole.vue`
- `frontend/src/components/ConversationTimeline.vue`
- `AGENTS.md`
- `docs/当前进度.md`

### 4. 已完成内容

- `/api/runs/stream` 继续使用 `StreamingResponse` + `text/event-stream`，每个 SSE `data:` 行承载一个 JSON 事件。
- 后端新增 `assistant_start` 事件：第 0 轮 Opening Line 和后续业务轮次都会先创建被测模型气泡，再调用/展示模型回复。
- 后端将 `assistant_delta` 改为逐字输出，并在 delta 之间加入短节流，避免浏览器等到整轮结束后才一次性渲染。
- `mock_fallback` 也通过同一套 `assistant_start -> assistant_delta -> assistant_done` 事件模拟打字机输出。
- `openai_compatible` 调用优先使用 OpenAI SDK `stream=True`；若流式调用失败，再回退到普通完整回复，并由 `/api/runs/stream` 按字符切片输出给前端。
- 报告阶段新增 `report_generating` 事件，依次展示“报告生成中...”“LLM Judge 正在生成评分解释...”“Report Agent 正在生成报告...”“报告已生成”，并继续发送 `judge_result` 和 `done`。
- 前端 `RunConsole.vue` 新增 `assistant_start`、`report_generating` 事件处理，保留现有布局，仅更新当前阶段文案和流式消息合并逻辑。
- `ConversationTimeline.vue` 对空的流式模型气泡显示光标，不再显示“暂无回复内容”占位，保证视觉上是真正等待模型增量输出。
- 流式异常仍以 `error` 事件返回，前端收到后停止 loading 并展示错误原因。

### 5. 流式事件格式

核心事件保持如下结构：

```json
{"event":"assistant_start","turn_index":0}
{"event":"assistant_delta","turn_index":0,"content":"您"}
{"event":"assistant_done","turn_index":0,"content":"您好，请问您是负责人吗？"}
{"event":"user_message","turn_index":1,"content":"我是负责人，你说吧。"}
{"event":"rule_result","turn_index":1,"summary":"本轮评分完成"}
{"event":"report_generating","message":"LLM Judge 正在生成评分解释..."}
{"event":"judge_result","score":89.23,"reason":"..."}
{"event":"done","run_id":123,"report_id":456}
```

### 6. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 流式与 provider 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_run_stream.py tests\test_model_providers.py -q`，结果 `9 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `50 passed`。
- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方注释 warning，不影响本次功能。
- 单测已校验 `/api/runs/stream` 返回 `assistant_start`、逐字 `assistant_delta`、`assistant_done`、`report_generating`、`judge_result`、`done`，且 `assistant_start` 出现在首个 `assistant_delta` 之前。
- 接口抽样输出确认第 0 轮事件顺序为 `stage -> stage -> assistant_start -> assistant_delta...`，尾部事件顺序为 `report_generating -> judge_result -> report_generating -> report_generating -> done`。

### 7. 当前仍存在的问题

- 为保证任务白名单和串场校验，`openai_compatible` 的最终落库内容仍会经过 `TargetModelClient.generate_reply(...)` 的任务类型校验；如果外部 SDK 不支持 `stream=True`，前端仍会看到按字符模拟的流式效果。
- 本次完成了编译、测试和构建验证；真实浏览器点击的主观观感仍建议在演示环境再手动确认一次。

AGENTS.md 更新说明：
- 本次固化了 `/api/runs/stream` 事件顺序、报告阶段事件和异常事件，属于开始评测页核心交互约束，因此已同步更新 `AGENTS.md`。

## 〇、2026-05-20 本次修改：修正 Opening Line 对话启动顺序

### 1. 本次修改日期

2026-05-20

### 2. 本次修改目标

本阶段不重建项目、不大改 UI，修正官方脱敏外呼任务的对话起点：存在 Opening Line 的任务必须由被测模型先发起开场白，再由用户模拟器根据 `case.initial_message` 回应。

### 3. 修改文件列表

- `backend/app/services/evaluation_service.py`
- `backend/app/services/target_model_client.py`
- `frontend/src/components/ConversationTimeline.vue`
- `frontend/src/pages/RunConsole.vue`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_knowledge_base.py`
- `backend/tests/test_task_reply_isolation.py`
- `AGENTS.md`
- `docs/当前进度.md`

### 4. 已完成内容

- `EvaluationService.start_evaluation(...)` 和 `/api/runs/stream` 均新增第 0 轮 Opening Line 启动逻辑：先保存被测模型开场，再进入第 1 轮用户回应。
- `TargetModelClient` 新增 `generate_opening_reply(...)` 和 Opening Line 提取/简化逻辑；课程直播默认开场为“您好，请问您是负责人吗？”，飞毛腿默认开场为“你好，请问是你本人吗？我是站长。”。
- `case.initial_message` 语义调整为“用户听到开场白后的第一句回应”，不再表示系统对话第一句话。
- Opening Line 阶段单独评分，只检查身份确认开场、任务角色和业务串场，不检查费用、企业微信、配置路径等后续流程规则。
- RunConsole 对话时间线支持显示“第 0 轮 · 被测模型开场”，并把用例摘要中的“初始问题”改为“用户首句回应”。
- 修复课程直播回复格式规范化被误记为 validator fallback 的日志噪音。
- 回归测试更新为区分 Opening Line 消息和业务回复消息。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `49 passed`。
- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方注释 warning，不影响本次功能。
- 实际场景验证 1：课程直播“负责人正常沟通”前四条消息为：第 0 轮被测模型“您好，请问您是负责人吗？”；第 1 轮用户“我是负责人，你说吧。”，被测模型“课程发布页新增两个直播选项。”；第 2 轮用户追问标准/低延迟区别。
- 实际场景验证 2：飞毛腿“质疑报名排名”前两条消息为：第 0 轮被测模型“你好，请问是你本人吗？我是站长。”；第 1 轮用户“为什么别人能报上，我排不上？”，被测模型说明报名按排名进行且不是站长人工干预。

### 6. 当前仍存在的问题

- Opening Line 目前作为 `run_messages.turn_index=0` 保存；历史已生成的 run/report 不会自动重算，需要重新执行评测才能看到新起点。
- 课程直播“标准直播/低延迟区别”仍保持短句分步回答，因此第一句先说明标准直播，下一轮再补充低延迟。

### 7. 下一步建议

- 在浏览器中手动复测 RunConsole 流式展示，确认第 0 轮开场、用户首句回应和后续业务轮次的视觉顺序符合演示预期。

AGENTS.md 更新说明：
- 本次固化了 Opening Line 启动顺序和 `case.initial_message` 长期语义，属于核心流程约束变化，因此已同步更新 `AGENTS.md`。

## 〇、2026-05-19 本次修改：强化 Agent 平台定位与不训练模型说明

### 1. 本次修改日期

2026-05-19

### 2. 本次修改目标

本阶段不重建项目、不大改 UI，在现有 Dashboard、RunConsole、模型配置页和文档基础上强化“Agent 编排 + 轻量 RAG + LLM-as-a-Judge”的平台定位，同时明确当前系统不进行模型训练，`mock_fallback` 仅用于演示兜底。

### 3. 修改文件列表

- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/RunConsole.vue`
- `frontend/src/pages/ModelConfig.vue`
- `docs/系统总体设计.md`
- `docs/产品说明文档.md`
- `docs/当前进度.md`

### 4. 已完成内容

- Dashboard 新增“Agent 评测工作流”卡片，展示：任务指令解析 → 知识召回 → 用户模拟器 Agent → 被测模型 Agent → 规则裁判 → Judge Agent → 报告生成 Agent。
- Dashboard 的 AI 能力说明补充 Knowledge Retriever，并突出真实模型接入、LLM-as-a-Judge 和可解释报告。
- RunConsole 顶部工作流改为 Agent/组件命名：Scenario Agent、Knowledge Retriever、User Simulator Agent、Target Model、Rule Judge、LLM Judge Agent、Report Agent，并保留当前步骤高亮。
- RunConsole 开始评测页补充 provider 说明：`mock_fallback` 仅用于演示兜底，不是真实 AI；`openai_compatible` 和 `custom_endpoint` 才是真实模型接入方式。
- ModelConfig 新增“真实模型接入说明”，明确 `mock_fallback` 只保证演示流程跑通，API Key 不在前端保存。
- `docs/系统总体设计.md` 和 `docs/产品说明文档.md` 明确当前系统不进行模型训练，并说明原因：官方脱敏数据量较小、不适合直接微调；赛题重点是复杂指令下自动化评测；系统 AI 能力来自 Agent 编排、轻量 RAG 和 LLM-as-a-Judge；后续积累标注数据后才考虑微调用户模拟器或评估器。
- 两份文档补充 AI 能力来源：用户模拟器 Agent、Knowledge Retriever、`openai_compatible`、LLM-as-a-Judge、Report Agent。

### 5. 本次验证结果

- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方注释 warning，不影响本次功能。
- 源码检查：确认 `Dashboard.vue` 包含“Agent 评测工作流”和完整 Agent 链路。
- 源码检查：确认 `RunConsole.vue` 包含 Scenario Agent、Knowledge Retriever、User Simulator Agent、Target Model、Rule Judge、LLM Judge Agent、Report Agent，并包含 `mock_fallback` 演示兜底说明。
- 源码检查：确认 `ModelConfig.vue` 包含“真实模型接入说明”和真实 provider 接入说明。
- 文档检查：确认 `docs/系统总体设计.md` 和 `docs/产品说明文档.md` 均包含“不进行模型训练”“轻量 RAG”“LLM-as-a-Judge”“Knowledge Retriever”“Report Agent”和 `mock_fallback` 边界说明。

### 6. 当前仍存在的问题

- 本次为展示与文档定位强化，没有新增训练能力，也没有改变现有 provider 接口。
- 真实浏览器中的视觉观感仍建议在演示环境手动确认一次，重点看 Dashboard 新卡片、RunConsole 顶部工作流和 ModelConfig 说明条是否与现有深色主题协调。

### 7. 下一步建议

- 后续可在报告页继续补充 Agent 证据链，例如展示每个阶段使用的知识片段、触发规则和 Judge 证据来源。

AGENTS.md 更新说明：
- 本次没有改变长期规则、核心架构、启动方式、禁止事项或重要验证标准；属于页面展示与文档说明强化，因此未更新 `AGENTS.md`。

## 〇、2026-05-19 本次修改：RunConsole 接入流式评测展示

### 1. 本次修改日期

2026-05-19

### 2. 本次修改目标

本阶段不重建项目、不大改 UI，在已有深色加载态基础上，让 `RunConsole.vue` 优先接入 `POST /api/runs/stream`，评测过程按 SSE 事件逐步展示用户气泡、被测模型打字机回复、规则结果和 Judge 初评；如果浏览器或接口不支持流式，再回退到原 `/api/runs/start`。

### 3. 修改文件列表

- `frontend/src/pages/RunConsole.vue`
- `frontend/src/components/ConversationTimeline.vue`
- `backend/app/services/evaluation_service.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `RunConsole.vue` 的开始评测逻辑改为优先使用浏览器 `fetch` + `ReadableStream` 调用 `/api/runs/stream`。
- 新增 SSE 解析逻辑，逐条读取 `data:` JSON，并根据 `event` 更新页面状态。
- 收到 `stage` 时更新顶部深色 Agent 工作流：任务初始化、知识召回、用户模拟、模型回复、规则评分、LLM 评估、报告生成。
- 收到 `user_message` 时立即插入当前轮用户气泡；收到 `assistant_delta` 时追加到当前轮模型气泡，实现打字机效果；收到 `assistant_done` 时固定完整模型回复。
- 收到 `rule_result` 时更新当前轮命中规则、失败规则、引用知识点、轮次得分和扣分原因。
- 收到 `judge_result` 时右侧评分摘要显示 Judge Agent 初评总分和简要原因；收到 `done` 后拉取完整 report 和 run messages，并启用“查看完整报告”。
- 收到 `error` 时显示深色错误卡片并结束 loading，不让页面一直等待。
- `ConversationTimeline.vue` 新增模型流式输出光标，并显示 `扣分原因`。
- `/api/runs/stream` 的 `rule_result` 事件补充 `deduction_reason` 和 `current_stage` 字段，便于前端解释规则结果。
- 如果浏览器不支持 `ReadableStream`、流式接口不可用、或接口未返回 `text/event-stream`，前端会自动 fallback 到原 `/api/runs/start`，保持兼容。

### 5. 本次验证结果

- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方注释 warning，不影响本次功能。
- 流式接口专项与旧 start 回归：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_run_stream.py tests\test_runs.py -vv`，结果 `3 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `48 passed`。
- 实际本地 SSE 验证：调用 `POST /api/runs/stream` 后可看到 `user_message` 先返回，随后多条 `assistant_delta` 分段返回，再返回 `assistant_done` 和 `rule_result`；`rule_result` 已包含 `deduction_reason`。
- 源码检查：`RunConsole.vue` 无对话区 `v-loading`；新增的工作流、错误卡片、空状态和 Judge 初评卡片均使用深色样式。

### 6. 当前仍存在的问题

- 真实浏览器点击的视觉验证仍建议在演示环境中手动复测一次，确认不同窗口宽度下气泡滚动和右侧评分更新观感符合预期。
- 当前模型流式仍基于后端完整回复切分出的 `assistant_delta`，不是底层 provider 的真实 token streaming。
- fallback 到 `/api/runs/start` 时仍是一次性加载完整对话，但会保留深色进度状态，不会出现白色 loading。

### 7. 下一步优先级

- 在浏览器中手动跑飞毛腿和课程直播各一条用例，确认用户气泡、模型打字机、规则结果、Judge 初评和完整报告跳转串起来。
- 如后续要进一步提升真实感，可让 `TargetModelClient` 对 `openai_compatible` provider 支持真实 token streaming。

AGENTS.md 更新说明：
- 本次属于前端展示链路和既有流式接口的接入，不改变项目定位、核心约束、启动方式、禁止事项或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-19 本次修改：新增单次评测流式接口

### 1. 本次修改日期

2026-05-19

### 2. 本次修改目标

本阶段不重建项目、不大改已有接口，在保留 `POST /api/runs/start` 一次性评测接口的基础上，新增 `POST /api/runs/stream`，让开始评测过程可以通过 Server-Sent Events 逐步输出阶段、用户消息、被测模型分段回复、规则结果、Judge 结果和最终 run/report。

### 3. 修改文件列表

- `backend/app/api/runs.py`
- `backend/app/services/evaluation_service.py`
- `backend/tests/test_run_stream.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增 `POST /api/runs/stream`，请求体复用 `RunStartRequest`，字段与 `/api/runs/start` 保持一致：`task_id`、`case_id`、`model_provider`、`model_name`。
- 使用 `StreamingResponse` 输出 `text/event-stream`，每条 SSE 的 `data:` 内容都是 JSON，并带有 `event` 字段。
- `EvaluationService` 新增 `stream_evaluation_events(...)`，复用现有评测主链路中的任务/用例读取、`UserSimulator`、`TargetModelClient`、`RuleJudge`、`EvaluatorAgent` 和 `ReportService`。
- 流式事件覆盖：`stage`、`user_message`、`assistant_delta`、`assistant_done`、`rule_result`、`judge_result`、`done`。
- 当前被测模型 SDK 仍按非流式方式拿完整回复；流式接口会把完整回复按短片段切分为多个 `assistant_delta`，不影响最终 run/message/report 保存。
- 异常路径会输出 `{"event":"error","message":"..."}`；如果 run 已创建，会把 run 标记为 `failed` 并结束流，避免前端一直 loading。
- 新增 `test_run_stream.py`，覆盖正常流式事件、持久化 report/message、以及缺失 task 时返回 error 事件。

### 5. 本次验证结果

- 流式接口专项与旧 start 回归：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_run_stream.py tests\test_runs.py -vv`，结果 `3 passed`。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `48 passed`。
- 实际本地接口验证：调用 `POST http://127.0.0.1:8080/api/runs/stream`，`task_id=4`、`case_id=13`、`model_provider=mock_fallback`，持续返回 `stage`、`user_message`、多条 `assistant_delta`、`assistant_done`、`rule_result`、`judge_result`，最后返回 `done`，生成 `run_id=1596`。
- 错误场景验证：调用不存在的 `task_id=999999`，流式返回 `stage` 后返回 `error: task not found`。
- 原有非流式接口验证：调用 `POST /api/runs/start`，`task_id=4`、`case_id=13`、`model_provider=mock_fallback`，返回 `success=true`，生成 `run_id=1597`，原接口不受影响。

### 6. 当前仍存在的问题

- 当前 `assistant_delta` 是后端拿到完整回复后的模拟分段流式输出，不是真实 provider token streaming；后续若接入 OpenAI compatible streaming SDK，可在 `TargetModelClient` 内扩展真实 token 级流式。
- `stream_evaluation_events(...)` 与 `start_evaluation(...)` 目前各自编排保存逻辑，行为保持一致但存在少量重复代码；后续若继续扩展评测流程，可再抽公共执行器降低维护成本。
- 前端暂未接入 `/api/runs/stream`，本次只新增后端接口和测试。

### 7. 下一步优先级

- 下一步可让 `RunConsole.vue` 在点击“开始评测”时优先调用 `/api/runs/stream`，用事件直接驱动深色进度条和对话区逐步渲染。
- 如果真实 `openai_compatible` provider 需要更自然的 token 级输出，再单独扩展 `TargetModelClient` 的流式调用能力。

AGENTS.md 更新说明：
- 本次新增的是评测运行接口能力，但未改变项目定位、核心约束、启动方式、禁止事项或重要验证标准；原 `/api/runs/start` 保持兼容，因此未更新 `AGENTS.md`。

## 〇、2026-05-19 本次修改：修复开始评测深色加载态

### 1. 本次修改日期

2026-05-19

### 2. 本次修改目标

本阶段不重建项目、不大改 UI，只修复 `RunConsole` 开始评测时中间对话区出现大面积白色 loading 覆盖层的问题，让加载态、空状态和错误状态都符合当前深色 AI 评测平台主题。

### 3. 修改文件列表

- `frontend/src/pages/RunConsole.vue`
- `docs/当前进度.md`

### 4. 已完成内容

- 移除对话面板上的 `v-loading`，避免 Element Plus 默认白色遮罩覆盖深色对话区。
- 新增深色评测进度状态，按阶段展示：初始化评测任务、召回知识库、用户模拟器 Agent 生成用户发言、调用被测模型、规则评分、Judge Agent 评估和报告生成。
- 开始按钮在运行中显示为“评测中...”，禁用重复点击，并在按钮下方显示当前执行阶段。
- 对话区空状态改为深色背景、低饱和图标和说明文字：“请选择任务和用例后开始评测”。
- 接口失败或返回失败时显示深色错误状态，包含“评测失败”、错误原因和“请检查后端服务或模型配置”，避免白屏。
- 评分摘要空状态改为深色说明块，不再使用默认白底 `el-empty`。
- 为 `RunConsole` 保留防御性深色 `el-loading-mask` / `el-empty` 样式覆盖，避免后续局部默认组件回退到白色主题。

### 5. 本次验证结果

- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方注释 warning，不影响本次页面修复。
- 源码检查：`RunConsole.vue` 已无对话面板级 `v-loading` 和直接渲染的 `el-empty`；仅保留按钮 loading、深色进度组件和防御性深色覆盖样式。
- 本地服务检查：`127.0.0.1:8080` 和 `127.0.0.1:5173` 均在监听，前端根页面可返回 `index.html`。
- 评测链路检查：调用 `POST /api/runs/start`，`task_id=4`、`case_id=13`、`model_provider=mock_fallback` 返回 `success=true`，生成 `run_id=1592`、`report_id=1592`。

### 6. 当前仍存在的问题

- 本次是 `RunConsole` 局部体验修复，未改其他页面的表格 loading 或空状态。
- 当前阶段进度是前端根据评测流程展示的阶段提示；后端尚未提供逐阶段流式事件，因此真实耗时较短时阶段可能快速跳转到报告生成。

### 7. 下一步优先级

- 在浏览器中手动点击“开始评测”，确认运行中对话区只出现深色进度状态，不再出现白色覆盖层。
- 如后续需要更精准进度，可考虑让后端评测链路输出阶段事件，但不建议在本次 UI 修复里扩大接口改动。

AGENTS.md 更新说明：
- 本次属于页面 loading / 空态 / 错误态的局部体验修复，没有改变项目定位、核心架构、长期规则、启动方式、禁止事项或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-19 本次修改：区分分支专项与全流程覆盖用例

### 1. 本次修改日期

2026-05-19

### 2. 本次修改目标

本阶段不重建项目、不大改 UI，重点修正后端用例模式、全流程 expected_steps、对话推进和最终规则评分。目标是让课程直播“负责人正常沟通”等 `full_flow` 用例逐步覆盖收费检查、企业微信添加和结束确认，同时让“追问直播区别”“质疑报名排名”等 `branch` 用例只评测当前分支，不强制覆盖全量官方流程。

### 3. 修改文件列表

- `backend/app/models/case.py`
- `backend/app/schemas/case.py`
- `backend/app/core/database.py`
- `backend/app/api/cases.py`
- `backend/app/api/reports.py`
- `backend/app/schemas/report.py`
- `backend/app/services/case_mode.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/dialogue_state.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_case_deduplication.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `EvaluationCase`、Case schema 和 SQLite 兼容迁移新增 `expected_steps` 字段，用于记录全流程用例应覆盖的关键步骤。
- 官方课程直播“负责人正常沟通”设为 `case_mode=full_flow`，`max_turns=10`，`expected_steps` 覆盖：身份确认、产品升级说明、确认是否知情、直播区别、发布方式、配置路径、费用/加速线路费、企业微信添加、结束确认。
- 官方飞毛腿“正常愿意配送”设为 `full_flow`，`expected_steps` 覆盖：确认身份、告知合同生效、询问是否开始配送、完成要求、安全提醒、结束确认。
- 飞毛腿“不想配送”修正为 `branch`；迁移回填和 `case_mode` 推断不再把普通“不想配送”自动归为异常终止。
- `evaluation_service.py` 的 `full_flow` 结束逻辑改为基于 `expected_steps` 覆盖率：不足 70% 不提前结束；达到 70% 后可进入收尾；异常拒绝或开车仍可提前终止。
- `UserSimulatorAgent` 在 `full_flow` 模式下按 `expected_steps` 逐步追问：课程直播会推进到区别、第三方配置、看不到选项、学员端费用、企业微信和“没问题了”；飞毛腿会推进合同生效、完成单量、影响和安全提醒。
- `rule_judge.py` 在整段报告评分时对 `full_flow` 用例补充“全流程覆盖：{step}”规则；未完成的 expected_steps 只在全流程结束后计入失败，分支用例不因此扣分。
- 报告 API 增加 `full_flow_expected_steps` 输出，便于前端或调试查看每个 expected step 的完成状态、待完成步骤和覆盖率。
- 课程直播 mock 回复补充费用、企业微信和第三方系统看不到选项后的简短回答，保证全流程用例能自然推进到官方 Step 5、Step 6、Step 7。

### 5. 本次验证结果

- 后端编译检查：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 相关回归：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py tests\test_active_rules.py tests\test_case_deduplication.py -vv`，结果 `23 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest -q`，结果 `46 passed`。
- 验证 1：课程直播“追问直播区别”仍为 `branch`，每轮规则文本不包含企业微信、结束确认、优惠券等全流程后续步骤。
- 验证 2：课程直播“负责人正常沟通”实际跑出 9 轮，覆盖标准直播 5-10 秒、低延迟 1-2 秒、第三方配置路径、学员端费用、企业微信添加和结束确认；报告 `full_flow_expected_steps` 为 9/9，`coverage_ratio=1.0`。
- 验证 3：飞毛腿“质疑报名排名”仍只激活排名、非站长干预、避免承诺资格和简短自然，不检查退出、天气、安全或奖励规则。

### 6. 当前仍存在的问题

- 本次只修后端用例规划、模拟器推进和报告输出，未新增前端样式或新的全流程可视化组件。
- 历史已生成的 run/report 不会自动重算 `full_flow_expected_steps`；需要重新执行评测才能看到新结果。
- `full_flow` 当前以关键词识别 expected_steps 完成情况，足够支撑官方脱敏任务；如果后续导入更复杂任务，需要继续补充任务特定步骤识别规则。

### 7. 下一步优先级

- 优先在浏览器开始评测页手动跑课程直播“负责人正常沟通”，确认前端对 8-10 轮全流程展示清晰，并在报告页查看 `full_flow_expected_steps`。
- 再用真实 `openai_compatible` provider 跑同一全流程用例，观察真实模型是否能遵守短句分步 prompt 并覆盖费用、企业微信和结束确认。

AGENTS.md 更新说明：
- 本次是已有 `case_mode` / `expected_steps` 机制下的后端细化与回归修复，没有改变项目定位、核心架构、长期开发规则、启动命令、禁止事项或重要验证标准，因此本轮未更新 `AGENTS.md`。

## 〇、2026-05-19 本次修改：优化课程直播短句回复与发言机会判定

### 1. 本次修改日期

2026-05-19

### 2. 本次修改目标

本阶段只处理课程直播任务中的被测模型 prompt、简短回复判定和“给商家发言机会”判定，不修改飞毛腿任务规则，不新增 RAG，不改前端样式。目标是让课程直播回复更符合官方“15-20 字、一个信息点、说完暂停”的外呼要求，同时避免评分器把合理的一句话回答误判为“不给用户发言机会”。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/dialogue_state.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_knowledge_base.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `TargetModelClient` 的课程直播 system prompt 增加硬约束：每次最多约 15-20 个中文字符、每次只说一个信息点、说完停下来等商家回应、不要一次性讲完区别/费用/配置/企业微信、多问题分步回答、不使用“好的/哈哈/嗯嗯/嘿嘿/嘻嘻”等语气词。
- 课程直播开车场景 mock 回复固定为“那我稍后再打。”并结束；忙碌场景先回复“就1分钟，保证简短。”，后续只补一句重点。
- 课程直播“追问直播区别”mock 策略改为分步说明：首轮只说“标准直播延迟约5-10 秒。”；若已说明标准直播，再补“低延迟约1-2 秒，更适合互动。”；费用问题单独回答。
- `rule_judge.py` 调整课程直播简短规则：普通轮次 25 字以内通过，26-40 字作为轻微扣分，超过 40 字失败；用户明确要求详细或一次说清楚时放宽到 50 字以内；多行编号、多个步骤或多个分号连续说明会被识别为可能过长。
- `rule_judge.py` 调整“禁止不给用户发言机会”：只有连续灌输多个流程步骤、一次性讲完区别/费用/配置/企业微信、用户忙或打断后仍长篇解释、用户开车后仍继续推销时才判失败。
- 简短一句说明、一个信息点后暂停、或以简短问题收尾（如“您用 Web 控制台还是第三方系统发课？”）会判定为给商家发言机会。
- `dialogue_state.py` 将课程直播“已说明标准直播延迟”和“已说明低延迟直播延迟”拆开识别，只有两者都覆盖时才算完成“标准直播/低延迟直播区别”；同时将“怎么配置/哪里/路径/选项”优先识别为配置路径阶段，避免“你说重点，下一步怎么配置？”被误路由为忙碌场景。
- 回归测试补充课程直播短句首步、组合长答轻扣、不抢话、发布方式问题给发言机会、课程 prompt 约束和 mock 分步回复断言。

### 5. 本次验证结果

- 课程直播专项回归：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py tests\test_dialogue_deduplication.py tests\test_knowledge_base.py -vv`，结果 `23 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `46 passed`。
- 后端编译检查：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 验证 1：课程直播“追问直播区别”，模型回复“标准直播延迟约5-10 秒。”时，命中“是否回复简短自然”和“是否说明标准直播延迟”，不失败“禁止不给用户发言机会”。
- 验证 2：模型回复“标准直播延迟5-10秒，费用低；低延迟直播1-2秒，互动更流畅。”时，可产生简短性轻微扣分，但不会同时乱判多个失败，也不会严重失败“不给用户发言机会”。
- 验证 3：模型回复“您用 Web 控制台还是第三方系统发课？”时，命中“是否询问当前发布方式”和“是否给商家发言机会”。
- 验证 4：课程直播“负责人正常沟通”仍能覆盖发布页升级、5-10 秒、1-2 秒、Web/第三方发布方式和配置路径，未因分步短句策略提前中断。

### 6. 当前仍存在的问题

- 本次只修课程直播任务的 prompt 与评分判定，未改前端展示，历史报告不会重新计算旧规则结果。
- 课程直播真实外部模型如果忽略 system prompt 继续长篇输出，后端会按新的阈值扣分，但不会强制截断真实 provider 的原始回复；安全替换仍只处理任务串场等硬约束。
- “26-40 字轻微扣分”目前体现在指标分扣减中，不单独新增一条前端可见的“轻微扣分规则”，避免扩大本阶段展示改动。

### 7. 下一步优先级

- 优先用真实 `openai_compatible` provider 跑课程直播“追问直播区别”和“负责人正常沟通”，观察真实模型是否稳定遵守短句分步 prompt。
- 如真实模型仍频繁长答，下一步应在 provider 返回后增加课程直播专用的非破坏性提示反馈或二次压缩策略，但需要单独评估是否会影响“被测模型原始表现”的评测真实性。

AGENTS.md 更新说明：
- 本次属于课程直播任务下的局部 prompt 与评分判定修复，没有改变项目定位、核心架构、长期开发规则、启动命令、禁止事项或重要验证标准，因此本轮未更新 `AGENTS.md`。

## 〇、2026-05-19 本次修改：修复 case_focus 规则路由与同义命中

### 1. 本次修改日期

2026-05-19

### 2. 本次修改目标

本阶段只修后端规则激活和规则命中判断，不改前端样式、不新增新功能。目标是让每个官方用例只激活当前用例相关规则，支持“不是站长手动决定”等同义表达命中，并禁止 LLM-as-a-Judge 把 active rules 之外的规则作为失败项写入报告。

### 3. 修改文件列表

- `backend/app/services/rule_judge.py`
- `backend/app/services/agents/evaluator_agent.py`
- `backend/app/services/report_service.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/api/reports.py`
- `backend/app/schemas/report.py`
- `backend/tests/test_active_rules.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `rule_judge.py` 新增 `case_focus` 推断路由，优先根据 `case_focus` 字段，缺省时根据 `case.name`、`user_profile`、`initial_message`、`user_behavior_type` 推断。
- 飞毛腿 `case_focus` 已覆盖：`normal_delivery`、`unwilling_delivery`、`contract_impact`、`exit_flying_leg`、`bad_weather`、`ranking_question`、`reward_question`。
- 课程直播 `case_focus` 已覆盖：`responsible_person`、`non_responsible_person`、`busy_merchant`、`driving_merchant`、`live_type_difference`、`third_party_config_missing`、`fee_or_coupon`。
- 已知官方用例走固定 focus 规则集合，不再从任务 instruction 或全量 triggerable rules 中混入退出、天气、排名、奖励、企业微信、优惠券等无关规则。
- `ranking_question` 只激活报名排名、非站长干预、避免承诺资格、自然简短；`exit_flying_leg` 只激活退出流程；`bad_weather` 只激活安抚、安全、雨天订单/资格和避免冒险配送。
- “是否说明不是站长干预”增加同义/组合命中：不是站长决定、不是站长手动决定、不是我这边能调、不是我能决定、不是人为指定、非人工指定、非手动、排名规则决定、名额按排名等。
- “是否说明报名按排名进行”增加同义命中：按排名、排名靠前、名额按排名、报名排名、系统排名、排名规则。
- “是否避免承诺一定获得资格”等避免型规则改为风险词不存在即可通过；若出现“一定能报上 / 我给你名额 / 保证有资格 / 站长给你调整”等才失败。
- `score_turn`、每轮 message detail、报告 explainability 和报告 API 输出补充 `case_focus` 与 `active_rule_names`，保留原有 `active_rules` 字典结构兼容前端。
- `EvaluatorAgent` prompt 明确 LLM 只能评价 `active_visible_rules`，并在解析 OpenAI compatible 结果时过滤 active rules 之外的 `evidence.issue` 和 `failed_rules`。
- `ReportService` 在合并 LLM evidence / failure cases 前再次过滤，防止 LLM 自由发明的非当前用例规则进入报告或前端展示。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 规则专项测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py -vv`，结果 `5 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `41 passed`。
- 验证 1：飞毛腿“质疑报名排名”，用户问“为什么别人能报上，我排不上？”，模型答“报名排名不是站长手动的，排名靠前自然上去了。”；命中“是否说明报名按排名进行”和“是否说明不是站长干预”，且“是否说明不是站长干预”不失败。
- 验证 2：飞毛腿“询问如何退出飞毛腿”，用户问“你还没说怎么退出飞毛腿。”；`case_focus=exit_flying_leg`，只激活前一天 Z 点前、App 飞毛腿报名取消、次日生效、避免编造退出方式和自然简短，不出现“是否说明不是站长干预”。
- 验证 3：飞毛腿“抱怨恶劣天气”，用户问“今天下雨这么大，怎么还让我跑？”；`case_focus=bad_weather`，只激活安抚、安全、雨天订单/资格、避免强迫冒险和自然简短，不出现退出流程、排名规则或奖励规则。
- LLM 过滤验证：构造 LLM 输出“是否说明不是站长干预”作为退出用例失败项时，`EvaluatorAgent` 和 `ReportService` 均会过滤，只保留 active rules 子集。

### 6. 当前仍存在的问题

- `case_focus` 目前主要通过官方用例名称和初始问题推断；如果用户手动创建新名称用例且未提供明确触发语，可能回落到旧的通用激活逻辑。
- 本次未新增数据库字段；报告和每轮 message detail 会输出推断后的 `case_focus`，但 Case 表本身暂未持久化该字段。
- 历史报告不会重算；只有新运行的评测使用新的 focus 路由、同义匹配和 LLM 过滤逻辑。

### 7. 下一步优先级

- 在浏览器里实际跑三条回归用例：质疑报名排名、询问如何退出飞毛腿、抱怨恶劣天气，确认开始评测页和报告页展示的失败规则与后端一致。
- 如继续增强，可在 Case 编辑/生成链路中显式保存 `case_focus`，减少对名称推断的依赖。

AGENTS.md 更新说明：
- 本次属于后端规则激活 bug 修复和评估结果过滤，没有改变项目定位、核心架构、长期开发规则、启动命令、禁止事项或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-18 本次修改：精简官方用例并分层规则展示

### 1. 本次修改日期

2026-05-18

### 2. 本次修改目标

本阶段只修改后端测试用例和规则判断逻辑，不重建项目、不大改前端页面。目标是把官方脱敏任务的默认用例收敛到核心 7+7，并在规则评分中区分业务可见规则和系统隐藏防串场规则，避免“质疑报名排名”等分支用例展示退出、安全、奖励或旧客服检查。

### 3. 修改文件列表

- `backend/app/services/rule_judge.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/api/reports.py`
- `backend/app/schemas/report.py`
- `backend/app/services/case_registry.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/seed/sample_data.py`
- `backend/tests/test_active_rules.py`
- `backend/tests/test_case_deduplication.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 官方飞毛腿任务默认用例收敛为 7 条：正常愿意配送、不想配送、询问合同影响、询问如何退出飞毛腿、抱怨恶劣天气、质疑报名排名、追问奖励规则。
- 官方课程直播任务默认用例收敛为 7 条：负责人正常沟通、非负责人转达、商家说忙、商家说在开车、追问直播区别、第三方系统看不到选项、询问费用或优惠。
- 新增 `sync_task_cases_to_catalog(...)`，启动 seed / Excel 导入时同步官方目录用例，删除非目录且非 `manual` 的历史用例，并迁移 run/report/batch item 的 `case_id` 引用。
- 同一 `task_id + name + initial_message` 的重复非 manual 用例会保留最早一条，其余删除；`data_source=manual` 的手动用例保留。
- `rule_judge.py` 新增 `visible_business_rules`，只承载当前用例、当前阶段和用户已触发的业务评分规则。
- `rule_judge.py` 新增 `hidden_guardrail_rules`，用于内部检查订单号、退款、酒店、团购券、商家出餐时间、平台超时规则、旧客服场景串用以及跨任务串场。
- 隐藏防串场规则不进入普通 `matched_rules`；只有真实违规时写入 `hidden_guardrail_rules.violated` 和报告 `failure_cases` 的“系统防串场检查”来源。
- 飞毛腿分支触发规则收敛为退出、天气安全、报名排名、合同影响、奖励补贴五类；未触发分支不进入 `visible_business_rules.failed`。
- “质疑报名排名”用例的业务可见评分规则收敛为：是否说明报名按排名进行、是否说明不是站长干预、是否避免承诺一定获得资格、是否回复自然简短。
- 课程直播分支触发规则收敛为负责人、非负责人、忙、开车、直播区别、费用优惠、第三方配置路径等场景；负责人正常沟通首轮不激活开车、优惠券、企业微信等未触发规则。
- 每轮 message detail 和报告 API 补充 `visible_business_rules`、`hidden_guardrail_rules`、`deduction_reason`，同时保留旧的 `matched_rules`、`failed_rules`、`active_rules` 等兼容字段。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 规则激活专项测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py -vv`，结果 `3 passed`。
- 用例目录与去重专项测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_case_deduplication.py -vv`，结果 `5 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `39 passed`。
- 验证飞毛腿“质疑报名排名”：`visible_business_rules` 只包含排名、非站长干预、避免承诺资格和自然简短；不包含订单号、退款、酒店、团购券、退出流程、安全提醒或奖励规则。
- 验证飞毛腿“询问如何退出飞毛腿”：只激活前一天 Z 点前、App 飞毛腿报名取消、次日生效、避免编造退出方式等退出流程规则。
- 验证课程直播“负责人正常沟通”：首轮 active/scored 规则不包含开车、优惠券、企业微信和结束通话规则。

### 6. 当前仍存在的问题

- 本次只改后端数据和评分结构，没有新增前端“系统防串场检查”专门展示区；完整报告可通过 `failure_cases.source=hidden_guardrail` 和 `hidden_guardrail_rules` 读取。
- 历史已生成 report 不会自动重算规则结果；只有新 run/report 使用新的规则分层。
- `data_source=manual` 的历史手动用例会保留，因此如果用户手动创建了非核心官方用例，列表中仍可能看到它们。

### 7. 下一步优先级

- 在浏览器中实际跑飞毛腿“质疑报名排名”和课程直播“负责人正常沟通”，确认前端默认展示只看到业务可见规则摘要。
- 如继续优化报告页，可增加一个折叠的“系统防串场检查”区，专门展示 `hidden_guardrail_rules.violated`。
- 后续可继续把更多非评分的“可建议项”从 required 规则中拆出，避免可选建议影响分支专项用例得分。

AGENTS.md 更新说明：
- 本次属于后端 bug 修复、官方用例目录收敛和规则输出结构补充，没有改变项目定位、核心架构、长期开发规则、启动命令、禁止事项或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-18 本次修改：优化评测过程前端展示

### 1. 本次修改日期

2026-05-18

### 2. 本次修改目标

在不重建项目、不大改 UI 的前提下，让开始评测页和报告页更直观展示评测起点、规则摘要、用户模拟状态和知识召回引用。重点避免把情绪、耐心误解为评分指标，同时让每轮知识引用可视化。

### 3. 修改文件列表

- `frontend/src/pages/RunConsole.vue`
- `frontend/src/components/ConversationTimeline.vue`
- `frontend/src/pages/ReportDetail.vue`
- `docs/当前进度.md`

### 4. 已完成内容

- `RunConsole.vue` 在多轮对话记录顶部新增“评测起点”展示区，包含用例名称、用户画像、初始问题、测试目标、用例模式、最大轮数和当前激活规则摘要。
- 对话区顶部保留当前目标，并把评测起点信息放在中间区域，避免用户画像和初始问题只存在于左侧配置栏。
- `ConversationTimeline.vue` 精简每轮默认规则展示：默认只展示当前阶段、命中规则前 3 条、失败规则前 3 条、待完成规则数量和未触发规则数量。
- 其余命中、遗漏、违规、待完成、未触发规则统一放到“展开查看规则详情”折叠区。
- 每轮用户模拟器状态默认只展示用户意图、对话进度和是否继续；`emotion_level`、`patience` 等模拟状态移入“调试信息”折叠区，并标注“模拟状态，仅用于生成下一轮用户，不参与评分。”
- 每轮 assistant 回复下方新增“引用知识点”展示，显示 `FAQ / Flow / Constraint / Opening` 类型和知识标题；无召回时显示“本轮未引用知识库”。
- `ReportDetail.vue` 新增“知识引用与使用评估”区，展示每轮引用知识点、模型是否已使用知识、未使用相关知识的原因，以及疑似知识库外编造内容。
- 完整对话证据继续复用 `ConversationTimeline.vue`，因此报告页每轮对话下也能看到知识引用和规则摘要。

### 5. 本次验证结果

- 前端构建：`npm run build` 通过；仍只有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning。
- 源码检查确认：飞毛腿退出用例在 RunConsole 中间顶部会显示 `selectedCase.initial_message`，并且每轮 assistant 下方会读取 `detail.retrieved_knowledge` 展示“退出飞毛腿流程”等知识标题。
- 源码检查确认：课程直播区别用例每轮 assistant 下方会展示 `FAQ：标准直播与低延迟直播区别`。
- 源码检查确认：情绪、耐心只出现在“调试信息”折叠区，默认展示字段为用户意图、对话进度和是否继续，不作为评分指标展示。

### 6. 当前仍存在的问题

- 本次未启动浏览器逐项截图验收；当前通过前端生产构建和源码路径检查确认展示逻辑。
- 知识引用展示依赖后端每轮 message detail 中的 `retrieved_knowledge`；历史旧 run 没有该字段时会显示“本轮未引用知识库”。
- 报告页知识使用评估依赖新报告中的 `llm_judge_result.knowledge_assessment`；历史报告缺少该字段时会显示没有记录知识召回。

### 7. 下一步优先级

- 用浏览器在开始评测页实际跑飞毛腿退出和课程直播区别两个用例，确认中间起点卡片、每轮知识引用和调试折叠区展示效果。
- 后续如继续优化前端，可在报告页为知识引用增加按知识标题筛选或跳转到对应轮次的轻量交互。

AGENTS.md 更新说明：
- 本次属于前端展示优化，没有改变项目定位、核心架构、长期开发规则、启动命令、禁止事项或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-18 本次修改：实现轻量知识召回

### 1. 本次修改日期

2026-05-18

### 2. 本次修改目标

实现轻量 RAG / 知识召回，让官方脱敏任务中的 `Opening Line`、`Conversation Flow / Call Flow`、`Knowledge Points / FAQ` 和 `Constraints` 参与对话生成与评估。本阶段只使用关键词检索，不引入向量数据库、不引入复杂 embedding、不新增外部知识库服务。

### 3. 修改文件列表

- `backend/app/services/knowledge_base.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/agents/evaluator_agent.py`
- `backend/tests/test_knowledge_base.py`
- `docs/当前进度.md`
- `AGENTS.md`

### 4. 已完成内容

- 新增 `knowledge_base.py`，提供 `build_knowledge_chunks(task)` 和 `retrieve_knowledge(task, user_message, top_k=3)`。
- 知识片段统一结构为 `task_id`、`chunk_type`、`title`、`content`、`source`，其中 `chunk_type` 取 `opening`、`flow`、`faq`、`constraint`。
- 从任务字段 `opening_line`、`call_flow`、`knowledge_points`、`constraints`、`instruction_text` 中拆分知识片段；对默认飞毛腿和课程直播任务补充精简 canonical 知识片段，避免长 instruction 文本污染单轮话术。
- 飞毛腿关键词召回已覆盖：退出/取消/报名、下雨/天气/危险、排名/名额/报不上、单日/多日/X 单/Y 单/影响、奖励/补贴。
- 课程直播关键词召回已覆盖：标准直播/低延迟/区别、费用/价格/便宜/优惠、Web/控制台/第三方/配置/设置、企业微信/加微信/手机号、开车、忙/没时间。
- `TargetModelClient.generate_reply(...)` 在生成每轮回复前先执行 `retrieve_knowledge(...)`。
- openai compatible prompt 和 custom endpoint payload 增加 `retrieved_knowledge`，并包含“以下是本轮可用知识片段，请优先依据这些内容回答。”的提示。
- mock fallback 在退出飞毛腿、合同影响、恶劣天气、报名排名、直播区别、配置路径、费用、开车、忙碌等分支优先使用召回知识片段组织回复。
- 每轮 `RunMessage.detail` 保存 `retrieved_knowledge`，后续报告和调试可直接追踪本轮召回证据。
- `EvaluatorAgent.evaluate(...)` 支持接收或从 messages 中读取 `retrieved_knowledge`，并输出 `knowledge_assessment`，判断已召回标题、已使用知识、遗漏知识和疑似编造知识。
- 调整 `fallback_used` 语义：去重改写只保留在 `target_call_chain` 中，不再把 provider fallback 标记为 true；安全替换和真实 provider fallback 仍会影响该标志。
- `AGENTS.md` 增加轻量知识召回长期约束和重要验证场景，并将 `knowledge_base.py` 加入核心后端文件。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 知识召回专项测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_knowledge_base.py -vv`，结果 `5 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `40 passed`。
- 前端构建：`npm run build` 通过；仍只有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning。
- “我想退出飞毛腿，怎么取消？”召回“退出飞毛腿流程”，消息 metadata 中保存该片段，mock 回复包含 App、Z 点、取消/次日生效等知识。
- “标准直播和低延迟有什么区别？”召回“标准直播与低延迟直播区别”，消息 metadata 中保存该片段，mock 回复包含 5-10 秒和 1-2 秒差异。
- 报告中的 `llm_judge_result.knowledge_assessment` 能看到对应 `retrieved_titles` 和 `used_knowledge`。

### 6. 当前仍存在的问题

- 当前是关键词召回，不做语义向量检索；同义表达覆盖依赖关键词表和规则扩展。
- canonical 知识片段优先保证默认脱敏任务演示稳定；如果后续导入新任务，需要继续增强通用字段拆分和标题归类。
- 前端尚未新增专门的知识召回展示区；当前可通过 run message detail 和报告中的 `llm_judge_result` 查看。

### 7. 下一步优先级

- 在报告详情页或对话消息 detail 中增加“本轮召回知识”轻量展示，方便演示证据链。
- 继续补充更多官方任务 FAQ/Constraints 的关键词触发词，尤其是企业微信、费用、配置路径和奖励/补贴边界。

AGENTS.md 更新说明：
- 本次将轻量知识召回接入被测模型生成、消息 metadata 和 Evaluator 评估，属于长期评测链路/核心架构变化，因此已更新 `AGENTS.md`。

## 〇、2026-05-18 本次修改：增加 case_mode 与对话轮次控制

### 1. 本次修改日期

2026-05-18

### 2. 本次修改目标

只处理用例模式和对话轮次控制，不新增 RAG、不新增知识库、不修改模型接入。通过 `case_mode` 区分分支专项、全流程覆盖和异常终止用例，避免“2 轮结束”和“应该继续推进”的场景混在一起看起来不稳定。

### 3. 修改文件列表

- `backend/app/models/case.py`
- `backend/app/schemas/case.py`
- `backend/app/core/database.py`
- `backend/app/services/case_mode.py`
- `backend/app/services/case_registry.py`
- `backend/app/api/cases.py`
- `backend/app/seed/sample_data.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/dialogue_state.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_active_rules.py`
- `frontend/src/components/CaseEditor.vue`
- `frontend/src/pages/CaseList.vue`
- `frontend/src/pages/RunConsole.vue`
- `docs/当前进度.md`
- `AGENTS.md`

### 4. 已完成内容

- `EvaluationCase`、Case schema、AI 生成草稿和手工用例编辑链路新增 `case_mode`，取值为 `branch`、`full_flow`、`abnormal_exit`。
- 新增 `case_mode.py`，统一规范化和兜底推断用例模式：退出/费用/排名等默认为分支专项，负责人正常沟通和正常愿意配送识别为全流程，开车/无法配送/拒绝继续识别为异常终止。
- SQLite 兼容迁移补充 `case_mode` 列，并对已有默认用例做回填；全流程用例的 `max_turns` 小于 5 时回填到 5。
- Excel 导入和 sample seed 已给默认飞毛腿、课程直播用例标注模式：飞毛腿“正常愿意配送”和课程“负责人正常沟通”为 `full_flow`，课程“商家说在开车”和飞毛腿“不想配送”为 `abnormal_exit`，退出/排名/天气/费用等专项问题为 `branch`。
- `CaseRegistry` 和 `/api/cases` 创建/更新链路会规范化 `case_mode`，AI 草稿保存时也会带上该字段。
- `evaluation_service.py` 不再只按 `max_turns` 停止；新增规划轮数和 stop decision，结合 `case_mode`、`should_continue`、异常终止信号、全流程关键步骤完成情况判断是否继续。
- `full_flow` 用例会尽量覆盖关键步骤；课程直播负责人场景会推进身份确认、发布页升级、标准/低延迟区别、发布方式和配置路径后再结束。
- `branch` 用例在当前分支目标完成后可提前结束；飞毛腿退出类用例 2-3 轮结束视为正常。
- `abnormal_exit` 用例遇到开车、不方便继续、无法配送或拒绝继续时允许 1-2 轮结束。
- `UserSimulatorAgent` 对 `full_flow` 增加继续追问能力：如果模型过早收尾但关键流程未完成，会追问直播区别、配置路径、完成要求或安全提醒。
- 收紧课程直播“已说明标准直播/低延迟区别”的识别，只有实际包含 5-10 秒、1-2 秒或等价差异说明才算完成，避免只出现“低延迟直播”就误判覆盖。
- `RunConsole.vue` 左侧和对话顶部显示用例模式、最大轮数和当前目标；`CaseEditor.vue` 支持选择用例模式；`CaseList.vue` 在列表和 AI 草稿预览中显示模式。
- `AGENTS.md` 增加长期约束：用例模式、轮次控制规则、关键验证场景，并补充 `case_mode.py` 为核心后端文件。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 对话轮次专项测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_dialogue_deduplication.py -vv`，结果 `8 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `35 passed`。
- 前端构建：`npm run build` 通过；仍只有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning。
- 飞毛腿“询问如何退出飞毛腿”：测试断言为 `branch`，2-3 轮结束，回复包含 App、前一天、Z 点、报名页/取消等退出流程信息。
- 课程直播“负责人正常沟通”：测试断言为 `full_flow`，不会 1-2 轮结束，至少覆盖发布页升级、5-10 秒/1-2 秒直播区别、Web 控制台/第三方系统、直播平台管理或勾选路径。
- 课程直播“商家说在开车”：测试断言为 `abnormal_exit`，1-2 轮结束，回复包含稍后再打，用户状态为 accepted/closed。

### 6. 当前仍存在的问题

- 本次未启动浏览器逐项截图验收；前端已通过生产构建验证。
- `case_mode` 已作为正式列落库；如果用户历史数据库里有同名同初始消息的旧重复用例，`get_or_create_case` 会复用最早记录，不会自动覆盖用户手动改过的模式。
- `full_flow` 的关键步骤目前主要覆盖默认飞毛腿和课程直播任务；后续新增通用任务时仍需要继续扩展关键步骤判断。

### 7. 下一步优先级

- 用浏览器在开始评测页人工跑三条代表用例：飞毛腿退出、课程负责人正常沟通、课程开车，确认页面上的模式、轮数、当前目标和实际对话一致。
- 如继续增强，应优先把 `case_mode` 展示到报告详情或消息 detail 折叠区，方便解释“为什么这条用例 2 轮结束是合理的”。

AGENTS.md 更新说明：
- 本次新增 `case_mode`、对话结束策略和重要验证场景，属于长期评测规则/重要验证标准变化，因此已更新 `AGENTS.md`。

## 〇、2026-05-18 本次修改：修复当前用例规则激活逻辑

### 1. 本次修改日期

2026-05-18

### 2. 本次修改目标

只修复规则激活逻辑，不新增 RAG、不新增知识库、不修改模型接入。解决规则评分把任务全量规则套到每个用例的问题，使当前轮只评分 `global_rules + case_rules + stage_rules + triggered_rules`，后续流程规则和未触发规则不进入当前扣分。

### 3. 修改文件列表

- `backend/app/services/rule_judge.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/schemas/report.py`
- `backend/app/api/reports.py`
- `backend/tests/test_active_rules.py`
- `docs/当前进度.md`

### 4. 已完成内容

- `rule_judge.py` 输出补充 `untriggered_rules`，并继续保留旧的 `not_applicable_rules` 作为兼容别名。
- `score_turn(...)` 和 `evaluate_conversation(...)` 均返回 `active_rules`、`matched_rules`、`failed_rules`、`pending_rules`、`untriggered_rules`、`current_stage` 和 `active_rules_explanation`。
- `active_rules` 内部按 `global_rules`、`case_rules`、`stage_rules`、`triggered_rules`、`pending_rules`、`untriggered_rules` 分类。
- 规则触发上下文改为基于用例名称、用户画像、初始消息、触发条件、用户实际发言和用户状态，不再用 `expected_goals` 等目标描述触发分支规则，避免把用例期望里的后续节点提前判为当前失败。
- 飞毛腿任务触发规则收敛为：退出/取消/飞毛腿报名、下雨/天气/危险、排名/名额/报不上、不跑/没完成/影响四类分支；未触发的天气、安全、排名、奖励等规则不会进入退出用例的 `failed_rules`。
- 课程直播任务触发规则收敛为：开车、优惠券/折扣/便宜、企业微信/联系方式、标准直播和低延迟区别、Web/第三方/配置路径五类分支；负责人正常沟通首轮不会把开车、优惠券、企业微信、配置路径或结束确认计入失败规则。
- `evaluation_service.py` 将每轮 `untriggered_rules` 写入 message detail。
- 报告 API 增加 `untriggered_rules` 输出；该字段从已持久化的 `active_rules.untriggered_rules` 或历史 `not_applicable_rules` 派生，不新增数据库列。
- `test_active_rules.py` 补充回归断言，覆盖飞毛腿退出用例和课程直播负责人首轮不误扣未触发规则。

### 5. 本次验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 定向规则测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py -vv`，结果 `5 passed`。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `33 passed`。
- 飞毛腿“询问如何退出飞毛腿”回归断言通过：退出流程、App 取消、提前一天 Z 点前、次日生效进入 active；天气、安全、报名排名、额外奖励、开始配送、单日/多日完成要求不进入当前 active/failed。
- 课程直播“负责人正常沟通”首轮回归断言通过：只激活负责人识别、升级说明、标准直播/低延迟直播和简短自然等当前阶段规则；开车、优惠券、企业微信、配置路径和结束确认不进入当前 failed。

### 6. 当前仍存在的问题

- 本次是后端评分逻辑修复，未使用浏览器逐项检查前端展示效果。
- `pending_rules` 仍用于展示后续流程节点，但不参与当前扣分；前端如需更清楚地区分“待完成”和“未触发”，后续可增加展示文案。
- 历史已生成报告不会重新计算评分；只有新生成的 run/report 会使用本次规则激活逻辑。

### 7. 下一步优先级

- 用浏览器在开始评测页各跑一次飞毛腿退出用例和课程直播负责人正常沟通首轮，确认页面上失败规则与报告页展示一致。
- 如继续优化评分，应优先补充更多触发型分支回归测试，例如课程开车、优惠券、企业微信、第三方系统看不到选项，以及飞毛腿天气和排名场景。

AGENTS.md 更新说明：
- 本次属于既有评分机制下的规则激活 bug 修复，未改变项目定位、核心架构、长期开发规则、启动命令、禁止事项或重要验证标准，因此未更新 `AGENTS.md`。

## 〇、2026-05-18 本次修改：修复 openai_compatible 页面评测结果展示

### 1. 本次修改日期

2026-05-18

### 2. 本次修改目标

修复 `openai_compatible` 在页面点击“开始评测”后无法正常显示真实模型结果的问题。重点对齐 `scripts/test_qwen_api.py` 已验证成功的 DashScope/Qwen 调用方式，并确保后端不再静默回退、前端能拿到 run/messages/report 并展示。

### 3. 修改文件列表

- `backend/app/core/config.py`
- `backend/.env.example`
- `backend/.env`
- `backend/app/services/target_model_client.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/batch_evaluation_service.py`
- `backend/app/api/model_providers.py`
- `backend/app/schemas/run.py`
- `backend/tests/conftest.py`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_runs.py`
- `backend/tests/test_batch_runs.py`
- `frontend/src/api/request.js`
- `frontend/src/pages/RunConsole.vue`
- `README.md`
- `docs/接口说明.md`
- `docs/部署说明.md`
- `docs/当前进度.md`

### 4. 已完成内容

- `TargetModelClient` 的 `openai_compatible` 调用改为使用 OpenAI SDK，与 `scripts/test_qwen_api.py` 保持一致：`api_key=TARGET_MODEL_API_KEY`、`base_url=TARGET_MODEL_BASE_URL`、`model=TARGET_MODEL_NAME`、`timeout=60`，读取 `response.choices[0].message.content`。
- 修复前端传入 `model_name=openai_compatible` 覆盖 `.env` 模型名的问题；`openai_compatible` 现在始终使用后端 `TARGET_MODEL_NAME`，本机验证为 `qwen-plus`。
- 新增 `TARGET_MODEL_ALLOW_FALLBACK=false` 配置；真实 API 调试阶段 openai 调用失败会返回显式错误，不静默回退 mock。
- 真实模型调用前记录非敏感日志：provider、API Key 是否配置、Base URL 是否配置、model_name、timeout；调用成功后记录 `provider_call_success=true` 和 `reply_length`，不打印 API Key。
- `/api/model-providers/test` 对 `openai_compatible` 发起一次真实 OpenAI-compatible 调用，成功返回 `success=true`、`ok=true`、`model_name` 和 `reply_length`。
- `/api/runs/start` 成功返回 `success=true`、`run_id`、`report_id`、`provider_requested`、`provider_used`、`fallback_used=false`、`message=evaluation finished`；失败返回 `success=false`、`run_id`、`error_code`、`error_message`。
- `EvaluationService` 继续逐轮保存用户模拟器消息和被测模型回复；真实 openai 回复正常写入 `run_messages` 并进入报告。
- `RunConsole.vue` 不再向后端传错误的 `model_name`；成功后按 `run_id` 拉取 messages、按 `report_id` 拉取 report；失败时显示 `error_message` 且不清空当前任务、用例、provider 选择。
- 前端请求超时从 20 秒调整到 180 秒，避免真实多轮评测还未完成时页面提前超时。
- 前端成功加载后会在控制台打印 `run_id`、`report_id`、`messages_count`、`report_exists`；失败时打印 provider 和错误信息。

### 5. 验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest` 共 33 项全部通过。
- 前端构建：`npm run build` 通过；仍仅保留既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning。
- 真实 provider 测试：`POST http://127.0.0.1:8080/api/model-providers/test` with `provider=openai_compatible` 返回 `success=True`、`ok=True`、`model_name=qwen-plus`、`reply_length=2`。
- 真实评测测试：`POST /api/runs/start` with `model_provider=openai_compatible` 返回 `success=true`、`run_id=1060`、`report_id=1060`、`provider_requested=openai_compatible`、`provider_used=openai_compatible`、`fallback_used=false`、`model_name=qwen-plus`。
- 消息与报告验证：`GET /api/runs/1060/messages` 返回 3 条消息，首条被测模型回复为“你好，我是站长。你飞毛腿合同今天已生效，能开始配送吗？”；`GET /api/reports/1060` 返回报告，总分 `75.28` 且包含 `llm_judge_result`。

### 6. 当前仍存在的问题

- 本次通过真实后端 API、前端构建和前端代码路径验证修复；未使用浏览器自动化截图逐项点击页面。
- 真实模型质量受当前 `TARGET_MODEL_NAME=qwen-plus` 和任务提示词影响，后续可继续优化提示词和任务白名单约束。
- `TARGET_MODEL_ALLOW_FALLBACK=false` 是当前真实 API 调试阶段配置；若要恢复演示兜底，可显式改回 `true` 并复测。

### 7. 下一步建议

- 在浏览器开始评测页选择 `openai_compatible` 跑一次，打开控制台确认 `messages_count > 0` 且 `report_exists=true`。
- 若需要展示更稳定的真实模型结果，可选 1-2 条代表性飞毛腿和课程直播用例做演示前固定复测。

## 〇、2026-05-18 本次修改：最终自检与收尾修复

### 1. 本次修改日期

2026-05-18

### 2. 本次修改目标

对当前 callflow-lab 进行最终自检，覆盖后端启动、Swagger、前端启动、核心页面、AI 能力、用例构建、业务串场、报告字段和工程质量。发现问题后只做收尾级修复，不重建项目、不大改 UI。

### 3. 修改文件列表

- `README.md`
- `docs/接口说明.md`
- `docs/测试说明.md`
- `docs/部署说明.md`
- `docs/当前进度.md`
- `backend/app/api/model_providers.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/user_simulator.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/agents/evaluator_agent.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/target_model_client.py`
- `backend/tests/conftest.py`
- `backend/tests/test_model_providers.py`
- 已删除空的本地运行日志：`backend/uvicorn*.log`、`frontend/vite*.log`

### 4. 已完成内容

- 公开文档收口：`README.md`、接口说明、测试说明、部署说明已替换早期退款/酒店/团购券样例，统一描述为复杂外呼任务评测平台，默认任务为飞毛腿和课程直播。
- 评测链路补齐新用例字段传递：`trigger_conditions`、`expected_final_state`、`user_behavior_type`、`data_source` 已进入 case payload，并被用户模拟器、规则评分和 LLM-as-a-Judge 上下文使用。
- `mock_fallback` 描述补充 `fallback, non-real AI` 标识，前端和接口都能明确它只是兜底，不是真实 AI 能力。
- 修复 `mock_fallback` run 中 `model_name` 可能被 `.env` 的真实模型名污染的问题；现在 mock 评测返回 `model_name=mock_fallback`。
- 对课程直播真实/openai-compatible 回复中的 `5-10秒`、`1-2秒` 做轻量归一化为 `5-10 秒`、`1-2 秒`，避免报告证据和回归测试因格式差异不稳定。
- pytest 运行时固定使用 mock provider，避免本机 `.env` 中真实模型配置导致自动测试调用外部接口、变慢或不稳定；不影响应用实际运行时配置。
- 删除空的本地 uvicorn/vite 日志文件，避免最终工程质量检查命中空文件。

### 5. 验证结果

- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest` 共 31 项全部通过。
- 前端构建：`npm run build` 通过；仅保留既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning。
- 后端运行检查：`http://127.0.0.1:8080/health` 返回 `ok`，`http://127.0.0.1:8080/docs` 返回 HTTP 200。
- 前端运行检查：`http://127.0.0.1:5173` 返回 HTTP 200。
- 页面能力源码检查：Dashboard 含 AI 能力模块；ModelConfig 只展示 `mock_fallback`、`openai_compatible`、`custom_endpoint`；RunConsole 需选择任务、用例和 provider 后点击开始评测；ReportDetail 展示 LLM 评估、规则、证据、扣分原因和建议。
- API 烟测通过：任务数 2；provider 列表为 `mock_fallback/openai_compatible/custom_endpoint`；手动用例新增、编辑、删除成功；AI 生成 6 条草稿并确认保存成功；保存后用例列表无同签名重复项。
- 实际评测烟测通过：飞毛腿和课程直播各启动一次 `mock_fallback` 评测，生成 run/report；飞毛腿 assistant 未命中课程直播和旧客服禁词；课程直播 assistant 未命中飞毛腿和旧客服禁词。
- 报告字段检查通过：报告包含总分、六项指标分、命中规则、失败规则、active rules、LLM 评估、metric explanations、failure cases、evidence messages、suggestions、score formula 和 messages。
- 工程质量扫描：排除 `node_modules/dist/.git/__pycache__/.pytest_cache` 后未发现空文件；`TODO` 仅命中文档/AGENTS 中的规则描述；旧业务关键词只命中禁止词/测试守卫逻辑，未命中公开 README 和三份说明文档。

### 6. 当前仍存在的问题

- 本次未使用真实浏览器截图或逐项点击工具做视觉级验收；当前通过前端构建、dev server HTTP 200、关键页面源码检查和 API 烟测确认不白屏与主链路可用。
- `npm run build` 仍有既有大 chunk 和第三方依赖注释 warning，不影响演示运行。
- 当前真实 `openai_compatible` 的质量验收仍依赖用户本机 `.env` 配置；自动化 pytest 已固定 mock provider，避免把外部 API 稳定性纳入单元测试结果。
- 工作区存在用户/历史未跟踪的 `backend/scripts/test_qwen_api.py` 和对应 `__pycache__`，本次未修改或清理。

### 7. 下一步建议

- 演示前用浏览器手动点一遍：Dashboard、任务页、用例页、模型配置、开始评测、完整报告。
- 如需要展示真实 AI 能力，分别配置 `TARGET_MODEL_*`、`EVALUATOR_*`、`CASE_GENERATOR_*` 后，对同一飞毛腿/课程直播用例各跑一次真实 provider 对比。
- 后续可做前端分包优化，减少 Vite chunk size warning。

## 〇、2026-05-17 本次修改：补齐人工构建测试用例能力

### 1. 本次修改日期

2026-05-17

### 2. 本次修改目标

在当前 callflow-lab 项目基础上补齐“自行构建测试用例”能力，让用户可以手动新增、编辑、删除测试用例，并能在开始评测页面快速新建或编辑当前用例。保持现有 API 路径、评测链路和 UI 风格，不重建项目、不大改 UI。

### 3. 修改文件列表

- `backend/app/models/case.py`
- `backend/app/schemas/case.py`
- `backend/app/core/database.py`
- `backend/app/services/case_generator_service.py`
- `backend/tests/test_cases.py`
- `backend/tests/test_case_generation.py`
- `frontend/src/components/CaseEditor.vue`
- `frontend/src/pages/CaseList.vue`
- `frontend/src/pages/RunConsole.vue`
- `docs/当前进度.md`

### 4. 已完成内容

- 后端确认并保留 `POST /api/cases`、`PUT /api/cases/{case_id}`、`DELETE /api/cases/{case_id}`、`GET /api/cases?task_id=xxx`。
- `EvaluationCase` 补齐 `user_behavior_type` 字段；已有字段包括 `task_id`、`name`、`user_profile`、`initial_message`、`expected_goals`、`required_rules`、`forbidden_rules`、`trigger_conditions`、`expected_final_state`、`difficulty`、`max_turns`、`data_source`。
- SQLite 兼容迁移补充 `user_behavior_type` 默认值，旧数据默认填充为“正常配合”。
- 新增复用组件 `CaseEditor.vue`，用于新增和编辑测试用例，包含所属任务、用例名称、用户画像、初始问题、测试目标、用户行为类型、最大轮数、难度、必须满足规则、禁止触发规则、触发条件和期望结束状态。
- `CaseEditor.vue` 增加必填校验，空字段会提示用户补齐。
- `CaseList.vue` 改为复用 `CaseEditor.vue`，增加编辑按钮、删除按钮，并保留新增用例和 AI 生成用例按钮。
- `RunConsole.vue` 增加“新建用例”和“编辑当前用例”快捷按钮，保存后刷新当前任务用例并选中新保存的用例。
- 开始评测页文案调整为明确“点击开始评测后执行”；实际运行逻辑继续要求选择任务、用例和模型 provider 后点击按钮才调用 `/api/runs/start`。
- AI 生成草稿和保存链路同步带上 `user_behavior_type`，避免 AI 生成用例与人工用例字段不一致。

### 5. 验证结果

- 后端编译验证：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest` 共 30 项全部通过。
- 定向 CRUD 测试：`tests/test_cases.py` 覆盖手动新增飞毛腿用例、编辑用例、按任务列表查询、使用该用例启动评测并生成报告、删除用例。
- AI 生成回归测试：`tests/test_case_generation.py` 通过，确认草稿保存后保留 `data_source=ai_generated` 和 `user_behavior_type`。
- 前端构建验证：`npm run build` 通过；仅保留既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning。
- 本机服务检查：`http://127.0.0.1:8080/health` 返回 `ok`，`http://127.0.0.1:5173` 返回前端 HTML，未出现白屏级构建错误。

### 6. 当前仍存在的问题

- 本次主要通过自动化测试和构建验证确认流程，没有使用真实浏览器逐项点击完成全链路人工验收。
- 删除用例当前会直接删除记录；若未来需要严格保护已生成 run/report 的历史可追溯性，可增加“已被评测引用的用例禁止删除或软删除”策略。
- `CaseEditor.vue` 当前使用多行文本维护数组字段，适合本阶段快速构建；后续可升级为可增删的规则条目编辑器。

### 7. 下一步建议

- 在浏览器中人工复测：新增一个飞毛腿用例、编辑字段、删除用例、再新增后到开始评测页选择并运行。
- 若演示需要保留历史报告完整追溯，下一步可把删除改为软删除或增加引用检查提示。

## 〇、2026-05-17 本次修改：新增 AI 辅助生成测试用例草稿能力

### 1. 本次修改日期

2026-05-17

### 2. 本次修改目标

在不重建项目、不大改 UI 的前提下，为测试用例页面增加“AI 生成用例”能力。系统可根据复杂任务指令生成测试用例草稿，先在前端预览，用户确认后再复用现有 `POST /api/cases` 保存链路，避免绕过用例幂等规则。

### 3. 修改文件列表

- `backend/app/core/config.py`
- `backend/.env.example`
- `backend/app/models/case.py`
- `backend/app/schemas/case.py`
- `backend/app/core/database.py`
- `backend/app/services/case_generator_service.py`
- `backend/app/api/cases.py`
- `backend/tests/test_case_generation.py`
- `backend/tests/test_case_deduplication.py`
- `frontend/src/pages/CaseList.vue`
- `docs/当前进度.md`

### 4. 已完成内容

- 新增 `POST /api/cases/generate`，按 `task_id`、生成数量、难度分布和用户行为类型返回测试用例草稿列表，不直接入库。
- 新增 `CaseGeneratorService`，支持 `mock` 和 `openai_compatible` 两种模式；未配置 `CASE_GENERATOR_API_KEY` 或 `CASE_GENERATOR_BASE_URL` 时自动使用 mock 兜底。
- mock 生成器已覆盖飞毛腿任务的正常愿意配送、不想配送、合同影响、退出飞毛腿、恶劣天气、报名排名、额外奖励、超范围咨询等类型。
- mock 生成器已覆盖课程直播任务的负责人沟通、非负责人转达、商家忙、开车、直播区别、费用差异、优惠券、第三方系统、企业微信、结束前追问等类型。
- 新增配置项：`CASE_GENERATOR_PROVIDER`、`CASE_GENERATOR_API_KEY`、`CASE_GENERATOR_BASE_URL`、`CASE_GENERATOR_MODEL`。
- 用例模型增加 `trigger_conditions`、`expected_final_state`、`data_source` 字段，并补充 SQLite 兼容迁移；AI 草稿保存后可保留 `data_source=ai_generated`。
- `CaseList.vue` 增加“AI 生成用例”按钮、生成参数弹窗、草稿预览表格、确认保存和取消按钮。
- 确认保存时逐条调用现有 `POST /api/cases`，继续复用 `task_id + name + initial_message` 幂等判重，不直接批量绕库插入。

### 5. 验证结果

- 后端编译验证：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端完整测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest` 共 29 项全部通过。
- 新增定向测试：`tests/test_case_generation.py` 验证生成接口返回 6 条草稿、不会直接入库、草稿可通过 `POST /api/cases` 幂等保存。
- 既有去重测试：`tests/test_case_deduplication.py` 通过，确认非人工重复用例仍可清理，人工用例保护逻辑保留。
- 前端构建验证：`npm run build` 通过；仍仅保留既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning。

### 6. 当前仍存在的问题

- 当前未配置真实 `CASE_GENERATOR_API_KEY` / `CASE_GENERATOR_BASE_URL`，实际验证使用的是 mock case generator 兜底。
- `openai_compatible` 生成器已接入 `/chat/completions` 协议，但真实模型输出的 JSON 合规性和用例质量仍需接入真实模型后复测。
- 草稿确认保存当前为逐条调用 `POST /api/cases`，适合本阶段保持幂等和简单可靠；后续如生成量很大，可再增加批量确认保存接口。

### 7. 下一步建议

- 配置真实 `CASE_GENERATOR_PROVIDER=openai_compatible` 后，用飞毛腿和课程直播各生成一批草稿，对比 mock 与真实模型的覆盖多样性。
- 在浏览器测试用例页复测：选择飞毛腿任务，生成 6 条草稿，确认保存后列表出现 AI 生成用例，重复确认不会出现同签名重复用例。

## 〇、2026-05-17 本次修改：增强 LLM-as-a-Judge 评估器与报告证据链

### 1. 本次修改日期

2026-05-17

### 2. 本次修改目标

在保留规则评分的基础上，完善评估器 Agent，使其支持 `mock` 兜底和 `openai_compatible` 真实大模型评估；报告需要合并规则评分与 LLM 语义评估结果，展示证据轮次、对话片段、扣分原因和优化建议。

### 3. 修改文件列表

- `backend/app/services/agents/evaluator_agent.py`
- `backend/app/services/report_service.py`
- `backend/app/api/reports.py`
- `docs/当前进度.md`

### 4. 已完成内容

- 重写 `EvaluatorAgent`，保留 `mock` 本地兜底评估，并支持 `openai_compatible` 通过 `/chat/completions` 调用真实大模型作为 LLM-as-a-Judge。
- `openai_compatible` 未配置 `EVALUATOR_API_KEY` 或 `EVALUATOR_BASE_URL` 时自动回退 mock evaluator，并在结果中标记 `provider_requested`、`fallback_used` 和 `fallback_reason`。
- LLM-as-a-Judge prompt 明确要求：只评价被测对话模型，不评价用户模拟器、不评价系统代码；必须输出六项指标分、扣分原因、对话证据和优化建议；只能输出合法 JSON。
- `ReportService` 合并规则评分与 LLM 评估分，使用固定权重公式计算总分，并把 LLM evidence 合并进 `evidence_messages`、`failure_cases` 和 `metric_explanations`。
- `reports.py` 修正报告序列化兜底文案，避免空字段时出现旧编码损坏文本。
- `.env.example` 已包含本阶段要求的 `EVALUATOR_PROVIDER`、`EVALUATOR_API_KEY`、`EVALUATOR_BASE_URL`、`EVALUATOR_MODEL` 配置项，无需重复新增。

### 5. 验证结果

- 后端编译验证：`python -m compileall app` 通过。
- 后端完整测试：`pytest` 共 27 项全部通过。
- 前端构建验证：`npm run build` 通过；仅保留 Vite/Rollup 体积和第三方注释警告，未阻塞构建。
- 评估器兜底验证：设置 `EVALUATOR_PROVIDER=openai_compatible` 且不配置 API Key/Base URL 时，返回 `provider=mock`、`provider_requested=openai_compatible`、`fallback_used=True`。
- 报告 API 验证：启动一次飞毛腿任务评测后，`GET /api/reports/{report_id}` 返回 `total_score`、六项指标分、`avg_latency_ms`、`matched_rules`、`failed_rules`、`llm_judge_result`、`evidence_messages`、`metric_explanations`、`failure_cases`、`suggestions`、`score_formula`。
- 报告字段抽检：`llm_judge_result` 包含六项指标、`overall_reason`、`evidence`、`suggestions`、`provider`、`provider_requested`、`fallback_used`；`evidence_messages=3`、`metric_explanations=6`、`failure_cases=2`、`suggestions=4`。

### 6. 当前仍存在的问题

- 当前未配置真实 `EVALUATOR_API_KEY` / `EVALUATOR_BASE_URL`，实际验证使用的是 mock evaluator 兜底；真实 LLM-as-a-Judge 的稳定性、成本和输出 JSON 合规性需要接入真实模型后继续复测。
- LLM 评估结果已进入报告证据链，但最终分数仍采用规则分与 LLM 分加权融合；后续可根据比赛演示需要调整 LLM 分权重或展示“规则分 / LLM 分 / 综合分”。

### 7. 下一步建议

- 配置一个真实 `openai_compatible` 评估模型，使用同一批报告对比 mock evaluator 与真实 LLM-as-a-Judge 的证据质量。
- 针对飞毛腿和课程直播各跑一组批量评测，检查 LLM evidence 是否能稳定引用正确轮次和模型原话。

## 〇、2026-05-17 本次修改：简化被测模型 provider 与统一 AI 能力定位

### 1. 本次修改日期

2026-05-17

### 2. 本次修改目标

弱化本地 mock 对比概念，将被测模型 provider 收敛为 `mock_fallback`、`openai_compatible`、`custom_endpoint` 三类；明确系统真正的 AI 能力来自用户模拟器 Agent、LLM-as-a-Judge、真实被测模型接入、AI 辅助生成测试用例和可解释评测报告。

### 3. 修改文件列表

- `backend/app/services/target_model_client.py`
- `backend/app/api/model_providers.py`
- `backend/app/core/config.py`
- `backend/app/core/database.py`
- `backend/app/models/run.py`
- `backend/app/models/batch_run.py`
- `backend/app/schemas/run.py`
- `backend/app/schemas/batch_run.py`
- `backend/app/services/batch_evaluation_service.py`
- `backend/.env.example`
- `backend/tests/test_model_providers.py`
- `backend/tests/test_batch_runs.py`
- `frontend/src/pages/RunConsole.vue`
- `frontend/src/pages/BatchEvaluation.vue`
- `frontend/src/pages/ModelConfig.vue`
- `frontend/src/pages/Dashboard.vue`
- `docs/系统总体设计.md`
- `docs/产品说明文档.md`
- `docs/比赛提交说明.md`
- `AGENTS.md`

### 4. 已完成内容

- 新增并统一使用 `normalize_target_provider(...)`，将历史 `mock_baseline`、`mock_strong` 兼容映射为 `mock_fallback`。
- 后端默认 `TARGET_MODEL_PROVIDER`、run/batch schema 默认值、批量评测默认 provider 均改为 `mock_fallback`。
- `/api/model-providers` 只返回 `mock_fallback`、`openai_compatible`、`custom_endpoint`，且不返回 API Key 明文。
- `openai_compatible` 或 `custom_endpoint` 未配置时，评测生成链路会自动使用任务白名单 fallback 回复兜底，并在消息 metadata 中保留 `target_fallback_used`。
- RunConsole、BatchEvaluation 和 ModelConfig 页面不再突出 `mock_baseline` / `mock_strong`。
- Dashboard 增加“AI 能力模块”卡片，展示用户模拟器 Agent、LLM-as-a-Judge、真实被测模型接入、AI 辅助生成测试用例和可解释评测报告，未把 `mock_fallback` 写成 AI 能力亮点。
- 系统总体设计、产品说明和比赛提交说明同步明确：本系统不训练对话模型，`mock_fallback` 只是演示兜底。

### 5. 验证结果

- 后端编译验证：`python -m compileall app` 通过。
- 后端测试验证：`pytest` 共 27 项全部通过。
- 前端构建验证：`npm run build` 通过；仅保留 Vite/Rollup 体积和第三方注释警告，未阻塞构建。
- Provider API 抽检：`GET /api/model-providers` 返回 `mock_fallback`、`openai_compatible`、`custom_endpoint` 三项。
- 旧 provider 兼容抽检：`POST /api/model-providers/test` with `mock_baseline` 返回 `provider=mock_fallback` 且 `ok=true`。
- 未配置真实模型兜底抽检：`POST /api/model-providers/test` with `openai_compatible` 返回 `ok=false`，提示评测时会回退到 `mock_fallback`。
- 单次评测链路抽检：使用 `openai_compatible` 启动飞毛腿任务，在未配置 API Key 时消息 metadata 中 `target_fallback_used=true`，首轮回复为“飞毛腿合同已生效，可以开始配送。”，未出现课程直播串场。
- 课程直播链路抽检：使用 `mock_fallback` 启动课程直播任务，首轮回复为“我们直播发布页新增两个选项。”，未出现飞毛腿串场。
- 当前本机已有后端 `127.0.0.1:8080` 和前端 `127.0.0.1:5173` 服务在监听；实时接口 `GET /api/model-providers` 返回三项 provider，前端入口返回 HTTP 200。

### 6. 当前仍存在的问题

- `mock_fallback` 只能保证演示链路跑通，不能代表真实 AI 回复质量。
- 若要体现真实被测模型能力，需要在 `.env` 配置 `TARGET_MODEL_API_KEY`、`TARGET_MODEL_BASE_URL`、`TARGET_MODEL_NAME` 或 `TARGET_MODEL_ENDPOINT` 后复测。
- 历史 run/report 中可能仍保留旧 provider 名称，展示层和新调用链已做兼容映射，但历史记录本身未批量迁移。

### 7. 下一步建议

- 配置一个真实 `openai_compatible` 或 `custom_endpoint` provider，重新验证飞毛腿和课程直播任务的跨场景防串场效果。
- 后续如需要多模型对比，应围绕真实外部 provider 或模型版本进行，不再使用本地 mock 强弱对比作为能力展示。

## 一、项目当前状态

当前项目为 callflow-lab，定位为“复杂外呼任务对话模型自动评测平台”。

系统目前已有基础前后端页面和接口，包括：
- 数据大屏
- 评测任务
- 测试用例
- 开始评测
- 多轮对话记录
- 评分摘要
- 完整报告页

当前默认评测任务来自脱敏 Excel 指令数据，主要包括：
1. 飞毛腿骑手合同生效外呼评测
2. 课程直播产品升级外呼评测

当前后端已开始从旧 mock 回复向“可接入被测模型的评测平台”重构，评测流程应统一通过 `target_model_client.py` 调用被测模型。

## 二、已完成内容

- 已搭建 FastAPI 后端。
- 已搭建 Vue3 + Vite 前端。
- 已有基础 Dashboard 页面。
- 已有任务、用例、评测运行、报告等基础页面。
- 已接入 Excel 脱敏任务数据导入能力。
- 已导入或支持导入飞毛腿骑手外呼、课程直播产品升级两类脱敏任务。
- 已为 Excel 任务自动生成多轮测试用例。
- 已升级用户模拟器为被外呼对象模拟器 Agent，能根据任务指令、用户画像、对话历史和被测模型上一轮回复动态生成发言。
- 已实现规则评分、报告生成、失败案例、指标解释、证据轮次等基础可解释报告字段。
- 已升级为“规则评分 + LLM 评估器”的混合评测体系，报告围绕被测对话模型在复杂任务指令下的表现生成。
- 已完成第一阶段被测模型接入层重构：
  - 新增 `backend/app/services/target_model_client.py`。
  - 当前 provider 已简化为 `mock_fallback`、`openai_compatible`、`custom_endpoint`。
  - 历史 `mock_baseline`、`mock_strong` 会兼容映射为 `mock_fallback`。
  - `openai_compatible` 和 `custom_endpoint` 无配置或异常时回退 `mock_fallback`。
  - `evaluation_service.py` 已改为统一调用 `TargetModelClient`。
  - `target_bot.py` 已改为兼容包装器，不再保留旧客服场景模板。
  - run 记录中已增加 `model_provider`、`model_name`。
- 已清理当前评测链路中旧外卖退款、酒店变更、团购券核销等业务 mock 关键词。
- 已验证当前两个任务的模型回复没有明显业务串场。

## 三、当前严重问题

当前需要重点关注的问题：

1. 早期旧业务 mock 可能仍然残留在非核心路径或历史文档中。
2. 后续新增任务时，需要继续防止飞毛腿任务、课程直播任务和其他任务之间回复串场。
3. `evaluation_service.py` 已改为调用 `target_model_client.py`，后续修改仍需持续确认这一调用链不被绕开。
4. `target_bot.py` 如果仍被引用，必须确认它只作为兼容包装器，不再生成旧回复。
5. 多轮重复回复问题已增加状态机和去重机制，但后续接入真实外部模型后仍需要继续复测。
6. 用户模拟器 Agent、LLM-as-a-Judge、可解释报告、批量评测等能力还可以继续增强。
7. 当前真实外部被测模型接口仅预留配置，默认仍使用 `mock_fallback` 保证演示稳定；`mock_fallback` 不作为系统 AI 能力亮点。

## 四、当前优先级

当前优先级如下：

1. 修复并持续防止不同任务之间的回复串场问题。
2. 清理旧业务 mock。
3. 确认被测模型回复统一走 `target_model_client.py`。
4. 确认飞毛腿任务和课程直播任务的 `task_type` 识别正确。
5. 增强用户模拟器 Agent。
6. 增强规则评分和 LLM-as-a-Judge。
7. 增强可解释报告。
8. 视时间增加批量评测能力和模型配置页能力。

## 五、验证用例

### 验证 1：飞毛腿任务

任务：
飞毛腿骑手合同生效外呼评测

用例：
询问合同影响

用户发言：
如果我今天没完成 X 单会怎么样？

期望模型回复应围绕：
- X 单
- 合同
- 派单
- 影响
- 完成要求

禁止出现：
- 退款
- 订单号
- 手机号后四位
- 商家出餐时间
- 平台超时规则
- 核销
- 酒店
- 标准直播
- 低延迟直播

### 验证 2：飞毛腿天气场景

任务：
飞毛腿骑手合同生效外呼评测

用例：
抱怨恶劣天气

用户发言：
今天下雨这么大，怎么还让我跑？

期望模型回复应围绕：
- 安全第一
- 雨天
- 能跑再接单
- 保住资格
- 记录情况

禁止出现旧客服退款相关内容。

### 验证 3：课程直播任务

任务：
课程直播产品升级外呼评测

用例：
非负责人转达

用户发言：
我不是负责人，我只是前台。

期望模型回复应围绕：
- 负责人
- 转达
- 直播
- 低延迟
- 发布页

禁止出现：
- 飞毛腿
- 骑手
- 配送
- 派单
- X 单
- Y 单
- 退款
- 订单号
- 核销
- 酒店

## 六、最近一次修改

### 2026-05-17：修复测试用例重复生成与展示问题

本次修改日期：
2026-05-17

本次修改目标：
修复测试用例列表和开始评测用例下拉框中大量重复用例的问题，保证后端启动初始化、Excel 导入和 sample seed 在重复执行时具备幂等性。核心判重条件为 `task_id + name + initial_message`。

修改文件：
- `backend/app/services/case_registry.py`
- `backend/app/seed/sample_data.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/api/cases.py`
- `backend/app/services/batch_evaluation_service.py`
- `backend/tests/test_case_deduplication.py`
- `backend/tests/test_active_rules.py`
- `frontend/src/pages/CaseList.vue`
- `frontend/src/pages/RunConsole.vue`
- `docs/当前进度.md`
- `AGENTS.md`

已完成内容：
- 新增 `case_registry.py`，统一提供 `case_exists`、`existing_case`、`get_or_create_case`、`unique_cases`、`deduplicate_cases` 等用例幂等和清理工具。
- `sample_data.py` 和 `import_instruction_excel.py` 在插入任务时优先按 `name + task_type` 复用已有任务；插入用例时按 `task_id + name + initial_message` 复用已有用例，不再无条件插入。
- `POST /api/cases` 改为幂等创建：同一任务下同名同初始问题的用例已存在时直接返回已有记录，不新增重复记录。
- `PUT /api/cases/{case_id}` 增加重复签名检查，避免把已有用例更新成重复项。
- `GET /api/cases` 和 `GET /api/cases?task_id=...` 返回前按同一签名去重，避免历史重复数据继续显示到前端。
- 新增 `POST /api/cases/deduplicate` 手动清理接口；保留最早创建的同签名用例，删除后创建的重复项，并在删除前把 run/report/batch item 的 `case_id` 引用迁移到保留项。
- 批量评测展开用例时也使用 `unique_cases(...)`，避免历史重复用例导致批量运行重复。
- `CaseList.vue` 和 `RunConsole.vue` 增加前端展示兜底去重，同一任务下 `name + initial_message` 相同只展示一条。
- 新增 pytest 覆盖重复创建不新增、重复 seed 不增加数量、手动用例不会被 seed 删除、手动清理接口能删除精确重复记录。

验证结果：
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `26 passed`。
- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_case_deduplication.py`，结果 `4 passed`。
- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning，不影响本次功能验证。
- 模拟后端启动初始化 3 次：连续执行 `create_db_and_tables()` + `seed_sample_data()` 三轮后，物理用例数量从 `19` 保持为 `19`，未继续增长。
- `GET /api/cases?task_id=...` 展示口径验证：飞毛腿任务返回 7 条且最大同签名重复数为 1；课程直播任务返回 8 条且最大同签名重复数为 1。
- 截图中关注用例展示验证：`询问如何退出飞毛腿`、`负责人正常沟通`、`商家说在开车` 在对应任务下均只出现 1 条。

当前仍存在的问题：
- 项目当前 `EvaluationCase` 还没有 `data_source` 字段，因此无法区分历史物理重复记录是否由用户手动创建；清理接口不会在启动时自动执行，避免误删。
- `POST /api/cases/deduplicate` 当前按精确签名清理重复项；如果历史数据只是语义重复但名称或初始问题不同，不会被自动合并。
- 前端已做展示兜底，但真实数据库中的历史重复项需要手动调用清理接口才会物理删除。

下一步建议：
- 如需物理清理当前历史重复数据，手动调用 `POST /api/cases/deduplicate`；也可以带 `task_id` 参数只清理某个任务。
- 后续若需要更强的人工用例保护，可给 `EvaluationCase` 增加 `data_source` 字段，将新建用例标记为 `manual`，导入和生成用例标记为 `excel_generated` 或 `auto_generated`。
- 在导入新的 Excel 任务前，先用 `/api/cases` 检查同签名重复数，确认幂等逻辑仍稳定。

AGENTS.md 更新说明：
- 本次固化了后端启动初始化、Excel 导入、sample seed 和测试用例创建的长期幂等性规则，并新增手动历史重复清理接口，属于重要验证标准变化，因此已更新 `AGENTS.md`。

### 2026-05-16：课程直播评分规则改为流程阶段与用户意图触发

本次修改日期：
2026-05-16

本次修改目标：
修复课程直播产品升级外呼任务中“负责人正常沟通”等用例被整条任务流程提前扣分的问题。评分规则从任务级全量检查调整为“全局硬约束 + 当前流程阶段 + 用户意图触发 + 待完成规则”的激活机制，避免第一轮直接检查费用、企业微信、配置路径和结束确认等后续节点。

修改文件：
- `backend/app/services/rule_judge.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/models/report.py`
- `backend/app/schemas/report.py`
- `backend/app/services/report_service.py`
- `backend/app/api/reports.py`
- `backend/app/core/database.py`
- `backend/tests/test_active_rules.py`
- `frontend/src/components/ConversationTimeline.vue`
- `frontend/src/pages/RunConsole.vue`
- `frontend/src/pages/ReportDetail.vue`
- `docs/当前进度.md`
- `AGENTS.md`

已完成内容：
- `rule_judge.py` 新增 `get_conversation_stage(...)`，课程直播任务支持 `identity_check`、`upgrade_intro`、`awareness_check`、`publish_method_check`、`configuration_guidance`、`fee_check`、`enterprise_wechat`、`closing` 等阶段。
- `rule_judge.py` 输出新增 `active_rules`、`pending_rules`、`not_applicable_rules`、`current_stage`、`active_rules_explanation`。
- 课程直播首轮“我是负责人，你说吧。”会进入 `upgrade_intro` 阶段，只激活负责人识别、升级说明、标准直播/低延迟直播和简短自然等当前阶段规则。
- 费用差异、企业微信添加、结束确认、Web 控制台 / 第三方系统路径等后续流程节点进入 `pending_rules`，不进入当前失败规则。
- 当用户追问“区别和在哪里配置”时，才激活标准/低延迟区别、发布方式判断和配置路径相关规则。
- `evaluation_service.py` 将每轮 active/pending/current_stage 写入 message detail，便于前端逐轮展示。
- 报告模型、schema、API 和数据库补列逻辑支持 `pending_rules` 与 `current_stage`。
- `RunConsole.vue`、`ConversationTimeline.vue`、`ReportDetail.vue` 增加待完成规则和当前阶段展示，不把 pending rules 放入失败规则。
- 新增 pytest 覆盖飞毛腿退出规则不误扣、课程直播负责人首轮不误扣后续流程、课程直播追问配置后才激活路径规则，以及实际报告字段。

验证结果：
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `22 passed`。
- 定向测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest tests\test_active_rules.py`，结果 `5 passed`。
- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning，不影响本次功能验证。
- 课程直播“负责人正常沟通”首轮验证：`current_stage=upgrade_intro`，active rules 包含“识别负责人、进入升级说明、新增标准直播和低延迟直播、保持简短自然”；失败/命中规则中不再出现“费用差异、企业微信添加逻辑、结束前确认是否还有问题、Web 控制台 / 第三方系统给出不同引导”。
- 课程直播第二轮“你直接说区别和在哪里配置”验证：才激活“标准直播费用低且延迟约 5-10 秒、低延迟直播延迟约 1-2 秒且互动更流畅、询问或判断当前发布方式、说明配置路径”等规则。

当前仍存在的问题：
- 最终整场报告当前以最后阶段和已触发问题为主要评分对象；如后续需要严格评估整场流程覆盖，可在对话结束后额外生成“最终流程覆盖检查”，但不能影响中间轮即时评分。
- 当前流程阶段判断仍是规则型关键词识别，默认覆盖课程直播脱敏任务；新增其他流程型外呼任务时需要扩展阶段定义。
- 前端已展示待完成规则，但未新增复杂的折叠交互或多阶段时间线，保持本轮“不大改 UI”的约束。

下一步建议：
- 在浏览器中用课程直播“负责人正常沟通”手动跑一次，确认首轮右侧失败规则不再出现后续节点。
- 继续复测课程直播“开车”“非负责人”“优惠券”“第三方系统看不到选项”等分支，确认触发规则只在用户表达相关意图后激活。
- 后续如果比赛演示强调完整流程覆盖，可新增最终报告级 flow coverage 区块，与逐轮 active rules 分开展示。

AGENTS.md 更新说明：
- 本次改变了长期评分规则设计和报告必备字段，属于重要验证标准与核心评分机制变化，因此已更新 `AGENTS.md`，固化 active/pending/not_applicable 规则和课程直播阶段约束。

### 2026-05-16：第六阶段模型配置页面与比赛提交文档补充

本次修改日期：
2026-05-16

本次修改目标：
继续第六阶段改造，在不重建项目、不大改 UI 的前提下，补充模型配置页面和比赛提交文档，使系统更符合比赛评审中的完整性、应用效果和商业价值要求。

修改文件：
- `backend/app/api/model_providers.py`
- `backend/main.py`
- `backend/tests/test_model_providers.py`
- `frontend/src/pages/ModelConfig.vue`
- `frontend/src/router/index.js`
- `frontend/src/App.vue`
- `docs/系统总体设计.md`
- `docs/比赛提交说明.md`
- `docs/产品说明文档.md`
- `docs/当前进度.md`
- `AGENTS.md`

已完成内容：
- 新增模型 provider 后端接口：`GET /api/model-providers` 和 `POST /api/model-providers/test`。
- provider 列表默认包含 `mock_baseline`、`mock_strong`、`openai_compatible`、`custom_endpoint`，并返回 provider 类型、启用状态、是否当前默认、Base URL、Model Name、Endpoint、API Key 是否已配置和说明文字。
- 后端接口不返回 API Key 明文字段；真实密钥仍只从后端 `.env` 读取。
- 新增 `frontend/src/pages/ModelConfig.vue`，展示 provider 名称、类型、当前是否启用、Base URL、Model Name、Endpoint、测试连接按钮和说明文字。
- 前端侧边栏新增“模型配置”入口，路由为 `/model-config`。
- `mock_baseline` 和 `mock_strong` 的测试连接返回可用；`openai_compatible` 和 `custom_endpoint` 会根据后端环境变量完整性给出配置提示。
- 重写 `docs/系统总体设计.md`，补充当前平台定位、总体架构、核心流程、Agent/LLM 边界、批量评测、模型 provider 和商业价值。
- 重写 `docs/比赛提交说明.md`，说明比赛要求映射、核心功能、演示流程、深度学习和 Agent 使用边界、创新点和商业价值。
- 新增 `docs/产品说明文档.md`，面向评审和业务方说明产品定位、目标用户、核心能力、典型场景、商业价值和当前边界。
- `AGENTS.md` 补充模型 provider 配置约束、后端 API、前端页面说明和基本验证标准。

验证结果：
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `17 passed`。
- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning，不影响本次功能验证。
- 文档自检：`docs/系统总体设计.md`、`docs/比赛提交说明.md`、`docs/产品说明文档.md` 均非空，长度分别为 7983、7165、6215 字节。
- 文档关键词自检：三份目标文档未命中“由 AI 生成”、`TODO`、“占位符”、`placeholder`。
- 接口验证：`GET http://127.0.0.1:8080/api/model-providers` 返回 200，返回 4 个 provider，且不包含 `api_key` 明文字段。
- 连接测试验证：`POST /api/model-providers/test` with `mock_baseline` 返回 `ok=true`。
- 前端路由验证：`http://127.0.0.1:5173/model-config` 返回 200。

当前仍存在的问题：
- 模型配置页当前读取 `.env` 和默认 provider 信息，暂不做前端持久化保存；这符合本阶段“不展示 API Key 明文”的安全要求。
- `openai_compatible` 和 `custom_endpoint` 的连接测试当前以配置完整性检查为主，未在无密钥情况下主动发起外部网络调用。
- 文档已覆盖当前功能，但后续若新增异步批量任务、报告导出或人工复核，需要继续同步更新。

下一步建议：
- 在浏览器中打开模型配置页，确认四类 provider 的展示状态和测试按钮交互。
- 后续如进入更完整的产品化阶段，可新增后端加密存储配置或仅管理员可见的模型配置管理。
- 比赛提交前，按 `docs/比赛提交说明.md` 的演示建议完整走一遍数据大屏、任务详情、开始评测、报告页、批量评测和模型配置页。

AGENTS.md 更新说明：
- 本次新增模型 provider API、模型配置页面和相关安全约束，属于长期平台能力和重要验证标准变化，因此已更新 `AGENTS.md`。

### 2026-05-16：第五阶段批量评测能力改造

本次修改日期：
2026-05-16

本次修改目标：
继续第五阶段改造，在不重建项目、不大改 UI 的前提下，新增批量评测能力，使系统可以按任务、用例范围、模型 provider 和重复次数生成多个 run/report，并输出批量 summary。该能力不是强制 A/B 测试，默认单模型批量评测可用，多模型仅作为可选对比统计。

修改文件：
- `backend/app/models/batch_run.py`
- `backend/app/models/__init__.py`
- `backend/app/schemas/batch_run.py`
- `backend/app/services/batch_evaluation_service.py`
- `backend/app/api/batch_runs.py`
- `backend/app/api/dashboard.py`
- `backend/app/core/database.py`
- `backend/main.py`
- `backend/tests/test_batch_runs.py`
- `frontend/src/pages/BatchEvaluation.vue`
- `frontend/src/router/index.js`
- `frontend/src/App.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/styles.css`
- `docs/当前进度.md`
- `AGENTS.md`

已完成内容：
- 新增批量评测持久化模型 `EvaluationBatchRun` 和 `EvaluationBatchRunItem`，记录 batch 配置、每个子运行、run/report 关联、得分、延迟、状态和错误信息。
- 新增批量评测 API：`POST /api/batch-runs/start`、`GET /api/batch-runs/{batch_id}`、`GET /api/batch-runs/{batch_id}/summary`。
- 新增 `BatchEvaluationService`，按 `task_ids`、`case_ids`、`model_providers`、`repeat_times` 展开执行；每次子运行复用 `EvaluationService.start_evaluation(...)`，不绕过现有用户模拟器、被测模型、规则评分、LLM 评估和报告生成链路。
- `case_ids` 为空时会自动使用所选任务下全部用例；`model_providers` 支持单模型默认 `mock_baseline`，也支持多个 provider 生成 `model_score_summary` 和 `best_model_provider`。
- 批量 summary 已包含 `batch_id`、`total_runs`、`finished_runs`、`failed_runs`、`average_score`、`average_latency_ms`、`pass_rate`、`task_score_summary`、`metric_score_summary`、`failed_rule_top5`、`report_list`。
- 新增 `frontend/src/pages/BatchEvaluation.vue`：支持选择任务、测试用例范围、模型 provider、重复次数，展示运行进度、平均分、通过率、失败规则 TOP5、报告列表；多 provider 时展示模型 provider 对比柱状图，但页面不写“A/B 测试”。
- 前端侧边栏新增“批量评测”入口，路由为 `/batch-runs`。
- Dashboard 增加批量评测入口、批量次数、平均通过率、最近批量评测结果；保留常见失败规则 TOP5。
- 全局 metrics 网格调整为自适应列数，避免新增 Dashboard 指标后挤压布局。
- 新增 pytest 覆盖单模型批量评测和多模型批量统计。

验证结果：
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `15 passed`。
- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning，不影响本次功能验证。
- 本地数据库当前脱敏任务实际 ID 为 4、5；按用户示例传 `task_ids: [1, 2]` 时接口正确返回 `tasks not found: [1, 2]`，未重置数据库。
- 单模型批量实际接口验证：使用当前实际任务 ID `4,5`，`model_providers=["mock_baseline"]`、`repeat_times=1`，生成 `batch_id=5`，`total_runs=15`、`finished_runs=15`、`failed_runs=0`、`average_score=61.15`、`average_latency_ms=153.55`、`pass_rate=53.33`，`failed_rule_top5` 返回 5 项，`report_list` 返回 15 条。
- 多模型批量实际接口验证：使用当前实际任务 ID `4,5`，`model_providers=["mock_baseline","mock_strong"]`、`repeat_times=1`，生成 `batch_id=6`，`total_runs=30`、`finished_runs=30`、`failed_runs=0`，返回 2 项 `model_score_summary`，`best_model_provider=mock_baseline`。
- 批量详情接口验证：`GET /api/batch-runs/6` 返回 200，`GET /api/batch-runs/6/summary` 返回 200。
- 前端路由验证：`http://127.0.0.1:5173/batch-runs` 返回 200。
- Dashboard summary 验证：`/api/dashboard/summary` 返回 `total_batches=6`、`avg_pass_rate=68.89`、`recent_batches=5`、`failed_rules_top5=5`。

当前仍存在的问题：
- 批量执行目前是同步串行执行，适合演示和中小规模评测；更大规模评测后续可引入后台任务队列、进度轮询和取消能力。
- 当前通过率阈值按 `total_score >= 60` 计算，后续如比赛评分标准不同，可抽成配置。
- 当前多模型统计只做 provider 级平均分、通过率、延迟对比，没有强制做显著性检验或复杂 A/B 分析。
- 批量页面已通过构建和路由验证，未使用浏览器截图工具做视觉留档。

下一步建议：
- 在浏览器中手动执行一次单模型批量评测，确认批量页面进度、报告列表和失败规则 TOP5 的可读性。
- 如果评测规模扩大，优先把批量执行改为异步任务，并增加运行中状态刷新。
- 后续再考虑模型配置页，把 provider、base_url、model_name 的管理从环境变量和下拉框扩展为可配置页面。

AGENTS.md 更新说明：
- 本次新增批量评测 API、持久化模型、批量服务和 Dashboard 验证标准，属于长期平台能力和核心架构变化，因此已更新 `AGENTS.md`。

### 2026-05-15：第四阶段前端可解释报告展示改造

本次修改日期：
2026-05-15

本次修改目标：
继续第四阶段改造，在不重建项目、不大改现有深色科技风 UI 的前提下，让前端更清晰展示“可解释、可量化”的评测报告，并统一平台定位文案为“复杂外呼任务对话模型自动评测平台”。

修改文件：
- `frontend/src/App.vue`
- `frontend/src/pages/Dashboard.vue`
- `frontend/src/pages/RunConsole.vue`
- `frontend/src/components/ConversationTimeline.vue`
- `frontend/src/components/FailureTable.vue`
- `frontend/src/pages/ReportDetail.vue`
- `frontend/src/pages/TaskDetail.vue`
- `frontend/src/pages/CaseList.vue`
- `backend/app/schemas/run.py`
- `backend/app/api/runs.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/target_model_client.py`
- `docs/当前进度.md`

已完成内容：
- 统一侧边栏和数据大屏的平台文案为“复杂外呼任务对话模型自动评测平台”，避免写成真实外呼系统或普通客服系统。
- `RunConsole.vue` 保持三栏布局，左侧新增被测模型 provider 选择，并继续展示 `task_type`、`model_provider`、`model_name`；中间对话记录通过 `ConversationTimeline` 展示每轮用户意图、情绪、耐心、目标进度和结束状态；右侧继续展示总分、雷达图、命中规则、失败规则和完整报告入口。
- 为 `POST /api/runs/start` 增加可选 `model_provider`、`model_name` 入参，并让 `EvaluationService` 按本次 run 的选择初始化 `TargetModelClient`，使前端 provider 选择能够真实进入评测链路。
- `ReportDetail.vue` 增强为专门的可解释报告页，展示任务名称、用例名称、被测模型 provider、模型名称、平均响应时间、总轮数、总分、雷达图、评分公式、命中规则、失败规则、LLM 评估结论、指标扣分原因、证据链、失败案例、优化建议和完整对话记录。
- `ReportDetail.vue` 会额外拉取任务、用例和 run 信息，用于补齐报告页上的任务/模型元信息；字段缺失时使用安全兜底，避免白屏。
- 空字段兜底文案已调整为“暂无失败规则”“暂无明显扣分原因”“暂无优化建议”等明确提示。
- `TaskDetail.vue` 增加显式“数据来源”展示，继续折叠展示 Role、Task、Opening Line、Call Flow、Knowledge Points、Constraints 和完整原始指令，并扩展关联测试用例表格的初始问题与规则摘要。
- `CaseList.vue` 保持原表格结构，补充无规则时的“暂无规则”兜底显示。

验证结果：
- 前端构建：`npm run build` 通过；仍有既有 Vite chunk size 和第三方 `#__PURE__` 注释 warning，不影响页面运行。
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `13 passed`。
- 当前服务连通性：`http://127.0.0.1:8080/docs` 返回 200，`http://127.0.0.1:5173` 返回 200。
- 前端路由连通性：`/dashboard`、`/tasks`、`/cases`、`/runs`、`/tasks/{task_id}`、`/reports/101` 均返回 200。
- 实际评测链路验证：通过 API 选择飞毛腿任务和首个测试用例，使用 `mock_baseline` 启动一次评测，生成 `run_id=101`、`report_id=101`；报告包含 `total_score=73.02`、`total_turns=3`、6 项指标解释、3 条证据消息、5 条命中规则和 5 条失败规则。

当前仍存在的问题：
- 本轮以构建、接口、路由和评测链路验证为主，未使用浏览器截图工具做视觉截图留档。
- 报告页已经展示 LLM 评估结果和证据链，但旧历史报告如果缺少新字段，会以兜底文案展示，证据丰富度取决于当次后端报告数据。
- `openai_compatible` 和 `custom_endpoint` provider 仍依赖后端环境变量配置真实地址和 Key；未配置时后端会按既有逻辑回退 mock，适合离线演示。
- 前端包体仍有 Vite chunk size warning，后续如需要上线体验可再做路由级拆包。

下一步建议：
- 用浏览器手动点击一次完整演示链路：数据大屏 → 评测任务详情 → 测试用例筛选 → 开始评测 → 查看完整报告，重点确认报告页证据链可读性。
- 继续根据真实演示样例优化 `metric_explanations` 和 `failure_cases` 的证据文本质量。
- 暂不新增批量评测或大改 UI，优先保证默认两个脱敏任务的单次评测报告稳定、清晰、可解释。

AGENTS.md 更新说明：
- 本次主要是前端报告展示增强和一个小的 run provider 入参兼容，不涉及长期规则、核心架构、启动方式、禁止事项或重要验证标准变化，因此未更新 `AGENTS.md`。

### 2026-05-15：第三阶段混合评测体系改造

本次修改日期：
2026-05-15

本次修改目标：
继续第三阶段改造，把评分模块升级为“规则评分 + LLM 评估器”的混合评测体系，确保评分报告围绕被测对话模型在复杂任务指令下的表现，而不是评价本系统代码质量。

修改文件：
- `backend/app/services/rule_judge.py`
- `backend/app/services/agents/evaluator_agent.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/report_service.py`
- `backend/app/models/report.py`
- `backend/app/schemas/report.py`
- `backend/app/api/reports.py`
- `backend/app/core/config.py`
- `backend/app/core/database.py`
- `backend/.env.example`
- `backend/tests/test_reports.py`
- `docs/当前进度.md`
- `AGENTS.md`

已完成内容：
- 保留并增强 `rule_judge.py`：硬规则继续负责必达流程、禁止规则、字数限制、业务串场、用户打断、安全结束、回答用户问题、机械重复等检查。
- 飞毛腿任务硬规则已调整为第三阶段清单：确认骑手身份、合同生效、开始配送、单日/多日完成要求、未完成影响、安抚、安全、排名非站长干预、退出流程、超出职责范围回电说明。
- 飞毛腿禁止规则已覆盖：额外资格、排名由站长决定、恶劣天气强迫配送、忽略骑手拒绝、长篇大论、超过 30 字、旧客服内容、机械重复、用户追问不推进。
- 课程直播任务硬规则已调整为第三阶段清单：负责人确认、非负责人转达、标准直播/低延迟直播、实时互动场景、发布方式、Web/第三方路径、费用差异、企业微信添加逻辑、结束前确认问题。
- 课程直播禁止规则已覆盖：开车继续推销、承诺优惠券/折扣、不合适语气词、超过 15-20 字、长篇正式解释、打断后继续大段说明、不给商家发言机会、机械重复、用户追问不推进。
- 新增 `backend/app/services/agents/evaluator_agent.py`，默认 mock evaluator，预留 `openai_compatible` provider。
- `.env.example` 和 `config.py` 新增 evaluator 配置：`EVALUATOR_PROVIDER`、`EVALUATOR_API_KEY`、`EVALUATOR_BASE_URL`、`EVALUATOR_MODEL`。
- `evaluation_service.py` 已从旧 `LLMJudge` 切到 `EvaluatorAgent`。
- `report_service.py` 合并规则评分与 LLM 评估结果，最终总分使用六项指标加权公式。
- 报告模型、schema 和 API 新增 `matched_rules`、`llm_judge_result`，并保留 `failed_rules`、`failure_cases`、`evidence_messages`、`metric_explanations`、`score_formula`。
- SQLite 自动补列逻辑已新增 `matched_rules` 和 `llm_judge_result`，避免旧数据库缺列。
- `test_reports.py` 增加对 `matched_rules`、`llm_judge_result`、六项 LLM 指标、证据、建议和总分权重的断言。

验证结果：
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `13 passed`。
- `test_reports.py` 已覆盖 `POST /api/runs/start` 后再 `GET /api/reports/{report_id}` 的报告读取链路，确认报告包含总分、各指标分、命中规则、失败规则、LLM 评分、证据消息、失败案例、扣分原因、建议和总分公式。
- 单独再次执行报告链路命令时，当前工具层出现 sandbox setup 错误；未作为项目验证结果记录。

当前仍存在的问题：
- 当前 LLM 评估器默认是 mock evaluator，适合离线演示；如果接入真实 openai compatible，需要用真实模型样本复测评分稳定性。
- 飞毛腿和课程直播硬规则已覆盖第三阶段清单，但部分规则在短对话自然结束时可能仍被判为遗漏，后续需要继续优化“场景相关必达”和“全局必达”的权重。
- 前端报告页目前复用已有展示区，未额外新增 LLM 评分专属 UI；API 已返回 `llm_judge_result`。

下一步建议：
- 用浏览器打开完整报告页，重点确认 `failure_cases`、`metric_explanations`、`evidence_messages` 的可读性。
- 继续优化规则评分的上下文适配，避免不相关场景硬性要求所有分支规则。
- 如进入第四阶段，可增强报告页对 LLM 评估结果和证据链的专门展示，但仍不建议大改 UI。

AGENTS.md 更新说明：
- 本次新增混合评测体系、EvaluatorAgent、evaluator 配置和报告必备字段，属于长期架构和重要验证标准变化，因此已更新 `AGENTS.md`。

### 2026-05-15：第二阶段用户模拟器 Agent 改造

本次修改日期：
2026-05-15

本次修改目标：
继续第二阶段改造，将用户模拟器升级为“被外呼对象模拟器 Agent”，使其根据任务指令、用户画像、用例目标、规则约束、对话历史和当前轮次动态生成用户发言，不再依赖固定轮次模板作为主链路。

修改文件：
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/user_simulator.py`
- `backend/app/services/evaluation_service.py`
- `backend/tests/test_dialogue_deduplication.py`
- `docs/当前进度.md`
- `AGENTS.md`

已完成内容：
- 重写 `UserSimulatorAgent`，每轮输入覆盖 `instruction_text`、`task_type`、`user_profile`、`initial_message`、`expected_goals`、`required_rules`、`forbidden_rules`、`messages`、`turn_index`。
- `user_state` 已扩展为包含 `persona`、`emotion_level`、`patience`、`info_completeness`、`goal_progress`、`interruption_count`、`current_intent`、`task_type`、`profile`。
- 飞毛腿任务支持愿意配送、不想配送、抱怨恶劣天气、质疑排名、追问合同影响、咨询退出流程等用户类型。
- 课程直播任务支持负责人、非负责人、忙碌商家、开车商家、追问直播区别、价格敏感商家、技术不熟悉商家等用户类型。
- Agent 会根据上一轮被测模型回复动态变化：未回答则追问，过长则“你说重点”，重复则降低耐心，说明清楚则接受，违反约束则不满或结束。
- 开车场景下，如果模型继续介绍直播、低延迟、费用或配置且未说明稍后再打，用户会拒绝并结束通话。
- 飞毛腿恶劣天气场景下，如果模型强迫冒雨或“不跑不行”，用户会情绪升级并结束。
- `user_simulator.py` 继续作为兼容入口，但主路径转调 `UserSimulatorAgent`。
- `evaluation_service.py` 已兼容新输出格式，将 `content` 保存为用户消息，并将 `intent`、`user_state`、`should_continue` 写入 message detail；当模型主动礼貌收尾时，本轮 detail 中的 `goal_progress` 会标记为 `accepted`。
- 补充 pytest：直接验证恶劣天气强迫配送会触发 rejected、开车场景继续推销会触发 rejected；集成验证开车场景正常稍后再打时提前结束且状态 accepted。

验证结果：
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `13 passed`。
- 后端接口：`http://127.0.0.1:8080/docs` 返回 200。
- 实际接口验证 1：飞毛腿“抱怨恶劣天气”共 3 轮。用户首轮抱怨下雨；模型提醒安全后，用户转为“晚点看雨小不小”；模型说明雨天资格后，用户接受并注意安全，metadata 中 `goal_progress=accepted`。
- 实际接口验证 2：课程直播“商家说在开车”共 1 轮。用户说在开车，模型回复“稍后再打、注意安全”，评测提前结束，metadata 中 `goal_progress=accepted`。
- Agent 直测：飞毛腿天气场景下模型强迫冒雨会输出不满结束，`goal_progress=rejected` 且 `emotion_level>=5`；课程开车场景下模型继续推销会输出不满结束，`goal_progress=rejected` 且耐心降至低位。

当前仍存在的问题：
- 当前 Agent 仍是可控规则型 Agent，不是外部 LLM 驱动的开放式用户模拟器；优点是演示稳定，缺点是新增任务需要继续扩展画像和阶段规则。
- `user_simulator.py` 中仍保留旧兼容方法，但主链路已不再调用；后续可在稳定后清理旧方法以降低维护成本。
- 前端暂未新增用户状态详情展示，只通过现有 message detail 和报告规则体现。

下一步建议：
- 继续第三阶段前，先在浏览器演示页复测飞毛腿天气、飞毛腿退出、课程开车、课程追问区别四个用例。
- 下一步可增强规则评分和 LLM-as-a-Judge，让报告更好解释用户状态变化、拒绝原因和提前结束原因。
- 在更多 Excel 任务导入前，为 `generic_outbound` 增加更通用的画像推断和追问策略。

AGENTS.md 更新说明：
- 本次将用户模拟器主链路升级为 `UserSimulatorAgent.generate_message(...)`，并固化了 Agent 输入、输出和动态行为要求，属于长期架构/规则变化，因此已更新 `AGENTS.md`。

### 2026-05-15：修复被测模型和用户模拟器重复回复

本次修改日期：
2026-05-15

本次修改目标：
只修复多轮对话中被测模型和用户模拟器机械重复回复的问题，暂停第二阶段、第三阶段和批量评测能力建设。

修改文件：
- `backend/app/services/dialogue_state.py`
- `backend/app/services/target_model_client.py`
- `backend/app/services/agents/user_simulator_agent.py`
- `backend/app/services/user_simulator.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/tests/test_dialogue_deduplication.py`
- `backend/tests/test_runs.py`
- `docs/当前进度.md`

已完成内容：
- 新增 `analyze_dialogue_state(...)`，从历史 messages 中提取当前轮次、最后用户/模型消息、模型已说主题、用户已问主题、当前阶段、是否已回答和是否应结束。
- `TargetModelClient.generate_reply(...)` 已接入对话状态，飞毛腿任务和课程直播任务不再只按 task_type 输出固定模板，而是按用户意图、已说主题和当前阶段推进。
- 增加 `deduplicate_assistant_reply(...)`，对历史 assistant 回复做完全相同、高相似度和核心句式重复检测，命中后替换为下一步推进话术。
- 新增 `UserSimulatorAgent` 状态机，用户模拟器会根据模型上一轮回复、已问问题和目标进度切换追问或结束，不再反复问同一句。
- `backend/app/services/user_simulator.py` 保留旧入口，但现在转调 `UserSimulatorAgent`，避免继续走旧模板循环。
- `evaluation_service.py` 支持提前结束：当用户模拟器返回 `should_continue=false` 或被测模型返回 `should_close=true` 时，不再机械跑满 `max_turns`。
- `rule_judge.py` 增加失败规则 `禁止机械重复回复` 和 `必须根据用户追问推进流程`，并为连续高相似回复、用户追问路径/影响但模型重复基础介绍的情况提供证据。
- 新增 pytest 覆盖飞毛腿合同影响、飞毛腿正常愿意配送、课程直播追问区别、课程直播开车提前结束四类重复回复场景。

验证结果：
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `11 passed`。
- 前端构建：`npm run build` 通过；仍有 Vite chunk size 和第三方注释 warning，不影响本次功能验证。
- 旧关键词扫描：`rg -n "退款|订单号|手机号后四位|商家出餐|平台超时|配送状态|超时节点|投诉|酒店|团购券|核销" backend/app/services backend/tests` 只命中测试断言、`TargetModelClient` 禁止词列表、`RuleJudge` 禁止串用旧客服流程检测列表；未命中当前回复生成模板。
- 后端 8080 已验证，`http://127.0.0.1:8080/docs` 返回 200。
- 前端 5173 已验证，`http://127.0.0.1:5173` 返回 200。
- 实际接口验证 1：飞毛腿任务、询问合同影响，2 轮结束；模型依次回复“单日需完成 X 单，否则合同和派单可能受影响。”、“好，注意安全，后续有问题再联系。”，未连续重复。
- 实际接口验证 2：课程直播任务、追问直播区别，4 轮结束；模型先说明“标准延迟约 5-10 秒，低延迟约 1-2 秒。”，用户追问“那具体在哪里选？”后模型推进到“您是用 Web 控制台还是第三方系统？”，再根据第三方系统回复“进入直播平台管理，勾选低延迟直播。”。
- 实际接口验证 3：课程直播任务、商家说在开车，1 轮结束；模型回复“那我稍后再打，您注意安全。”。

当前仍存在的问题：
- 当前状态机主要覆盖默认两类脱敏任务，后续新增 Excel 任务时需要继续扩展 `dialogue_state.py` 的主题识别和阶段推进。
- 真实外部模型 provider 返回内容虽会进入任务类型校验和去重，但外部模型的语义重复仍需要更多线上样本复测。
- 规则评分中部分“必须覆盖”的任务规则仍可能因对话提前自然结束而显示遗漏，后续需要区分“自然结束”与“未覆盖”的扣分权重。

下一步建议：
- 先在浏览器演示页继续复测默认任务，确认多轮展示和接口结果一致。
- 重复回复稳定后，再继续增强用户模拟器 Agent 的更多用户画像分支。
- 暂缓批量评测和模型配置页扩展，避免在核心单轮/多轮链路未稳定前扩大变更面。

AGENTS.md 更新说明：
- 本次属于普通 bug 修复和状态机实现，没有改变长期规则、核心架构、启动方式、禁止事项或重要验证标准，因此未更新 `AGENTS.md`。

### 2026-05-15：修复被测模型回复串场问题

本次修改日期：
2026-05-15

本次修改目标：
只修复被测模型回复在飞毛腿任务、课程直播任务和旧客服 mock 之间串场的问题，暂停后续阶段功能建设。

修改文件：
- `backend/app/services/target_model_client.py`
- `backend/app/services/evaluation_service.py`
- `backend/app/services/rule_judge.py`
- `backend/app/services/llm_client.py`
- `backend/app/services/user_simulator.py`
- `backend/app/seed/sample_data.py`
- `backend/app/seed/import_instruction_excel.py`
- `backend/app/api/dev.py`
- `backend/main.py`
- `backend/app/schemas/run.py`
- `backend/tests/test_task_reply_isolation.py`
- `frontend/src/pages/RunConsole.vue`
- `backend/README.md`
- `AGENTS.md`

已完成内容：
- 确认当前实际评测调用链为：`POST /api/runs/start` → `EvaluationService.start_evaluation` → `TargetModelClient.generate_reply`，主评测链路不经过 `TargetBot`。
- 在 `evaluation_service.py` 和 `target_model_client.py` 增加调用链日志，并把每轮 `target_call_chain`、`task_type`、`model_provider`、`model_name` 写入消息 detail，便于前端和接口排查。
- 重写 `TargetModelClient.generate_reply(...)`，按 `task_type` 分发到 `generate_rider_reply(...)`、`generate_course_reply(...)`、`generate_generic_outbound_reply(...)`。
- 增加 `infer_task_type(...)`：空 task_type 时根据飞毛腿/骑手/配送/派单、课程/直播/低延迟/机构/负责人推断任务类型。
- 增加 `validate_reply_by_task_type(...)`：飞毛腿或课程直播回复命中跨任务/旧客服禁词时，直接替换为对应任务的安全 fallback 话术。
- 清理 `llm_client.py` 中旧客服 mock fallback，避免旧 assistant mock 被历史兼容入口误用。
- 将默认 fallback seed 数据改为飞毛腿、课程直播和通用外呼任务，不再使用旧示例业务作为默认 fallback。
- 修复 `rule_judge.py` 中飞毛腿任务规则：只保留合同生效、开始配送、单日/多日完成要求、未完成影响、安抚、安全、报名排名、退出流程、恶劣天气安全和禁止串用旧客服流程。
- 飞毛腿回复命中旧客服流程词时，会产生高严重度失败规则 `禁止串用旧客服流程`。
- 新增开发接口 `POST /api/dev/reset-runs`，只清理 runs、messages、reports，不删除 tasks 和 cases。
- 新增 pytest 覆盖飞毛腿合同影响、飞毛腿恶劣天气、课程直播非负责人转达三类反串场场景。
- 在 `RunConsole.vue` 左侧评测配置区域临时展示 `task_type`、`model_provider`、`model_name`。
- 因本次固化了 `TargetModelClient.generate_reply(...)` 的任务类型白名单生成和最终校验架构，已同步更新 `AGENTS.md` 的长期防回归约束。

验证结果：
- 后端编译：`D:\Anaconda3\envs\callflow-lab\python.exe -m compileall app` 通过。
- 后端测试：`D:\Anaconda3\envs\callflow-lab\python.exe -m pytest`，结果 `7 passed`。
- 前端构建：`npm run build` 通过；仍有 Vite chunk size 和第三方注释 warning，不影响本次功能验证。
- 旧关键词扫描：`rg -n "退款|订单号|手机号后四位|商家出餐|平台超时|配送状态|超时节点|投诉|酒店|团购券|核销" backend` 只命中测试断言、`TargetModelClient` 禁止词列表、`RuleJudge` 禁止串用旧客服流程检测列表；未命中回复生成模板或默认 fallback seed。
- 后端已在 `http://127.0.0.1:8080` 启动，`/health` 可访问，`/docs` 返回 200。
- 前端已在 `http://127.0.0.1:5173` 启动，首页返回 200。
- 已调用 `POST /api/dev/reset-runs` 清理历史 run/message/report，首次清理结果为 runs 106、messages 445、reports 106。
- 实际接口验证 1：飞毛腿任务、询问合同影响，用例首轮用户为“如果我今天没完成 X 单会怎么样？”，assistant 返回“单日需完成 X 单，否则可能影响派单。”，未出现订单号、手机号后四位、退款、配送状态、超时节点等旧词。
- 实际接口验证 2：飞毛腿任务、抱怨恶劣天气，assistant 返回“理解，安全第一。能跑再接单。”，未出现旧客服流程内容。
- 实际接口验证 3：课程直播任务、非负责人转达，assistant 返回“麻烦您帮忙转达负责人，直播发布页升级了。”，未出现订单号、退款、骑手、飞毛腿等串场内容。
- 实际 run message detail 中已记录调用链：`POST /api/runs/start` → `EvaluationService.start_evaluation` → `TargetModelClient.generate_reply` → 对应任务生成器 → `TargetModelClient.validate_reply_by_task_type:pass`。

当前仍存在的问题：
- 当前真实外部被测模型接口仍是预留能力；如果配置 `openai_compatible` 或 `custom_endpoint`，外部回复会先进入任务类型校验，命中禁词会被替换为任务安全 fallback。
- 飞毛腿规则现在更严格，单个短回复可能不会覆盖所有必须规则，因此报告中仍可能出现遗漏规则；这是评分覆盖问题，不是串场问题。
- 前端调试字段为临时展示，后续演示稳定后可考虑保留到模型配置页或改为折叠调试信息。

下一步建议：
- 先在浏览器中用当前两个默认任务各跑一次，确认前端展示与接口验证一致。
- 串场问题稳定后，再继续增强用户模拟器 Agent、LLM-as-a-Judge 和可解释报告；不要在串场问题回归前推进批量评测或模型配置页。

### 2026-05-15：上下文文件与第一阶段记录

日期：
2026-05-15

修改目标：
建立项目上下文文件，并记录第一阶段被测模型接入层重构后的当前状态。

修改文件：
- `AGENTS.md`
- `docs/当前进度.md`

完成内容：
- 新增项目长期上下文与 AI 助手工作规范。
- 新增当前进度记录。
- 记录当前项目定位、核心任务、禁止旧业务 mock、核心文件、启动方式和验证标准。
- 记录第一阶段重构结果：`evaluation_service.py` 统一走 `target_model_client.py`，`target_bot.py` 仅保留兼容包装器。

验证结果：
- 文档文件已创建。
- 本次未修改前端、后端业务代码、数据库、接口、样式或业务逻辑。
- 最近一次业务验证结果：后端编译通过，后端测试 `4 passed`，Swagger `http://127.0.0.1:8080/docs` 可访问。
- 最近一次场景验证结果：飞毛腿天气场景未出现旧退款客服内容；课程直播场景未出现飞毛腿、骑手、配送、派单等串场内容。

当前仍存在的问题：
- 真实外部被测模型接口仍是预留能力，默认 provider 为 mock。
- 用户模拟器 Agent、LLM-as-a-Judge 和批量评测仍可继续增强。
- 历史文档或非核心路径中可能仍有旧示例描述，后续如影响演示再清理。

下一步建议：
- 优先完善用户模拟器 Agent，让不同被外呼对象更稳定地根据模型回复动态追问或终止。
- 增强 LLM-as-a-Judge 的语义评估和报告证据质量。
- 完善批量评测与模型配置页面，使平台更像完整评测产品。

## 七、后端启动方式

```bash
cd backend
uvicorn main:app --reload --port 8080
```

Swagger：

http://127.0.0.1:8080/docs

## 八、前端启动方式

```bash
cd frontend
npm install
npm run dev
```

前端地址：

http://127.0.0.1:5173

## 九、每次代码修改后的记录要求

每次 AI 修改完代码后，都必须更新本文件，至少记录：

1. 本次修改日期
2. 本次修改目标
3. 修改文件列表
4. 已完成内容
5. 当前验证结果
6. 当前仍存在的问题
7. 下一步建议

如果本次修改涉及长期规则、核心架构、启动方式、禁止事项或重要验证标准变化，再同步更新 AGENTS.md。

如果只是普通 bug 修复或页面调整，不要修改 AGENTS.md。
